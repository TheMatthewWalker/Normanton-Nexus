# CLAUDE.md (dotnet/)

Guidance for working in this subtree — the ASP.NET Core 10 rewrite of Normanton-Nexus, developed on the `dotnet-rewrite` branch alongside the live Node.js app at the repo root. See the repo root `CLAUDE.md` for the Node app; this file only covers `dotnet/`. The full migration plan (context, phased rollout, authorization redesign) lives in this session's plan file and should be re-derived/re-documented here as each phase lands, mirroring how `/home/user/SapServer/CLAUDE.md` documents that sibling rebuild.

## Status

**Phase 0 (Scaffold) done. Phase 1 (Foundation) done. Phase 2 (Engineering) done. Phase 3 (Quality) done. Phase 4 (Sales) done. Phase 5 (Finance) done — scoped, see below. Phase 6 (Production) backend done across all three sub-phases (6a/6b/6c) — several 6b/6c pieces (the label-printing entry-form integration, the draft→complete wizard's own multi-step UI, Drumming's entry form, Order Lookup/Drumming Ticket pages) have their frontend deliberately deferred as separately-scoped effort, tracked at each slice below, not blocking the next department.** See "Phase 6: Production" below for the full sub-phase split and why.

**Phase 7 (Warehouse) backend done across four sub-phases (7a master data + picking-materials panel, 7b pallet/package builder + staging reversal, 7c delivery completion pipeline + warning-log queues + `RedrumReversalHelper`, 7d Staging Post)** — frontend deliberately deferred, same precedent as every phase since Engineering. `sap.js`'s general Stock Transfer tool and the Batch Discrepancies supervisor tile (both referenced in `private/js/warehouse.js`) were NOT scoped into 7a-7d — flagged as remaining Warehouse-adjacent surface, not yet confirmed complete. See "Phase 7: Warehouse" below, including a flagged cross-cutting gap (permission-code migration skipped for both Production and Warehouse so far) found while closing out 7d.

**Phase 8 (Logistics) is now complete across all five sub-phases (8a Shipping, 8b Purchasing/Performance, 8c Customs, 8d Reference data, 8e Vendor Consignment Tracker).** Sub-phase 8d (Reference data) done; Sub-phase 8a (Shipping) functionally complete across 8a.1-8a.5c apart from `GET /analytics`; Sub-phase 8e (Vendor Consignment Tracker) complete across 8e.1 (DB/algorithm core) and 8e.2 (SAP sync + PDF); Sub-phase 8b (Purchasing/Performance) done across 8b.1 (shared forecast core + read-only dashboard routes), 8b.2 (vendor master data + demand adjustments + Isopar readings/planning-rate/stock-risk), 8b.3 (order suggestion engine), 8b.4 (inbound shipment tracking + Inbound Costs), 8b.5 (MRP Analysis forecasting), 8b.6 (consignment-customers CRUD + printable Production Plan + full refresh orchestration for Stock/Agreements/Invoicing/Otif/TurnsValClass/ValuationClasses/MrpAnalysisHistory + HMRC Tied Oil declarations + the dual-auth-scheme upload-notes route/orderbook-token credential exchange), and 8b.7 (Mark Received/Undo Received + elevated `create-po`/`regenerate-pdf`) — the one piece of 8b intentionally left out of scope is the live-formula orderbook Excel export itself. `GET /analytics` and `/turns-valclass/history`'s open-order integration now have everything they need on the DB side (`log.PurchaseOrderShipment` is fully modelled as of 8b.4) — still not wired up, tracked for a later pass. **Sub-phase 8c (Customs) — the last sub-phase to finish — is done across all four slices: 8c.1 (Customs Report admin CRUD), 8c.2 (French VAT/DDP Customs Report generation), 8c.3 (Kuehne+Nagel freight booking + document upload), and 8c.4 (ClearPort raw CDS export proxy).** 8a.5b (real SAP posting), 8a.5c (ClearPort), 8c.3 (Kuehne+Nagel), 8c.4 (ClearPort proxy), and 8e.2 (Consignment SAP sync + PDF) are all flagged as needing live-system verification before full trust — see their own sections below; every pure-algorithm piece across every sub-phase is already fully unit-tested. Frontend for the whole of Phase 8 remains deliberately deferred as separately-scoped effort, same precedent as every phase since Engineering. See "Phase 8: Logistics" below.

**Phase 9 (Admin + Management) backend is now complete — Sub-phase 9a (User &amp; Permission Administration) and 9b (DB Explorer + BAPI Inspector) both done; `deploy.js` read in full and deliberately deferred to Phase 10 (its scheduled-restart design assumes Node's Windows-Service hosting, which this app's IIS/ANCM model doesn't have — the plan itself says re-evaluate the *design*, not port it).** Admin's own frontend and the full permission-group CRUD UI remain deferred — see "Phase 9: Admin + Management" below for a real sequencing gap found while closing this out (Production/Warehouse/Logistics all deferred their per-tile permission-code split to Phase 10, so the full group-management UI has nothing fine-grained to manage yet for 3 of 8 departments). Next: Phase 10 (Cross-cutting closeout).

**Real, confirmed verification** (not just "it compiles"): `dotnet run` in Development — which enables ASP.NET Core's strictest DI validation (`ValidateOnBuild`/`ValidateScopes`) — boots clean with no DI graph errors across every service registered so far, including Quality's SSE-streaming bulk endpoint and its dynamic per-direction `IAuthorizationService` check. Real HTTP requests confirmed: `GET /Login` → 200 with correctly rendered HTML; unauthenticated department Razor Pages → 302 to `/Login?ReturnUrl=...`; unauthenticated `[ApiController]` JSON endpoints → 401 (not a redirect — matters, since `session-guard.js` keys off exactly that distinction); unmatched route → clean 404; `GET /health` → 200. This confirms the whole auth/routing/authorization pipeline wiring is real and working, not just type-checked.

**Not yet verified anywhere in this migration** (no live SQL Server or SapServer reachable in this sandbox): an actual login round-trip, session persistence across a simulated restart, either idle-timeout variant firing end-to-end, any real SapServerClient call, any real Dapper query. Same caveat class as SapServer's own repeated "confirmed for real in production" callouts — none of this has had that pass yet. Manual click-through of every department is likewise still pending for the same reason.

## Phase 1: Foundation

- Three Dapper connection factories (`Services/Sql/`) — `INexusDb`/`INexusOperationsDb`/`INexusArchiveDb`, each just a captured connection string, safe as singletons.
- EF Core migrations (`Data/Migrations/`, tooling-only — see `Data/NexusMigrationContext.cs`): `EnsureCoreSchema` idempotently creates `PortalUsers`/`PortalUserDepartments`/`PortalPermissions`/`PortalUserPermissions`/`PortalSessions`/`PortalAuditLog` if missing (guarded, safe to run against a DB the Node app's knex migrations already touched); `AddPermissionGroups` adds the new `PortalPermissionGroups`/`PortalPermissionGroupPermissions`/`PortalUserPermissionGroups` tables (see the plan's "Authorization model"). Verified with `dotnet ef migrations script` — real `dotnet ef database update` against a real SQL Server is still unverified.
- Cookie authentication with a SQL-backed `ITicketStore` (`Services/Auth/PortalSessionStore.cs`) — the direct analog of `lib/sqlSessionStore.js`, reusing the same `PortalSessions` table shape (not the same row *format* — this app's `SessionData` holds a serialized `AuthenticationTicket`, not Node's JSON session blob; users re-log-in once at cutover regardless, per the plan). Cookie `SecurePolicy` is `Always` only in Production — `SameAsRequest` everywhere else, so a plain `dotnet run` + `http://localhost` smoke test actually gets the cookie back (fixed after the `Always`-everywhere default silently broke local-HTTP session persistence).
- Per-user variable idle timeout (`Services/Auth/IdleTimeoutPolicy.cs` + `IdleTimeoutValidation.cs`) — 30 min default / 5 min `ShortIdleTimeout`, matching `config.js`'s `idleTimeoutMsFor` exactly, applied every request via `CookieAuthenticationEvents.OnValidatePrincipal` (the C# equivalent of `server.js`'s per-request `cookie.maxAge` reset under `rolling:true`).
- Login flow (`Pages/Login.cshtml(.cs)` + `Services/Auth/AuthService.cs`) — faithful port of `routes/auth.js`'s `POST /login`: hardcoded-hash dummy bcrypt compare for unknown usernames, lockout at 10 failed attempts (permanent, admin-unlock only — matches `useradmin.js`, no time-based auto-unlock), IP-partitioned rate limiting (10/15 min, matching `express-rate-limit`), a fresh session key on every successful login (session-fixation defense — no explicit `regenerate()` call needed, since `PortalSessionStore.StoreAsync` always mints a new key).
- `MustChangePassword` enforcement (`Services/Auth/MustChangePasswordPageFilter.cs` + `Pages/ChangePassword.cshtml(.cs)`) — global `IAsyncPageFilter` (registered via `AddMvcOptions` so it only ever runs for Razor Page requests, never `[ApiController]`/static assets) redirecting to a real dedicated page instead of Node's `landing.js` blocking modal. Re-signs-in with the claim dropped on success so the live session updates immediately, not just the DB row.
- Role (`operator`<`admin`<`superadmin`, superadmin bypasses) + department + the new per-tile-permission authorization gates, all independent (never ANDed on one route, matching `middleware/auth.js`) — `Services/Auth/AuthorizationRequirements.cs` + `NexusPolicyProvider.cs` lets a controller write `[Authorize(Policy = "Perm:WAREHOUSE_STOCK_ADJUST")]` / `"Dept:warehouse"` / `"Role:admin"` without pre-registering every policy name at startup.
- `Services/Auth/PermissionResolver.cs` — effective permission set = direct `PortalUserPermissions` grants UNION every group's permissions via `PortalUserPermissionGroups`/`PortalPermissionGroupPermissions`, computed once at login and baked into the ticket as claims (cheap since the ticket lives server-side in SQL, not the browser cookie — no cookie-size ceiling on permission count as tile-level codes accumulate).
- `Pages/Admin/PermissionGroups*` — minimal (not the full Phase 9 UI) group create/list/manage screen, `Role:admin` gated.
- `Models/ApiResponse.cs` + `NexusExceptions.cs` + `Middleware/ApiExceptionMiddleware.cs` — the `{success,data,error}` envelope every department `[ApiController]` returns, mapped from a small exception hierarchy (C# analog of SapServer's `ApiResponse<T>`/`SapExceptionMapper`). Scoped to `/api/*` only — Razor Page requests still go through the normal HTML error page.
- `Services/SapServerClient.cs` — typed `HttpClient` wrapper matching `sap.js`'s `makeSapToken`/axios pattern (shared-secret JWT: `{userId}`, issuer `normanton-nexus`, audience `sap-server`, 60s expiry). Originally planned for the Quality phase, built here once Engineering turned out to need it first. TLS pinning (`sap.js`'s `certs/sap-server-cert.pem`) is NOT ported — uses the system trust store; revisit once both apps are deployed against real certs.
- `Pages/Shared/_Layout.cshtml` + `wwwroot/css/site.css` — header/page-title-bar structure matches the Node app's real markup (confirmed by diffing three department HTML files — it's copy-pasted per department in Node, not templated). Styling is a clean, functional approximation, **not** pixel-accurate (`logistics.css`/`nexus-common.css` never fully captured) — department SVG badge icons also simplified to text-only. Revisit for visual parity once worth polishing.
- `Pages/Index.cshtml(.cs)` — the Hub landing page (port of `private/landing.html`), listing tiles for every department the current user belongs to (superadmin sees all 8).
- `wwwroot/js/shared/session-guard.js` — line-for-line port of `private/js/session-guard.js` (global `fetch` monkey-patch: 401 or a non-JSON body redirects to `/Login?error=session_expired`, deduped via `sessionStorage`). Must stay the first, non-deferred `<script>` in `<head>` — see `_Layout.cshtml`.
- `GET /health` — unauthenticated liveness check for IIS Application Initialization warm-up + external monitoring, matching SapServer's own `HealthController`.

**Deliberately deferred, not forgotten**: `notifications.js` and `deploy-banner.js` both need real backing features (a `Notifications`/`NotificationDeliveries` schema + `/api/notifications/*`; a `ScheduledDeployments` table + `/api/deploy/next`) that were never fully schema-confirmed during research — only inferred from `SELECT` queries, not `CREATE TABLE` statements. Come back to these once a department phase actually needs one, or as a dedicated small foundation follow-up, with the same schema-confirmation rigor `EnsureCoreSchema` got.

## Phase 2: Engineering (reference implementation for every later department phase)

Full vertical slice for Packaging Data (3 tiles), ported from `routes/packaging.js` + `private/engineering.html`/`private/js/engineering.js`:
- `Controllers/EngineeringController.cs` (`[Route("api/packaging")]`, same URL prefix Node used — only page URLs change in this migration, not the JSON API shape) + `Helpers/Engineering/EngineeringHelper.cs` (100% of the logic — thin-controller pattern starts here) + `Models/Dto/EngineeringModels.cs`.
- 8 of 11 routes proxy to SapServer's own `PackagingController`; DTOs mirror `SapServer/Models/Bapi/PackagingModels.cs` field-for-field (read directly from that repo, not guessed).
- **A deliberate, documented security tightening over Node's literal current behavior**: Node's write routes check ONLY `requirePermission('MASTER_DATA')`, never additionally `requireDepartment('engineering')` — the two gates are independent in Node and a route picks exactly one. `EngineeringController` requires BOTH (`Dept:engineering` at the class level, `Perm:ENG_*` per write action) — matches the plan's intent that permission groups are the tile-access mechanism *within* an already-accessible department, not a substitute for it. Strictly more restrictive than Node, never less — this same pattern repeats in every later department.
- `Services/Auth/SapCredentialCipher.cs` — the "New Packaging Creation" tile needs a user's own saved SAP password (`PortalUsers.SapPasswordEncrypted`) to call SapServer's elevated `create-elevated` endpoint. Byte-for-byte AES-256-GCM port of `lib/sapCredentials.js` (12-byte IV, 16-byte tag, `base64(IV‖tag‖ciphertext)`, UTF-8 plaintext), read from the real Node source. Round-trip + tamper-detection tests confirm internal correctness; byte-level interop with a real Node-encrypted value is unverified (no shared key/live DB here).
- `Data/Migrations/20260902054517_SeedEngineeringPermissions.cs` — first real run of the per-department permission-migration path: defines `ENG_MASS_UPDATE`/`ENG_NEW_PACKAGING`/`ENG_INSTRUCTION_DETAIL`, creates a default "Engineering Master Data" group bundling all three, migrates every existing `MASTER_DATA` grant into it. Legacy `MASTER_DATA` is left in place (not deleted) — full retirement is a later cleanup once confirmed against a real database.
- `Pages/Engineering/{Index,MassUpdate,NewPackaging,InstructionDetail}.cshtml(.cs)` + `wwwroot/js/engineering/{mass-update,new-packaging,instruction-detail}.js` — one real page and one dedicated JS file per tile, replacing `engineering.js`'s single-file dispatch-map/innerHTML-swap.
- 14 Helper tests + 4 controller tests.

## Phase 3: Quality

Stock Information (Display/Block/Unblock Stock) + Traceability Concessions, ported from `routes/quality.js` + `private/quality.js`/`.html`, and the review slice of `routes/productionnexus.js`'s concession endpoints.

- **A confirmed, deliberate behavioral fork found during research, not a guess**: `quality.js` signs every SapServer JWT with a *fixed* `{userId: 0}` service identity (its own local `makeSapToken()`, no argument) — distinct from `packaging.js`, which passes the real calling user. Preserved exactly (`QualityHelper.SapServiceUserId`), not "corrected," since SapServer-side `SapDepartmentPermissions` provisioning for these RFCs may already be keyed to that fixed identity. Locked in by a dedicated test.
- **Display Stock deliberately does NOT call SapServer's own `QualityController.GetBlockedStock`** (blocked-only, `BESTQ EQ 'S'`) — the real Node frontend never calls it either; it builds its own unfiltered `ZRFC_READ_TABLES` call for *all* LQUA stock in warehouse 312 and colors blocked rows client-side, gated only by department (no permission code). `QualityHelper.DisplayStockAsync` replicates that exact live behavior via SapServer's generic `api/rfc/execute` — confirmed `GetBlockedStock` is dead code from the real UI's perspective, not something this skips by accident.
- Block/Unblock proxy straight through to SapServer's `QualityController` (`api/quality/block`/`unblock`) — all WM-transfer-order-leg/bin-existence-check complexity stays on the SapServer side; DTOs mirror `SapServer/Models/Bapi/QualityModels.cs` field-for-field.
- **Bulk block/unblock is a real Server-Sent-Events stream** (`QualityController.Bulk`, `text/event-stream`, written directly to `Response.Body`) — the required permission depends on `body.Direction`, known only after model binding, so it's checked via injected `IAuthorizationService` rather than a static `[Authorize(Policy=...)]`. Frontend reads the stream manually via `response.body.getReader()`.
- Split legacy `QUAL_BLOCKING` (covered block AND unblock, including both bulk directions, with one code) into `QUAL_BLOCK_STOCK`/`QUAL_UNBLOCK_STOCK`. `QUAL_CONCESSION` maps to `QUAL_TRACEABILITY_CONCESSION` for the review action only.
- **Traceability Concessions reuses Production-domain data** (`prod.TraceabilityConcessions`, cross-database-joined to `Nexus.dbo.PortalUsers`). Ported the core read/approve/reject surface; deliberately did **not** port two Node side effects — a production-batch event-log write (`writeEvent`) and an in-app notification (`notify()`) — both depend on systems not built yet. The Production-side "raise a concession" action isn't ported at all yet.
- Simplified Node's right-click context-menu to a plain inline per-row action link (same API call, more discoverable, no client-side permission-based hide/show — the API's 403 is the real gate either way).
- 12 Helper tests (SSE row-result mapping, SAP-formatted quantity parsing — including the not-obviously-correct "any all-period no-comma string is thousands-grouped" behavior Node's own bulk loop has, ported faithfully rather than silently "fixed" — the fixed service-user-id, audit-message formatting, WM-vs-non-WM field gating).

## Phase 4: Sales

Customer Standard Instructions (SQL-only CRUD + bulk import) + the shared Production Schedule report + Schedule Agreement Waterfall (proxies to SapServer), ported from `routes/sales.js`, `routes/salessap.js`, `routes/productionschedule.js`/`productionschedulesql.js`, and `private/js/sales.js`/`production-schedule.js`/`salesWaterfall.js`/`salesWaterfallPivot.js`.

- **First Helpers in this migration to touch Dapper/SQL directly** (Engineering/Quality were both SAP-proxy-only) — `Helpers/Sales/SalesHelper.cs` (`log.CustomerStandardInstructions`) and `Helpers/ProductionSchedule/ProductionScheduleHelper.cs` (`log.AgreementSnapshot`/`prod.ProductionScheduleComments`/`log.OrderFulfillmentTracking`). Confirms the three-connection-factory + Dapper wiring compiles and DI-resolves correctly, though — same caveat as everywhere else — no real query has run against a live SQL Server yet.
- **`ProductionScheduleHelper`'s schema note**: the SQL migration *comments* elsewhere in this codebase reference a `dbo` schema for this feature, but the real Node querying code (`productionschedulesql.js`) uses `log.AgreementSnapshot`/`prod.ProductionScheduleComments`/`log.OrderFulfillmentTracking` (schema-qualified) — this port follows the real querying code, not the stale-looking comment. Confirm against the real `NexusOperations` database before trusting this.
- **`AgreementSnapshotColumns`'s `OriginalDoc`/`OriginalDocItem` → `ReferenceDocument`/`Item` column aliasing is deliberate and load-bearing**, not a naming preference — the raw `AgreementSnapshot` columns flip meaning once SAP creates a delivery against the line, so the alias is what keeps the C# model's field names accurate across that transition (flagged by research as critical to preserve).
- Split legacy `SALES_SUPERVISOR` (which covered both Sales' own Customer Standard Instructions writes AND, via Node's `requireAnyPermission(['PROD_SUPERVISOR','SALES_SUPERVISOR'])`, the shared Production Schedule comment/ETA edit) into two codes: `SALES_CUSTOMER_INSTRUCTIONS` (Sales-only) and `PROD_SCHEDULE_EDIT` (shared with Production). `Data/Migrations/20260902061656_SeedSalesPermissions.cs` creates both default groups; the `PROD_SCHEDULE_EDIT` group's holder-migration query is the first one in this codebase with a genuine multi-value `WHERE PermissionCode IN (...)` source filter (every earlier migration filtered on one legacy code, where `UQ_UserPermission` alone guarantees no duplicate source rows) — deduplicates `UserID` in an inner subquery *before* computing `GETDATE()` in the outer `SELECT`, since a user holding both legacy codes would otherwise produce two source rows whose independently-evaluated `GETDATE()` values could both survive a naive `SELECT DISTINCT` and then collide on the `(UserID, GroupID)` primary key. Caught during self-review, not by any test or tool — see the migration's own inline comment for the full reasoning.
- **First real exercise of the new `AnyDepartmentRequirement`/`Dept:production,sales` comma-syntax** (added to `Services/Auth/AuthorizationRequirements.cs`/`NexusPolicyProvider.cs` specifically for this tile) — `ProductionScheduleController`'s class-level gate.
- Frontend: `Pages/Sales/{Index,CustomerInstructions,ProductionSchedule,ScheduleWaterfall}.cshtml(.cs)` + `wwwroot/js/sales/{customer-instructions,schedule-waterfall}.js`. The Production Schedule tile's JS is deliberately placed at the department-neutral `wwwroot/js/production-schedule/index.js` rather than under `wwwroot/js/sales/`, since Node mounts the exact same component on both the Sales and Production pages — Phase 6 (Production) will add its own page linking this same file rather than duplicating it.
- **Two deliberate frontend simplifications over the Node original, both matching the "clean functional approximation, not pixel parity" precedent already set by Engineering/Quality**: (1) Production Schedule's per-date buckets render always-expanded (no collapsible sections); (2) the Schedule Agreement Waterfall grid drops the sticky label-column/rowspan-merged-cell visual polish (the DOM equivalent of the source Excel PivotTable's frozen panes) in favour of a plain table with label columns repeated on every row — the pivot-building logic itself (`buildWaterfallPivot`, including the cumulative-vs-raw toggle) is ported verbatim and unsimplified, since that's actual behavior, not visual polish.
- No client-side permission-based hide/show anywhere in this phase (Customer Standard Instructions' Add/Edit/Delete/Import controls, Production Schedule's row-edit controls) — same simplification Quality's Concessions page already established; the API's 403 is the real gate either way.
- `ScheduleWaterfall`'s SapServer call uses the real calling user's ID, confirmed against `salessap.js` — distinct from Quality's fixed `{userId: 0}` service identity (`QualityHelper.SapServiceUserId`). Locked in by a dedicated test.
- 15 `SalesHelperTests` + 5 `SalesControllerTests`. **No tests exist for `ProductionScheduleHelper`/`ProductionScheduleController`** — every one of its methods opens a real SQL connection unconditionally before any other logic runs (`INexusOperationsDb.CreateConnectionAsync` actually calls `SqlConnection.OpenAsync`, unlike a lazy-open pattern), so there is no validation-only code path to exercise without a live SQL Server, unlike `SalesHelper` (whose validation happens before ever calling `CreateConnectionAsync`, and whose bulk-import all-rows-invalid case never reaches a query) or `GetScheduleWaterfallAsync` (SAP-only, no SQL at all). Come back to this once a real `NexusOperations` database is reachable, or if `ProductionScheduleHelper` gets refactored to validate/branch before opening a connection.
- Real HTTP smoke test (`dotnet run`, `ValidateOnBuild`/`ValidateScopes` in Development): DI graph resolves cleanly with the new SQL-touching Helpers in the mix; `/Sales`, `/Sales/CustomerInstructions`, `/Sales/ProductionSchedule`, `/Sales/ScheduleWaterfall` → 302 to `/Login` when unauthenticated; `/api/sales/customer-instructions`, `/api/production-schedule` → 401 (not a redirect); all three new JS files serve 200 from `wwwroot`.

## Phase 5: Finance

GL Account Groups (CRUD) + Material Costing/Actual Costs/Profit Center Data (three SAP costing proxies) + Stock Adjustments (approve/reject stock counts + Gains/Losses reporting), ported from `routes/finance.js`, the `/cost-sheet`/`/costing/*` handlers in `routes/sap.js`, the Finance-facing slice of `routes/stockcount.js`/`stockcountsql.js`, and `private/js/finance.js`.

- **Scope deliberately narrower than Node's `routes/stockcount.js`/`stockcountsql.js` (~1,750 lines combined) — researched in full, but only the ~350-line slice Finance's own Stock Adjustments tile actually calls was ported.** Research (a dedicated background investigation, not a guess) confirmed those two files are not Finance-exclusive at all: the count-creation/line-entry/bin-completion/Finished-Goods-scanning/discrepancy-resolution surface (`LOG_SUPER`-gated) is genuinely Warehouse's own feature, shared only because Node mounted both departments' needs on one router. Building all of it now would have usurped Phase 7 (Warehouse)'s own vertical slice rather than deferring cleanly to it. `StockCountController`/`StockCountHelper` cover exactly: list counts (optionally by status), a material-grouped variance report, approve, reject, and the Gains/Losses history report — all `Perm:FIN_STOCK_APPROVE`-gated (an existing legacy code, already tile-scoped in Node, no split/default-group migration needed, unlike every earlier department's coarser legacy codes). Phase 7 extends this same controller/helper with the Warehouse-side actions rather than duplicating them — see `Models/Dto/StockCountModels.cs`'s header comment for the exact route inventory deferred.
- **`GetCountReportAsync` only implements Node's `groupBy=material` variant** — `groupBy=bin` throws a clear `NexusValidationException` pointing at Phase 7, rather than silently returning wrong data. `SapQty` on each report row is *derived* (`CountedQty - VarianceQty`) rather than summed directly from the raw per-line `SapQty` column — a material can span several `(StorageType, Bin)` groups, each independently "folded" by Node's group-variance logic so only one line per group ever carries a live, non-zeroed `VarianceQty`/`VarianceValue` (summing the raw per-line `SapQty` across groups would double-count); `CountedQty` and `VarianceQty` both sum correctly across groups by construction, so deriving `SapQty` from their difference is the safe, self-consistent choice — see the method's own doc comment for the full reasoning.
- **A genuinely new permission code, not a legacy-code split**: `FIN_GL_GROUPS_MANAGE`, gating GL-group create/update/delete. Research confirmed Node's GL-group writes and all three costing proxies have *no* permission check at all beyond `requireLogin` — a real gap, not a coarse-but-present code to split. `Data/Migrations/20260902064136_SeedFinancePermissions.cs` defines the code and a default (initially empty) "Finance Administrator" group — unlike every earlier Seed*Permissions migration, there's no legacy-holder INSERT, since there were no existing holders of anything to carry forward; an admin grants the group manually. Read access to every Finance tile stays `Dept:finance`-gated only (still a tightening over Node's "any logged-in user of any department").
- **The three SAP costing proxies are consolidated under `api/finance/*`, not Node's literal `api/sap/*` mount** — `routes/sap.js` is a shared, multi-department file in Node; these three handlers (`cost-sheet`, `costing/period-balance`, `costing/profit-center`) are genuinely Finance-only in practice (only `finance.js`'s frontend ever calls them), so this port groups them with the rest of `FinanceController` rather than preserving an incidental Node file-organization artifact as if it were a stable API contract. Every other endpoint in this migration keeps Node's literal URL — this is the one deliberate exception, and it's called out here so it isn't mistaken for an oversight.
- **KST_LABELS (Material Costing's cost-breakdown component mapping) and PC_SEGMENT_MAP/PC_SEGMENT_ORDER (Profit Center Data's profit-center-code → PV/PTFE/Other business-rule table) are ported verbatim from the real Node source, re-checked directly rather than trusted from a paraphrase** — an earlier research summary's component count didn't quite match Node's actual 8-field mapping, caught before writing the port by re-reading `private/js/finance.js` directly. Both are pure frontend business logic with no server-side equivalent; must never be "derived" or approximated.
- **Material Costing's per-row Quantity/Incoterms/Country form fields are dropped, not ported** — confirmed against SapServer's real `CostSheetRequest` DTO that only `Materials[]`/`Date` are ever read server-side; the Node form collected three fields it never actually used. Porting genuinely dead UI would violate this codebase's "no speculative fields" principle.
- **Actual Costs' client-side period-net/cumulative-balance recalculation is ported verbatim, not simplified** — it deliberately ignores SAP's own year-to-date `balance`/`CumBalance` fields (`PeriodBalanceRow` carries them through unused) and instead computes `period net = debit + |credit|`, `cumulative = running sum from zero across the fetched period range`, entirely client-side. This is real, load-bearing business logic, unlike the visual simplifications below.
- **Visual/structural simplifications, matching the "clean functional approximation over pixel parity" precedent every earlier department set**: GL Account Groups' floating add/edit modal → an in-page form (multi-account entry via one-per-line textarea, not dynamic add/remove input rows); Material Costing's right-click context menu → a plain per-row "Breakdown" toggle button; the Actual Costs/Profit Center Data DataTable's per-column filter inputs are dropped; Stock Adjustments has no client-side `FIN_STOCK_APPROVE`-based tile hiding (same simplification Quality's Concessions page already made — the API's 403 is the real gate either way).
- **No CSV export was ported for any tile in this phase**, despite the migration plan flagging Finance as "the first phase that's meaningfully export-heavy." Research found the *only* export mechanism anywhere in this department is a single client-side `Blob`-based CSV builder shared by Material Costing/Actual Costs/Profit Center Data, whose filename is hardcoded `material-costing-<date>.csv` regardless of which tile produced the data (a real naming bug in the Node original) — and Stock Adjustments, despite being the most "reporting-shaped" tile (multi-section Gains/Losses report), has *zero* export in Node at all. Rather than porting a bug-for-bug client-side `Blob` CSV (or half-guessing a server-side ClosedXML replacement's exact shape under real time pressure), export was deliberately left out of this phase — flagged here as real, acknowledged scope, not an oversight, and a natural candidate for a small dedicated follow-up once a server-side export convention is decided for this app generally (Finance's Stock Adjustments Gains/Losses report and Actual Costs/Profit Center Data are the best candidates to start with).
- 8 `FinanceHelperTests` + 2 `StockCountHelperTests` + 4 `FinanceControllerTests` + 2 `StockCountControllerTests` = 16 new tests. Same DB-touching-methods-are-untestable-without-a-live-SQL-Server caveat as Sales/ProductionSchedule: GL group CRUD's success paths and every `StockCountHelper` method except `GetCountReportAsync`'s groupBy-validation and `RejectAsync`'s reason-validation open a real connection unconditionally before any other logic runs. The three costing proxies (SAP-only, no SQL at all) are fully tested, including both validation-failure and success paths.
- Real HTTP smoke test (`dotnet run`, `ValidateOnBuild`/`ValidateScopes` in Development): DI graph resolves cleanly; `/Finance` and all five tile pages → 302 to `/Login` when unauthenticated; `/api/finance/gl-groups`, POST `/api/finance/cost-sheet`, `/api/stockcount/counts`, POST `/api/stockcount/counts/1/approve` → 401 (not a redirect); every new JS file serves 200.

## Phase 6: Production

**By far the largest department in the app (~14,300 lines of Node source: `routes/production.js` 398, `routes/productionnexus.js` 5,454/87 routes, `routes/labels.js` 1,424, `private/js/production-nexus.js` 6,495/24 tiles) — researched in full via a dedicated background investigation before writing any code, then deliberately split into three sub-phases** rather than attempted as one slice, matching the migration plan's own call for this ("budget this as its own multi-week effort; consider sub-splitting into two confirmable slices internally" — this port uses three). Unlike every earlier department, Production's entire SAP-facing surface already has a working, previously-proven SapServer proxy endpoint (`ProductionController.cs`/`ProductionHelpers.cs`) — no new SapServer endpoints are needed anywhere in this department.

- **Sub-phase 6a (done)**: the 7 Supervisor Reports, Batch History, and Traceability — entirely read-only (except the trace-link create), needing no `writeEvent`/`notify()` infrastructure and no SapServer calls at all. Deliberately chosen first, matching the "smallest/simplest first" precedent every earlier phase set.
- **Sub-phase 6b (done)**: the 7 Entry + 7 Data tiles (the shared metre-process entry/completion engine, Mixing including Billet Staging), Scrap approve/reject/retry, SAP Reversals, Scrap Reversal, and the entire label-printing subsystem (`routes/labels.js` — browser-preview HTML + server-side PDF via QuestPDF/ZXing.Net/raw-TCP `TcpClient` printer send). `writeEvent`/`prod.EventLog` (`ProductionEventLogHelper`) became real, load-bearing infrastructure here, per the 60+ Node call sites depending on it — every `notify()` call site stays a deferred stubbed no-op, same as Quality's Phase 3 precedent.
- **Sub-phase 6c (done)**: BOM download/persistence/validation + Traceability Concessions' *raise* action (the counterpart to Quality's already-shipped *review* actions), the CO/BR/CL/TW/EX draft→complete two-step wizard, Drumming's full entry flow (BOM hard-block, concession-goods-movement vs. combined-backflush branch, braid-consumption auto-backflush, `Z_ZPRODBATCH_MAINT`), Failed Backflush's five-way per-process retry queue, and Order Lookup/Drumming Ticket/coils/reversal-status/data. Sequenced last because it concentrated every genuinely risky/stateful piece research flagged, and because Failed Backflush's retry logic re-invokes nearly all of Drumming's own posting logic and so couldn't be built before that landed. See the five "Sub-phase 6c, Nth slice" sections below for the full detail on each piece.
- **Explicitly out of scope for all of Production's sub-phases**: the Pallet Builder label routes in `labels.js` (Warehouse/Logistics domain — printing a pallet-scan/finish label is not a Production concern despite living in the same Node file), `routes/staging.js` behind the Staging Post tile (865 lines, its own vertical slice — arguably belongs to Warehouse's Phase 7), and `routes/production.js`'s legacy `NexusArchive`-backed reads (`dbo.Batches`/`dbo.Mixing`/`dbo.Extrusion`/etc. — an older, superseded generation of the same concept `prod.*` replaced; no confirmed consumer in `production-nexus.html`'s tile grid was found, so porting it is deferred pending confirmation it's not dead). All three are real, researched, deliberately deferred — not overlooked.
- **A soft cross-department dependency to watch, not yet a blocker**: Drumming's re-drum-reversal check (`assertParentBatchesReversed`, gating on `prod.Drumming.IsReversed`) depends on a flag only ever set by Warehouse/Staging's transfer-confirmation code (`lib/redrumReversal.js`), not by anything in Production's own routes. If Sub-phase 6c ships before a minimal Warehouse transfer-confirmation slice exists, this gate will simply never trip in the C# app — a silent-pass-through risk to make a deliberate decision about (stub permissive with a comment, block with a "not yet supported" message, or sequence 6c after that Warehouse slice) rather than an accidental gap, when 6c is planned.
- **`PROD_SUPERVISOR` is confirmed the single widest-reaching legacy code in this migration** — 26 routes across ~8 tiles (all 7 reports, Open Runs, SAP Reversals, Approve Scrap, Expired Mix Batches, Failed Backflush, Scrap Reversal). Sub-phase 6a deliberately does **not** split it yet (unlike Engineering/Quality/Sales's coarse codes) — a real per-tile split (e.g. `PROD_REPORTS_VIEW`, `PROD_REVERSAL`, `PROD_SCRAP_APPROVE`, `PROD_FAILED_BACKFLUSH`) is a genuine design decision flagged for whichever sub-phase (6b or 6c) actually ports the tiles it would gate — splitting ahead of the tiles existing would mean guessing at a split that later sub-phases might not actually want. 6a's own 9 tiles reference the legacy code directly, same as how `FIN_STOCK_APPROVE` needed no split in Finance.
- **Two client-side-only permission gaps found and closed in 6a**: Node's `GET /trace/:pc/:id` and `GET /history` sit inside the Supervisor HTML section (implying `PROD_SUPERVISOR` is required) but neither route checked the permission server-side. This port adds `Perm:PROD_SUPERVISOR` to both — a tightening matching the UI's own evident intent, same "the API's 403 is the real gate either way" principle applied everywhere else. Because these two report/history/trace pages are *entirely* supervisor-only (not just specific write actions within an otherwise-open page, the shape every earlier phase's per-action `[Authorize]` handled), the C# port also gates the Razor Pages themselves with `Perm:PROD_SUPERVISOR` (stacked with `Dept:production` — multiple `[Authorize]` attributes on one class combine with AND), not just the API — avoiding a non-supervisor seeing a page that 403s immediately on every data load.
- **`Controllers/ProductionNexusController.cs` uses `api/productionnexus`**, matching Node's actual mount (`routes/productionnexus.js`) — deliberately distinct from a future `api/production` controller for `routes/production.js`'s legacy archive reads (out of scope per above), so the two are never conflated the way their Node route files' similar names could suggest.
- Report SQL (`ProductionReportsHelper`) is ported faithfully including Node's own real inconsistency in which reports apply the process-code filter and which don't (Scrap and SAP Performance never filter by process in Node, even though the query param exists; Output/Batches/Shift/Operator/Material-output all do) — preserved exactly, not "fixed," matching this codebase's established convention of documenting-not-silently-correcting an existing behavior once it's confirmed intentional-by-omission rather than an obvious bug.
- Frontend: `Pages/Production/{Index,BatchHistory,Traceability,Reports/*}.cshtml(.cs)` + `wwwroot/js/production/{reports-common,batch-history,traceability,reports/*}.js`. `reports-common.js` is a shared, non-module script (same load-order convention as `session-guard.js`) factoring out the filter bar/table-builder/CSV-export helpers the 7 report pages would otherwise duplicate — mirrors Node's own `rptFiltersHtml`/`rptTable`/`wireExport` shared-helper pattern. Charts are deliberately not ported (visual polish, same simplification precedent as every earlier department's report-adjacent tiles) — every report still shows its real data as CSV-exportable tables.
- Index page lists only 6a's 9 built tiles, not the other 15 as "Coming soon" placeholders — unlike Finance's 3 genuinely-unbuilt-in-Node placeholder tiles, everything Production defers is real and working in Node today, so labeling it "coming soon" would misrepresent Node's own current state; it's simply not listed until its sub-phase lands.
- 4 new tests (`ProductionHelperTests`, `ProductionNexusControllerTests`) — only `AddTraceLinkAsync`'s validation-failure path is testable without a live SQL Server; every report query and `GetHistoryAsync`/`GetTraceChainAsync` open a real connection unconditionally before any other logic runs, same caveat as most of Finance/Sales's SQL-touching Helpers.
- Real HTTP smoke test (`dotnet run`): DI graph resolves cleanly; all 9 new pages → 302 to `/Login` when unauthenticated; `GET /api/productionnexus/reports/output`, `GET /api/productionnexus/history`, `POST /api/productionnexus/trace` → 401 (not a redirect); every new JS file serves 200.

### Sub-phase 6b, first slice: Mixing entry

The first real piece of 6b — `POST /mixing/entry`, backend + frontend, port of `runMixingEntry`/`private/js/production-nexus.js`. Chosen first within 6b because it's the smallest, most self-contained of the entry-flow clusters (unlike the shared metre-process engine, which spans 5 process codes and a multi-phase completion wizard).

- **`ISapServerClient.GetAsync<T>` gained a new optional `body` parameter** (`GetAsync<T>(path, userId, object? body = null, bool longRunning = false, ct = default)`) — SapServer's `check-profit-centre` endpoint is declared `[HttpGet]` but still reads a `[FromBody]` request (matching Node's own `sapGet()` helper, which does `axios.request({ method: 'get', data: body })`). This is the first Production call needing it; every real call site in this codebase already used `ct: ct` as a named argument, so the insertion doesn't break any of them — only the Moq test setups in `EngineeringHelperTests`/`EngineeringControllerTests`, which used positional args, needed updating (mechanical fix, not a behavior change).
- **`writeEvent`/`prod.EventLog` is now real, shared infrastructure** (`ProductionEventLogHelper.WriteEventAsync`), not stubbed — matches the plan flagged in the Phase 6 notes above (60+ Node call sites depend on it). Deliberately takes an already-open `SqlConnection`, not `INexusOperationsDb`, since every real call site writes it as one of several statements against a connection already in use for the same request — same non-transactional shape as Node itself (a partial failure mid-sequence can leave earlier writes committed; Mixing entry's own per-tub loop is the clearest example of this being genuinely intentional, not an oversight).
- **`notify()` is NOT wired up** for the SAP-failure alert this route sends in Node — same reasoning and same precedent as Quality's Phase 3 concession review (the Notifications feature is still deferred since Phase 1).
- **New shared exception types**: `NexusConflictException` (409 — the duplicate supplier-batch/tub-number check) and `NexusBadGatewayException` (502 — SAP genuinely couldn't confirm something needed to proceed safely, e.g. a failed profit-centre check; mirrors Node's `Object.assign(new Error(...), { statusCode: 502 })` convention). Both added to `Models/NexusExceptions.cs` alongside the existing NotFound/Validation/Permission trio.
- **`ProductionSapHelpers.cs`** holds the shared SAP-response-parsing/profit-centre-validation logic every later 6b cluster will also need (`ParseSapBackflush`, `AssertProfitCentreAsync` + the `PROFIT_CENTRES` table, `LogBackflushAlertAsync`, `CurrentShiftId`) — built once here rather than duplicated per-cluster, anticipating the metre-process engine's identical needs.
- Each tub really does post an independent SAP backflush (`Zf40nRequest`/`BdcResponse` — new shared DTOs mirroring SapServer's `Models/Bapi/ProductionModels.cs` field-for-field) — a partial per-tub failure is preserved exactly as Node's real, intentional behavior (see `MixingHelper.EnterAsync`'s own doc comment), not "fixed" into an all-or-nothing transaction.
- Label printing (`labelPrint()` in Node, fired after a successful post) is deliberately not wired into the frontend yet — the whole PDF/barcode/raw-TCP-printer subsystem (`routes/labels.js`) is its own not-yet-built piece of Sub-phase 6b, tracked separately.
- 6 new Helper tests (`MixingHelperTests` — every validation-failure path, since `EnterAsync` validates before ever opening a connection) + 1 new Controller test. Real HTTP smoke test confirms the new page (302 unauthenticated) and API route (401 unauthenticated) behave correctly.

### Sub-phase 6b, second slice: Billet Staging

`GET/PATCH/POST /mixing/staging/*` + `/mixing/tubs/*` — mix-tub lifecycle (stage into Billet, scan-to-stage, return to Conditioning, search), backend + frontend, port of the corresponding section of `routes/productionnexus.js`.

- **Purely a Normanton-Nexus-only concept with no SAP counterpart** — mix materials aren't batch-managed in SAP, so staging status/Conditioning Time/Billet balance all live only in `prod.MixingTubs`/`prod.MixingTubReturns`. No SapServer calls anywhere in this cluster.
- **Reached in Node via the Extrusion entry wizard's parent-picker "chooser," not a top-level HTML tile of its own** — built here as a real standalone page anyway (Index → Billet Staging), since it's genuinely useful on its own and the Extrusion wizard that would otherwise surface it doesn't exist yet (that's a later slice of 6b). The wizard can link into this same page/JS once it lands, rather than duplicating the staging logic.
- **`BilletStagingHelper.StageTubAsync` (private) is the single shared guard path for both the manual stage button and the scan-to-stage ticket parser** — mirrors Node's own single `stageTub()` helper exactly (24h-minimum/96h-maximum conditioning window, scrapped/reversed/already-staged checks, all in one place) — the two public entry points (`StageAsync`/`StageByRefAsync`) both delegate to it rather than re-implementing the guards.
- The scan-to-stage ticket format (`MX-{8-digit MixingID}-T{TubSeq}`) is parsed with the same regex Node uses, via a `[GeneratedRegex]`-sourced partial method (compile-time-generated, not `Regex.Match` with a runtime-compiled pattern).
- 4 new Helper tests (the two validation-before-DB paths: `StageByRefAsync`'s ticket-format rejection, `ReturnToConditioningAsync`'s non-positive-quantity rejection). Real HTTP smoke test confirms the new page and all 5 new API routes behave correctly.

### Sub-phase 6b, third slice: the shared metre-process (EX/CO/BR/CL/TW) entry engine

`POST /process/:pc/entry` + `GET /process/:pc/open-entries` + `GET /process/:pc/data` + `GET /open-runs` (+cancel), backend + frontend, port of the "Generic entry"/"Open runs" sections of `routes/productionnexus.js`.

- **SCOPE NOTE, deliberate**: this covers only the direct one-step entry (immediate backflush, no BOM download/validation) — Node's `entry` route itself does no BOM work at all, unlike `draft`/`complete`. The two-step draft→complete workflow (`POST /process/:pc/draft`, `POST /process/:pc/complete/:recordID`) needs BOM download/validation and the hard-block-vs-approved-concession branch, and is deferred to Sub-phase 6c alongside Drumming and Traceability Concessions, which share the same BOM helper block (`fetchBom`/`persistBomSnapshot`/`validateTraceabilityAgainstBom`/`validateRawMaterialBatches` in Node) — splitting entry from draft/complete here is not a simplification of Node's own logic, it's following a real seam already present in Node's own code (the `entry` route is genuinely simpler and self-contained).
- **Process-code metadata (`ProductionSapHelpers.Process`, the table/PK/ref/UOM/qty-column map) is now shared infrastructure** — moved out of `ProductionHelper.cs` (which used to hold a private copy for Batch History/Traceability only) since `MetreProcessHelper` needed the identical map for Open Runs (spans all 9 non-Firewall process codes, not just the 5 metre ones). One source of truth now, not two copies that could drift.
- **EX's scrap-entry math differs from every other metre process, ported as a real two-branch difference, not unified**: EX enters scrap per-reason in real KG; CO/BR/CL/TW only record an overall total plus reason *occurrence counts*, so each reason's KG share is derived proportionally (`occurrences / totalOccurrences`). `RecordEntryScrapAsync` keeps Node's own `code === 'EX'` branch structure rather than inventing one shared formula.
- **A failed SAP backflush still returns 201, not an error status** — same "the record was saved, only the SAP posting failed" convention Mixing entry already established; `MetreProcessEntryResult.Status` is `"SAP_FAILED"` with the error in `.Error`, not an HTTP exception. `notify()` is still not wired up, same deferred-Notifications reasoning as every earlier Production write action.
- **Frontend deliberately narrower than the backend it calls**: `Pages/Production/MetreEntry.cshtml` is one Razor Page parameterized by process code (`@page "{code}"`, `asp-route-code="EX"` etc. from 5 Index tiles) rather than 5 near-duplicate page files — mirrors Node's own single `runMeterProcessEntry()` engine backing all 5 tiles. Only the direct-entry form is built (material/length/notes); additional-operators/parent-batch-linking/scrap-entry UI is real, unbuilt scope — the backend (`MetreProcessHelper.EnterAsync`) already accepts all of it, so the form can grow into it later without a backend change. Open Entries/Data listing pages for these 5 processes are likewise not built yet (backend routes exist and are tested) — deferred for time within this budget-constrained slice, flagged here rather than silently skipped.
- 8 new Helper tests (every validation-before-DB path across `EnterAsync`/`GetOpenEntriesAsync`/`GetDataAsync`/`CancelOpenRunAsync`). Real HTTP smoke test confirms all 6 new pages (302 unauthenticated, including the invalid-process-code page which auth intercepts before its own `NotFound()` check ever runs) and all 4 new/extended API route groups (401 unauthenticated) behave correctly.

### Sub-phase 6b, fourth slice: Scrap approve/reject/retry/queue

`GET /scrap/{summary,failed,entries,pending}` + `PATCH /scrap/:id/retry` + `POST /scrap/{approve,reject}` + `GET /scrap/:id/documents` + `GET /scrap-reasons`, backend + frontend, port of the scrap-management section of `routes/productionnexus.js`. Distinct from Scrap Reversal (a later 6b slice, built on `prod.ScrapMaterialDocuments.IsReversed` — `scrapDocSql()` in Node was read for context but is that slice's concern, not this one's) and from per-process entry-time scrap *recording* (`MetreProcessHelper.RecordEntryScrapAsync`, already shipped in the third slice) — this cluster is purely supervisor review/posting of already-recorded `prod.ScrapEntries` rows.

- **`POST /api/production/scrap/post` on SapServer takes the *finished-good* material/quantity/header, not a BOM component** — confirmed by reading `SapServer/Controllers/ProductionController.cs` directly (`PostScrap`, `[Route("scrap/post")]`): SapServer itself explodes the material's BOM server-side, posts one MB11/BDC per component (and an LT01 transfer order for components in storage locations 1710/1711), and returns a `BdcWrapper` (`{ Responses: BdcResponse[] }`) with one entry per component — exactly matching Node's `parseBomScrapResponse`'s `sapRaw.data.responses` unwrap. New DTOs `ScrapPostRequest`/`BdcWrapper` added to `ProductionSapModels.cs` mirroring SapServer's `BomScrapRequest`/`BdcWrapper` (`Models/Bapi/ProductionModels.cs`) field-for-field.
- **Every SAP posting response is validated the same way** (`ParseBomScrapResponse`, private to `ScrapHelper`): every entry in the `BdcWrapper` must be `Type=="S" && MessageClass=="M7" && MessageNumber=="060"`, or the whole call is treated as failed — mirrors Node's `parseBomScrapResponse` exactly, including validating *every* response in the array, not just the first.
- **Node's real `retry` route has a latent ReferenceError bug that this port deliberately does not reproduce**: `batchRef` is declared inside Node's `try` block but referenced from its `catch` block's audit call — if the process/material lookup itself throws before `batchRef` is assigned, Node would throw a *second*, unrelated error from inside the error handler. `ScrapHelper.RetryAsync` avoids this by structuring the method in two phases instead: a validation phase (not-found/unknown-process/process-record-missing — throws `NexusNotFoundException`/`NexusValidationException` directly, propagating straight out, matching Node's early `return` before ever reaching its catch) that fully resolves `material`/`batchRef` first, followed by a SAP-call-and-persist phase wrapped in its own try/catch that can safely reference `batchRef` because it's now guaranteed assigned. Same non-reproduction precedent as `SapPad`'s and `RfcRowHelpers.GetDecimal`'s bugs in SapServer's own CLAUDE.md — a real bug gets fixed by construction, not carried forward, when it's cheap to avoid and doesn't change any *intentional* behavior.
- **`ScrapHelper.ApproveAsync`/`RejectAsync` are real bulk operations, not a loop that aborts on the first failure**: each `scrapId` gets its own connection and its own try/catch (`ApproveOneAsync`/`RejectOneAsync`), run concurrently via `Task.WhenAll` — direct port of Node's `Promise.all` over the `scrapIDs` array, same "partial success is real" precedent as Mixing entry's per-tub loop.
- **The 404-vs-other-failure distinction in Approve is preserved exactly**: a `SapProxyException` with `StatusCode == 404` (SapServer genuinely has no `/scrap/post` route deployed yet) leaves the entry pending — no `IsApproved`/`SAPPosted` update at all, so it can be retried once SapServer catches up — while every other failure marks the entry `IsApproved=1, SAPPosted=0, SAPErrorMessage=...` so it shows up in the Failed queue. Getting this backwards would either lose entries into a permanently-approved-but-never-postable state (on a routine 404 during a rolling deploy) or leave genuinely-failed entries silently stuck as pending forever.
- **`GetPendingAsync` ports Node's `sys.columns` existence guard for `IsApproved`/`IsVoided` verbatim**, not simplified away — both columns were added to `prod.ScrapEntries` via an out-of-band migration this app doesn't manage (see the root CLAUDE.md-equivalent "Schema stays as-is" principle), so an environment where either hasn't been applied yet would otherwise 500 instead of just rendering an unfiltered queue.
- **`GET /scrap-reasons` (used by the retry-queue's reason dropdown) is ported here too**, even though it isn't strictly part of this cluster's own Node route block — it's small, and both this cluster's Posted Scrap retry UI and the future Drumming/metre-wizard scrap-entry UI need it; no reason to defer a 15-line read-only helper to whichever slice happens to need it first.
- **Frontend**: `Pages/Production/{ApproveScrap,PostedScrap}.cshtml(.cs)` + `wwwroot/js/production/{approve-scrap,posted-scrap}.js`, both `Dept:production` + `Perm:PROD_SUPERVISOR` gated (stacked, same convention as Open Runs/the 7 reports). Node's drill-down (`openScrapDrilldown`, a modal showing per-material scrap-entry detail for a clicked summary row) is ported as an inline expand/collapse under the clicked row instead of a real modal — no shared modal component exists yet anywhere in this port, and building one just for this one drill-down would be scope creep; same "visual-polish simplification" precedent as `reports-common.js` skipping charts.
- 1 new Helper test (`GetDocumentsAsync`'s non-positive-scrapId guard — the only path in this cluster that validates before opening a connection) + 3 new Controller tests (`ScrapApprove`/`ScrapReject`'s empty-`scrapIds`-array guard, `ScrapDocuments`'s non-positive-id guard — all Controller-level validation, same precedent as `QualityController`'s "No rows provided."/`EngineeringController`'s "material is required." checks). Real HTTP smoke test confirms both new pages (302 unauthenticated) and all 8 new/extended API routes (401 unauthenticated, including the bulk POST endpoints) behave correctly.

### Sub-phase 6b, fifth slice: SAP Reversals

`GET /reversal/{search,by-batch/:pc/:id,find}` + `PATCH /reversal/:id` + `POST /reversal/{execute,bulk}`, backend + frontend, port of the Reversal section of `routes/productionnexus.js`. Reverses `prod.SAPPostings`/backflush documents themselves (via SAP's MF41) — distinct from Scrap Reversal (a separate later 6b slice reversing `prod.ScrapMaterialDocuments`/MBST documents instead, not yet built).

- **`POST /api/production/reverse-backflush` (MF41) and `POST /api/production/scrap/reverse` (MBST) share one request DTO on SapServer** — confirmed by reading `SapServer/Controllers/ProductionController.cs` directly: both take the same `Mf41Request { MaterialDocument }` and return a `BdcResponse`. One shared `Mf41Request` record added to `ProductionSapModels.cs` rather than two near-identical DTOs.
- **`POST /reversal/execute` and `PATCH /reversal/:sapPostingId` are ported backend-only, with no frontend page calling them** — confirmed by grepping every frontend file that calls `api/productionnexus` (`production-nexus.js`, `quality.js`, `landing.js`): neither route is referenced anywhere. Node's own UI only ever drives the bulk path (`/reversal/bulk`) for the actual reversal action; `execute`/the manual `PATCH` mark-as-reversed exist as real, working API surface with no in-app consumer today. Ported for API parity (a genuinely real, working Node route, not overlooked scope) since porting them is cheap, but no Razor Page was built to drive them specifically — same reasoning as the generic `RfcController`/`FunctionController` endpoints having no dedicated UI either.
- **A new exception type, `NexusUnprocessableEntityException` (422)**, was needed for `execute`'s "this document needs to be reversed using MBST" case (SAP error `M7`/`066`) — Node's `res.status(422)` for this exact case had no existing equivalent in `NexusExceptions.cs` (which previously topped out at 409/502). Added alongside the existing NotFound/Validation/Permission/Conflict/BadGateway set.
- **`ScrapHelper.ReverseJobScrapAsync` is new shared infrastructure, called from this cluster, not from anywhere within `ScrapHelper` itself** — mirrors Node's module-level `reverseJobScrap(pool, processCode, recordID, uid)` exactly: voids any not-yet-SAP-posted scrap for the job being reversed (so it can never be approved later) and reverses any already-posted scrap documents via SapServer's MBST endpoint, logging (not throwing) if any individual document's SAP reversal fails — the parent backflush reversal that triggered this cleanup must still succeed regardless of whether every scrap document could be cleanly reversed too. Both `MarkReversedAsync` (the single PATCH path) and `BulkReverseAsync`'s per-document `MarkReversedByDocumentAsync` call into it.
- **DEVIATION, deliberate — `BulkReverseAsync` does not stream progress over Server-Sent Events the way Node's `/reversal/bulk` does.** Node opens one SSE connection per bulk request (with a 20s heartbeat to survive proxy idle timeouts) so the UI can show live per-document progress while SapServer processes potentially dozens of ~15s reversals in parallel. This port runs the identical concurrent per-document SAP calls (`Task.WhenAll`, same "partial success is real" precedent as Scrap approve/reject's `ApproveOneAsync`/`RejectOneAsync`) but returns one plain JSON response instead — no SSE infrastructure exists anywhere else in this port, and building it for this one endpoint would be new infrastructure, not a port. The functional outcome (which documents reversed, which failed, why, whether a scrap-cleanup cascade ran) is byte-for-byte identical to Node's; only the live-progress UX during a large batch is not reproduced — `sap-reversals.js`'s bulk button shows a static "Reversing N documents…" state instead of a live counter/progress bar.
- **`Type=="E"`/`MessageClass`/`MessageNumber` response branching is ported exactly, including the two named special cases**: `RM`/`210` ("already reversed" — 409 on the single `execute` path, a `synced: true` non-error result on bulk, since bulk resyncs the DB from SAP's own already-reversed state rather than failing the whole batch over it) and `M7`/`066` ("must use MBST" — 422 on `execute`, a plain per-item failure message on bulk, since MBST-required documents aren't something bulk reversal can resolve on its own). Every other `E` response falls through to a generic SAP-error message, same as Node.
- **Frontend**: `Pages/Production/SapReversals.cshtml(.cs)` + `wwwroot/js/production/sap-reversals.js`, `Dept:production` + `Perm:PROD_SUPERVISOR` gated (this whole tile is supervisor-only in Node too — reversal is not an ordinary-operator action). Five search modes (material document / batch reference / material / date range / operator) dispatch to the three different GET endpoints exactly as Node's `doSearch()` does; results render as a checkbox-selectable table feeding the single bulk-reverse action.
- 6 new Helper tests (`ReversalHelperTests` — every validation-before-DB/before-SapServer-call path: `SearchAsync`'s missing-materialDocument, `FindAsync`'s all-empty-query, `MarkReversedAsync`'s missing-reversalDocumentSAP, `ExecuteAsync`'s missing-materialDocument) + 1 new Controller test (`ReversalBulk`'s empty-array guard). Real HTTP smoke test confirms the new page (302 unauthenticated) and all 6 new API routes (401 unauthenticated) behave correctly.

### Sub-phase 6b, sixth slice: metre-process Data tiles (EX/CO/BR/CL/TW)

`Pages/Production/MetreProcessData.cshtml(.cs)` + `wwwroot/js/production/metre-data.js` — the Data half of the "7 Entry + 7 Data tiles" the Phase 6 plan called for; the backend (`MetreProcessHelper.GetDataAsync`) already existed from the third slice, only the frontend was missing.

- **Open Entries deliberately still has no standalone frontend page, and now never will as a page of its own** — confirmed by reading Node's `runCompleteRun` (the only caller of `GET /process/:pc/open-entries`): it exists purely as the picker step of the draft→complete two-step wizard ("Complete Run"), itself out of scope until Sub-phase 6c. A page just listing open entries with nothing to *do* with one (no Complete action to route into) would be a dead end, not a useful interim page — so this is a scope clarification, not a further deferral: Open Entries gets a page when Complete Run does, as one flow, not before.
- **Data, by contrast, is genuinely self-contained in Node too** (`runMeterProcessData`) — a filterable historical-record report independent of the completion workflow, real buildable scope now. One Razor Page parameterized by process code (`@page "{code}"`, `asp-route-code="EX"` etc.), same pattern `MetreEntry.cshtml` already established, backing all 5 Data tiles from Index rather than 5 near-duplicate files.
- **Reuses `reports-common.js`'s `api`/`downloadCsv`** (loaded as a second classic script before `metre-data.js`) rather than duplicating the fetch-wrapper/CSV-export logic a third time in this department — the same shared, non-module script the 7 Supervisor Reports already load.
- **Node's row-click detail modal (SAP postings + scrap entries + a reprint-label button) is ported as an inline expand/collapse row**, not a modal — no shared modal component exists anywhere in this port (same precedent as Posted Scrap's drill-down) — and the reprint-label button is dropped entirely rather than stubbed, since label printing (`routes/labels.js`) isn't built in this port yet; re-add it when that subsystem lands.
- No new Helper tests (`GetDataAsync` was already tested in the third slice; this is a pure frontend addition). Real HTTP smoke test confirms all 5 new pages (302 unauthenticated, including an invalid process code — auth intercepts before the page's own `NotFound()` check runs, same as `MetreEntry`) and the new static JS file behave correctly.

### Sub-phase 6b, seventh slice: Scrap Reversal

`GET /scrap-reversal/{missed,search}` + `POST /scrap-reversal/reverse{,/bulk}`, backend + frontend, port of the Scrap Reversal section of `routes/productionnexus.js`. Reverses `prod.ScrapMaterialDocuments` via SapServer's MBST endpoint — distinct from SAP Reversals (the fifth slice, reversing `prod.SAPPostings`/backflush documents via MF41), though both share the `Mf41Request` DTO and the same SSE-not-reproduced deviation.

- **CONTRACT NOTE, confirmed by reading `SapServer/Controllers/ProductionController.cs`'s `ReverseScrap` action directly**: `POST /api/production/scrap/reverse` always returns HTTP 200 with a single `BdcResponse` — never an array/wrapper, and never a non-2xx failure for a genuine ABAP-level rejection (only for a real transport/permission failure) — the same "always 200, caller reads Type" convention documented elsewhere in SapServer's own CLAUDE.md. Node's `reverseScrapDocumentItem`/`isAlreadyReversed` hedges defensively against several possible response shapes (an array under `data.responses`, the M7/067 "already reversed" code arriving via either a thrown HTTP error or a 200 body) because its author evidently didn't have this confirmed when it was written. `ScrapReversalHelper`'s private `ReverseScrapDocumentItemAsync` only needs the single-`BdcResponse`-in-a-200 path plus one generic catch for real connectivity/permission failures — simpler than Node's own code, and documented as *why* rather than silently dropping logic Node has.
- **Node does not validate `Type=="S"` on the normal-success path here, unlike `scrap/post`'s `parseBomScrapResponse`** — only the M7/067 "already reversed" case is special-cased; any other response (including, in principle, a genuine ABAP rejection) is treated as a success and the document is marked reversed with whatever `DocumentNumber` came back. This port preserves that real, unvalidated behavior exactly rather than tightening it — flagged in `ReverseScrapDocumentItemAsync`'s own comment so it isn't mistaken for an oversight on a future pass.
- **The shared `scrapDocSql(where)` SQL-fragment builder is ported as a private method** (`ScrapReversalHelper.ScrapDocSql`) taking a trusted, server-built `WHERE` fragment — never user input directly, same convention Node's own comment calls out. `GetMissedAsync` and `SearchAsync` both build on it, exactly mirroring `/scrap-reversal/missed` and `/scrap-reversal/search`'s own Node implementations.
- **`ReverseScrapDocumentItemAsync` never throws — same "never-throws contract" Node's own doc comment calls out** (bulk runs many of these concurrently via `Task.WhenAll`/`Promise.all` and one item's failure must not cancel the others) — it returns a private `ScrapReversalItemOutcome` record instead; the single `POST /scrap-reversal/reverse` action then maps a failed outcome's `Status` (400/404/409/502) onto the matching `NexusApiException`, while the bulk action passes every outcome straight through as a `ScrapReversalBulkItemResult` regardless of success.
- **DEVIATION, deliberate, same as SAP Reversals' bulk path**: `BulkReverseAsync` runs the identical concurrent per-document SAP calls Node's `/scrap-reversal/reverse/bulk` does but returns one plain JSON response instead of Node's SSE progress stream (which reuses the same event shape as `/reversal/bulk`, per Node's own comment) — no SSE infrastructure exists anywhere in this port; see `ReversalHelper.cs`'s doc comment for the full rationale, not repeated here.
- **Frontend simplification, deliberate**: Node's search UI switches between 5 single-field "modes" (by material document / batch ref / material / date range / operator) the same way SAP Reversals' does; since `/scrap-reversal/search` actually combines every provided field with AND server-side regardless of "mode," `scrap-reversal.js` shows all 7 filter fields (material document, batch ref, material, process, date from/to, operator) at once instead of mode-switching tabs — every filter Node supports is still reachable, just not gated behind a mode selector. `Pages/Production/ScrapReversal.cshtml(.cs)`, `Dept:production` + `Perm:PROD_SUPERVISOR` gated.
- 1 new Helper test (`SearchAsync`'s all-empty-query guard — the only validation-before-DB path in this cluster; `ReverseScrapDocumentItemAsync`'s own `scrapDocumentId`/`materialDocument` guard runs after opening a connection, mirroring Node's own placement of that same check inside `reverseScrapDocumentItem` after acquiring the pool, so it isn't unit-testable without a real SQL Server) + 1 new Controller test (`ScrapReversalReverseBulk`'s empty-`items`-array guard). Real HTTP smoke test confirms the new page (302 unauthenticated) and all 4 new API routes (401 unauthenticated) behave correctly.

### Sub-phase 6b, eighth slice: label browser preview (labels.js, HTML half only)

`GET api/labels/process/{processCode}/{recordId}` — port of the browser-preview half of `routes/labels.js`. This is a genuinely different kind of slice from everything else in 6b: `labels.js` needs new dependencies (a barcode library, a PDF library, a raw-TCP client) nothing else in this migration needs, so it was deliberately split rather than ported as one piece — see the scope note below for exactly where the line was drawn and why.

- **SCOPE SPLIT, deliberate**: `labels.js`'s two routes that matter to Production (`GET /process/:pc/:id` — browser preview, rendered HTML the browser prints via `window.print()`) and (`POST /process/:pc/:id/print` — server-side PDF generation sent directly to a network printer over raw TCP) turned out to need almost entirely disjoint tooling once actually read closely: the preview path needs a barcode image (embedded as a base64 PNG `data:` URI) but nothing else; only the print path needs PDF layout (`buildPDF`/`drawLabelPage`, ~220 lines of QuestPDF-shaped drawing code once ported) and a raw socket client (`tcpPrint`). Building only the preview path first delivers real, immediately usable functionality (view/reprint a label from the browser, which is most of what `labelPrint()`'s actual floor usage is) without pulling in QuestPDF or a `TcpClient` port for a return that wasn't there yet. `GET /printers` and `PATCH /printers/default` are deferred alongside the print path specifically because they only exist to serve it (picking a target network printer) — nothing in this port would call them yet.
- **`GET /pallet/scan/:id`, `GET /pallet/finish/:id` and their `/print` siblings were confirmed out of scope, not overlooked** — same conclusion the Phase 6 top-level scope note already reached before any of `labels.js` was read line-by-line: these are Warehouse/Logistics pallet-builder concerns living in this file only because Node organizes by "prints a physical label," not by department.
- **Barcode generation is genuinely new dependency surface for this migration, and — unlike almost everything SAP/SQL-facing in this migration — is fully testable for real in this sandbox**: `LabelBarcodeHelper` uses ZXing.Net's `BarcodeWriterPixelData` (pure managed, raw BGRA32 pixel bytes, no OS-level graphics dependency) plus SixLabors.ImageSharp (also pure managed) to encode a real PNG — deliberately not `System.Drawing.Common`, which needs `libgdiplus` on Linux and is discouraged there since .NET 6. `LabelBarcodeHelperTests` actually decodes the produced PNG (`Image.Load` + checks real width/height), not just "the call didn't throw" — a rare case in this migration where a new piece of infrastructure could be verified end-to-end without a live SQL Server or SapServer. One of those tests (an all-whitespace input) initially asserted the wrong expectation — space is itself a valid Code 39 character in both Node's regex and this port's, so `"   "` legitimately encodes rather than returning null — caught by the test itself, not silently "fixed" by weakening the assertion; the test was corrected to match the real, correct behavior once traced through.
- **`LabelDataHelper.FetchLabelDataAsync`'s raw SQL row is mapped through a private `LabelRawRow` record, not a positional `ValueTuple`** — the row is 12 columns wide, past the point where a flat C# `ValueTuple` auto-nests into `TRest` and Dapper's positional tuple-mapping support becomes unreliable; every other tuple-mapped query in this migration stays at ≤7 columns for exactly this reason, so this one uses a named-property record instead rather than risking silent column misalignment.
- **`fetchMixingTicketsData`'s real reason for existing is preserved exactly**: a Mixing entry backflushes each tub to SAP independently, but the generic single-record fetch path would show one ticket for the whole batch's combined weight and only the first tub's SAP document — silently dropping the rest. `FetchMixingTicketsDataAsync` builds one `LabelData` per tub instead (each with its own weight/SAP document/supplier tub number), including the `?tub=N` single-tub reprint filter and the "no tub rows yet → one whole-batch ticket so printing never silently produces nothing" fallback, both ported verbatim from Node's own comments.
- **`LabelHtmlHelper.RenderPage` is pure string-building (no I/O) and is fully unit-tested for real** — `LabelHtmlHelperTests` asserts on actual rendered HTML content (status badge colour, presence/absence of the SAP-document section on an incomplete record, one `.label` div per Mixing tub, the combined multi-ticket page title), not just "didn't throw" — matching the same "genuinely testable, so genuinely tested" opportunity `LabelBarcodeHelperTests` found.
- **`LabelsController` is a new, separate controller mounted at `api/labels`** (not `api/productionnexus`), matching Node's own separate router mount (`app.use('/api/labels', requireLogin, labelsRoutes)`) — and deliberately carries no `Dept:`/`Perm:` policy beyond `NexusControllerBase`'s own `[Authorize]`, since no route in `labels.js` checks department or a permission code either, only that the caller is logged in at all. Confirmed by reading every route in the file, not assumed.
- **Frontend**: no new page — the browser-preview route is linked directly from where Node's own reprint button lived: a "🖨 Print Label" link appended to `metre-entry.js`'s/`mixing-entry.js`'s post-submit result (opens the new record's label in a new tab) and to `metre-data.js`'s row-detail expand (added now that the route it was deferred pending actually exists — see the seventh slice's note about dropping that button entirely; it's real now, not stubbed).
- 5 new `LabelBarcodeHelperTests` + 4 new `LabelHtmlHelperTests` (both fully real, see above) + 2 new `LabelsControllerTests` (unsupported-process-code and non-positive-record-id guards — the only validation-before-DB paths in `LabelsController`). Real HTTP smoke test confirms the new route requires authentication for both a valid and an invalid process code, and every touched JS file still serves 200.
- **Still deferred, tracked as its own remaining piece of Sub-phase 6b**: server-side PDF generation (`buildPDF`/`drawLabelPage`/`buildLabelsPDF`) via QuestPDF, embedding a barcode image inside a PDF (same `LabelBarcodeHelper` PNG bytes, different embed API), a raw-TCP `TcpClient` port of `tcpPrint`, and `GET /printers`/`PATCH /printers/default`/`POST /process/:pc/:id/print` themselves.

### Sub-phase 6b, ninth slice: server-side label printing (labels.js PDF/raw-TCP half)

`POST api/labels/process/{pc}/{id}/print` + `GET api/labels/printers` + `PATCH api/labels/printers/default` — the half of `labels.js` deliberately deferred by the eighth slice. Completes label printing end-to-end: browser preview (already shipped) plus now a real "send this label straight to a network printer" action.

- **`LabelPdfHelper` is NOT a pixel-for-pixel port of Node's PDFKit drawing, and says so in its own doc comment** — PDFKit draws with absolute x/y cursors and hand-measured font sizes per field (checked once, by the original author, against real values); QuestPDF's layout model is declarative (Row/Column/constraint-based) and doesn't map cleanly onto that coordinate math. Rather than fight QuestPDF's engine to replicate PDFKit's exact numbers — a project impossible to visually verify in this sandbox anyway (no PDF viewer, no real printer) — the same content and grouping (header bar, two-column Batch Reference+barcode/Material+barcode left, SAP Material Document+barcode/Operators/Machine/Completed right, footer) is reproduced with QuestPDF's own idiomatic elements. `LabelPdfHelperTests` verifies what's actually checkable instead: valid `%PDF`-prefixed bytes, correct page count (one per label — one per Mixing tub, matching Node's one-job-per-batch `tcpPrint` design), and no exception across every real data shape (open/complete/no-machine/no-SAP-document/A4/A5).
- **QuestPDF's strict layout validation caught a real bug this port would otherwise have shipped**: the first version of the barcode-embedding helper fixed a target *height* and let width float (mirroring the shape, not the substance, of Node's own `bcImg`/`renderedH`, which actually fixes *width* and lets height follow the barcode's aspect ratio). A long batch reference encodes to a short, wide barcode image; forcing that to a tall fixed height blew the resulting width past the two-column layout's available space. PDFKit would have silently drawn the oversized image past its intended bounds — QuestPDF instead throws `DocumentLayoutException` before ever producing a PDF. Fixed by constraining on width (`MaxWidth` + `.FitWidth()`), which is what Node's own code actually does — this was a genuine port-fidelity bug (a superficial rather than substantive read of `bcImg`), caught only because QuestPDF's stricter validation surfaced it immediately in a real, ordinary test run rather than as a silent visual defect nobody would notice until a real print job came out wrong.
- **FURTHER SIMPLIFICATION, deliberate, documented in `LabelPdfHelper.BuildLabelsPdf`'s own doc comment**: content auto-sizes to whatever height it actually needs rather than being pinned to Node's fixed 420pt/148mm content box (which existed so an A4 label sits in the top half of the sheet, not stretched full-page). QuestPDF enforces strict content-must-fit constraints — a hand-tuned fixed height here would have reintroduced exactly the same class of `DocumentLayoutException` risk the barcode fix above just caught, the moment any real field's content ran longer than what was manually checked, with no PDF viewer in this sandbox to catch it before a real deploy. The trade-off is real (an A4 label now fills more of the page than Node's did) but was made deliberately, not discovered by accident.
- **`GET /printers`/`PATCH /printers/default` read/write `dbo.PortalUsers.DefaultPrinterID` via `INexusDb`, not `INexusOperationsDb`** — matching Node's own `getNexusPool()` call for these two routes specifically (every other `labels.js` route, including the print action itself, reads production data from NexusOperations) — confirmed by reading Node's implementation, not assumed from the rest of the file's pattern.
- **`LabelPrinterOptions`/`LabelPrinterConfig` (new `Services/LabelPrinterOptions.cs`) replace `config.json`'s top-level `printers` array** — bound from a new `LabelPrinters` section in `appsettings.json` (a wrapping object with a `Printers` list, matching this app's existing Options-binding convention, not a bare root-level array) rather than the bare array Node's `config.js` exposes as `printersConfig`. Same `Id`/`Name`/`Host`/`Port`/`PaperSize` fields as Node's config shape.
- **QuestPDF requires `QuestPDF.Settings.License = LicenseType.Community` before any `GeneratePdf()` call, or it throws** (a 2023 licensing change) — set both in `Program.cs` (documents the requirement for a reader scanning startup) and, belt-and-braces, in `LabelPdfHelper`'s own explicit static constructor, since `NormantonNexus.Tests` never runs `Program.cs` and would otherwise hit the same exception on the very first test. An explicit static constructor was needed rather than a field initializer — the "beforefieldinit" JIT optimization can defer a bare field initializer's execution past the point a static method is actually called, which is exactly what happened here on the first attempt.
- **`LabelPrintHelper.SendToPrinterAsync` (the `tcpPrint` port) waits for the printer to close its end of the connection before returning**, not just for the local write to complete — mirrors Node's `sock.on('close', () => resolve())` exactly (as opposed to resolving as soon as `sock.write()`'s callback fires), via a half-close (`Shutdown(SocketShutdown.Send)`) followed by draining reads until EOF, with the same 15s timeout Node's `sock.setTimeout(15000)` uses.
- **Frontend**: no new page — `metre-data.js`'s row-detail expand (already carrying the "🖨 Print Label" preview link from the eighth slice) gains a "🖨 Send to Printer…" control that lazily fetches the printer list on first click, lets the user pick one (pre-selecting their saved default), and POSTs the print request — completing both halves of Node's original single reprint button.
- 4 new `LabelPdfHelperTests` (real PDF generation + page-count verification, see above) + 4 new `LabelsControllerTests` (unsupported process code, no printers configured, unknown `printerId` — all validation-before-DB paths in `PrintProcess`). Real HTTP smoke test confirms all 3 new API routes require authentication and the app still starts cleanly with the new `IOptions<LabelPrinterOptions>`/QuestPDF wiring in place.
- **Label printing (`routes/labels.js`) is now fully covered for Production's purposes** — every route Node has for the 7 supported process codes is ported; only the explicitly-out-of-scope pallet-builder routes (Warehouse/Logistics concern) remain unported, by design.

### Sub-phase 6c, first slice: BOM + Traceability Concession infrastructure

`BomHelper.cs` — the shared BOM download/persistence/validation and Traceability Concession-raising infrastructure the not-yet-built CO/BR/CL/TW draft→complete two-step wizard and Drumming's entry flow both need. This slice ships the process-generic pieces (`GET process/:pc/bom-preview`, `GET`/`POST .../bom` + `/bom/refresh`, `GET .../trace` (flat parent links), raw-material-batch CRUD, `POST .../concession`) — not either wizard itself, which stays real, tracked, unbuilt scope (see below).

- **This is genuinely the highest-risk remaining piece of Production, by the Phase 6 top-level scope note's own earlier assessment** — stateful, multi-step, SAP-goods-movement-posting logic with real financial/inventory consequences if a mismatch slips through. Built carefully, in a deliberately narrow first slice (infrastructure + the one fully self-contained action, concession-raising) rather than attempting the full wizard in one pass.
- **`GET /api/production/bom` and `GET /api/production/check-profit-centres` are both confirmed GET-with-body SapServer endpoints** (read directly from `SapServer/Controllers/ProductionController.cs`, same "declared `[HttpGet]`, reads `[FromBody]`" contract already established for `check-profit-centre` in Mixing entry) — `SapBomQuery`/`SapProfitCentresRequest` mirror SapServer's `BomQuery`/`ProfitCentresRequest` field-for-field; `SapProfitCentreRow` reuses the same `Material`/`ProfitCentre` shape SapServer's own `PerformanceModels.cs` defines (SapServer reuses one `ProfitCentreRow` type across its Performance and Production controllers — this port mirrors that reuse with its own `SapProfitCentreRow`, not a Performance-specific name).
- **`FetchProfitCentresAsync` never throws — matches Node's `fetchProfitCentres(...).catch(() => new Map())` exactly** — a failed bulk profit-centre lookup means every BOM component classifies as non-raw-material rather than failing the whole BOM fetch. `FetchBomAsync` itself is NOT wrapped this way (a failed live BOM lookup is a real failure the caller must see), matching Node's asymmetric error handling exactly rather than "fixing" the inconsistency.
- **`BuildActualComponentList`'s quantity precedence is preserved exactly**: an approved concession's own `Quantity` (when the reviewer set one) wins outright over the computed BOM-ratio quantity (`componentQty * totalQty`, rounded to 3dp) — verified directly in `BomHelperTests` since this is pure, no-I/O logic (a rare case in this cluster that's actually unit-testable without a live SQL Server).
- **`QualityHelper.ReviewConcessionAsync` (Quality's Phase 3) now writes the production-batch event-log entry it originally deferred** — its own doc comment explicitly flagged this as "come back and wire in once [Production's event log] exists," and `ProductionEventLogHelper` now does (built in Sub-phase 6b) — closing a real, previously-documented gap rather than leaving it to rot. The in-app notification (`notify()`) stays deferred; only the Notifications feature itself is still missing.
- **`GET trace/:pc/:id` (the recursive ancestor-chain endpoint, Sub-phase 6a) and the new `GET process/:pc/:id/trace` (a flat, single-level read of `prod.ProductionTrace`) are genuinely different Node routes serving different UI needs** — the former backs the standalone Traceability tile's full-chain view, the latter backs the Complete Run wizard's read-only checklist re-render for a job whose parent links were already saved at draft time. Kept as two separate routes/methods, matching Node exactly, rather than merged.
- **Still real, tracked, unbuilt scope after this slice**: Drumming's full entry flow (`submitDrumming` — BOM validation, braid-consumption auto-backflush via `backflushBraidedComponents`, concession-triggered goods-movement backflush, `Z_ZPRODBATCH_MAINT`), Failed Backflush's five-way per-process retry queue (its DR branch specifically re-invokes almost all of Drumming's own posting logic, so it can't be built before Drumming's entry flow lands — confirmed by reading Node's `/failed-backflush/:pc/:id/retry` DR branch directly), Drumming coils/reversal-status/ticket-print, and Order Lookup. The CO/BR/CL/TW draft→complete wizard itself shipped in the very next slice below.
- 9 new `BomHelperTests` (every validation-before-DB/before-SapServer-call path across `GetBomPreviewAsync`/`GetLatestBomAsync`/`RefreshBomAsync`/`AddRawMaterialBatchAsync`/`RaiseConcessionAsync`, using `MockBehavior.Strict` for the SapServer mock so a guard that fails to fire is actually caught — a loosely-typed `GetAsync<T>` setup wouldn't intercept the real call's specific generic instantiation — plus `BuildActualComponentList`'s pure-logic quantity-precedence test). Real HTTP smoke test confirms all 8 new/extended API routes return 401 unauthenticated.

### Sub-phase 6c, second slice: the CO/BR/CL/TW/EX draft→complete two-step wizard

`POST process/:pc/draft` + `POST process/:pc/complete/:recordId` — port of the BOM-validated draft→complete workflow in `routes/productionnexus.js`, built directly on the first slice's `BomHelper` infrastructure. Completes `MetreProcessHelper`'s three-way entry-point split: `EnterAsync` (direct, no BOM work), `DraftAsync`+`CompleteAsync` (two-step, BOM-validated), both sharing `RequireMetreProcess`/`ProductionSapHelpers`/`RecordScrapAsync`.

- **EX's traceability check is genuinely a different mechanism from CO/BR/CL/TW/DR's, not a special case of the same one — ported as two real, separate code paths, matching Node's own two separate functions exactly**: `ValidateMxTubLinksAsync` (EX only) resolves a linked MX parent by a specific `TubID` — not just a `MixingID` — and additionally requires that tub be staged into Billet and not scrapped, checks with no equivalent anywhere in `BomHelper.ValidateTraceabilityAgainstBomAsync` (the CO/BR/CL/TW/DR path, which just checks a linked parent record's `Material` against the BOM). `ParentBatchRef` gained an optional `TubId` (defaulted `null`) to carry this — harmless for the direct-entry path (`EnterAsync`), which predates Draft/Complete and never needed tub-level precision.
- **`RecordScrapAsync` is now shared between `EnterAsync` and `CompleteAsync`'s scrap-recording steps** — Node itself duplicates this logic inline in both routes (`entry` and `complete` are separate route handlers with no shared function between them); this port consolidates it into one method taking the raw values instead of a specific request DTO, rather than mechanically re-duplicating the same EX-vs-everyone-else branching logic a third time. A real simplification over Node's own structure, not a functional change — same "Three similar lines is better than a premature abstraction, but ~30 near-identical lines twice over is not" judgment call applied elsewhere in this migration.
- **`MarkMetreSapFailedAsync` is reused for BOTH a genuine SAP posting failure AND a pure pre-SAP traceability hard-block** (no SAP call ever attempted) — mirrors Node's own `markSapFailed` function, which the Node route calls from three different places for exactly this reason. All three paths return `Status="SAP_FAILED"` with the record marked Status=6, an `IsSuccess=0` `SAPPostings` row, and a `SAP_FAIL` event — HTTP 200 (not an error status) at the controller in every case, matching Node's own `res.status(200).json(...)` for what looks at first glance like a failure response.
- **The concession-covered posting path calls SapServer's confirmed `goods-movement-backflush` (BAPI_GOODSMVT_CREATE) endpoint** — `SapGoodsMovementRequest`/`SapGoodsMovementComponent`/`SapGoodsMovementResponse` mirror SapServer's own `GoodsMovementRequest`/`GoodsMovementComponent`/`GoodsMovementResponse` (`Models/Bapi/ProductionModels.cs` there) field-for-field, confirmed by reading SapServer's controller directly — including that endpoint's own documented "UNCONFIRMED for this use case — verify via test.http" caveat on SapServer's side, inherited here as-is (this port can't resolve that uncertainty, only faithfully call the contract as documented). Every BOM component is posted explicitly (correct ones included, not just the concession's substituted one) via `BomHelper.BuildActualComponentList` — never just the mismatched component — so the normal automatic backflush never also silently consumes the original wrong material on top of the explicit posting.
- **BR still gets the full BOM-vs-traceability hard-block check even though it never posts its own backflush** (early return immediately after) — preserved exactly per Node's own comment: BR consumes real raw material even though the work centre's own SAP BOM data is unreliable for the backflush itself, so a wrong-material link there is exactly as much a traceability problem as on any other process.
- **`ApportionMxExpectedConsumptionAsync` (EX only) is purely a reporting/reconciliation figure that never gates or decrements anything** — populates `ProductionTrace.ExpectedConsumptionKG` for every linked MX tub, splitting a shared component's BOM-derived total equally across however many tubs of it were linked; wrapped in a swallowed try/catch at the call site, matching Node's `.catch(() => {})` exactly (a reporting figure failing to compute must never block a real completion).
- **Frontend deliberately not built in this slice** — the draft→complete wizard is a genuine multi-step UI (BOM checklist rendering, an MX-tub picker distinct from the direct-entry form's simpler parent-batch list, a "Refresh BOM" action, a concession-raise dialog reachable from a blocked checklist row) that doesn't fit `metre-entry.js`'s existing single-page form shape at all — building it properly is its own scoped frontend effort, tracked separately rather than rushed into an ill-fitting shape. The backend is fully built, tested, and reachable directly (e.g. via the API or a future dedicated wizard page) regardless.
- 5 new `MetreProcessHelperTests` (every validation-before-DB path across `DraftAsync`/`CompleteAsync` — non-metre process code, missing material, non-positive `lengthMetres`). Real HTTP smoke test confirms both new API routes return 401 unauthenticated.

### Sub-phase 6c, third slice: Drumming's entry flow

`POST drumming/stock` + `POST drumming/customer` (`DrummingHelper.SubmitAsync`) — port of `routes/productionnexus.js`'s `submitDrumming`, the last piece of "real, tracked, unbuilt scope" the first slice called out. Confirmed via frontend grep (zero callers) that `POST /drumming/entry` is genuinely dead code in Node and was NOT ported — the live Drumming tile only ever calls the two routes above, both backed by one shared function differing only in `entryType`.

- **Drumming has no separate draft step, unlike CO/BR/CL/TW/EX** — the record is created already `Status=4`/`CompletedAt=GETDATE()` in one request, immediately followed by BOM download, the hard block, and the backflush — closer in shape to `MetreProcessHelper.EnterAsync` than to `DraftAsync`+`CompleteAsync`, even though it reuses the *wizard's* BOM/concession infrastructure (`BomHelper`), not `EnterAsync`'s.
- **Two genuine Drumming-only mechanisms, both new in this slice, neither shared with the wizard**: `AssertParentBatchesReversedAsync` (a DR parent link means "re-drum this batch" — only allowed once the *original* drum's `IsReversed` flag is set, which today only Warehouse/Staging's not-yet-ported `redrumReversal.js` ever sets — see the still-unresolved soft cross-department dependency this creates, noted again below) and `BackflushBraidedComponentsAsync` (braiding never posts its own SAP backflush, so a braided semi-finished material never actually lands in SAP stock — this backflushes it on demand, scoped to exactly what this drum consumes, via a plain `POST api/production/backflush` call per braided parent, before the drum's own backflush runs; a failure here throws and is caught by the same catch block as a drumming-backflush failure, deliberately — don't post the drum's own backflush if the raw material it consumed couldn't be accounted for).
- **`SapDrumBackflushRequest`/`SapDrumBackflushResponse` mirror SapServer's own `DrumBackflushRequest`/`DrumBackflushResponse` (`Models/Bapi/ProductionModels.cs` there) field-for-field, confirmed by reading SapServer's `ProductionController.DrummingBackflush` action directly** — the combined ZF40N-backflush-then-`Z_ZPRODBATCH_MAINT` endpoint, distinct from the plain `backflush` action every other process/the braid-consumption step above uses. The response's `Backflush` field carries the same `Type`/`MessageClass`/`MessageNumber` success/failure shape as a plain `BdcResponse` (validated the same way as `ProductionSapHelpers.ParseSapBackflush`, just nested one level deeper) — `RcBatch`/`RcPack` (the `Z_ZPRODBATCH_MAINT` result) and the BOM-mismatch fields are only meaningfully populated once the backflush itself actually produced a material document, matching SapServer's own documented early-return behavior.
- **`SapBomQuery` gained an optional `Component` filter** (`string? Component = null`, backward-compatible with the 3 existing single-arg call sites) to support the braid-consumption step's single-component BOM lookup (`sapGet('/api/production/bom', { Material, Component })` in Node) — confirmed against SapServer's actual `BomQuery` DTO, which already accepts this filter server-side.
- **The hard block reuses the just-downloaded, in-memory BOM rows from this same request — not a fresh `LatestBomSnapshotAsync` re-read**, unlike `MetreProcessHelper.CompleteAsync`. This is a real, deliberate difference from the wizard (which re-reads because draft and complete are two separate requests, possibly far apart in time and after a "Refresh BOM"); Drumming has no such gap to bridge, so it validates against exactly what it just fetched and persisted a few lines earlier, matching Node's own `drBomRows` variable reuse exactly.
- **The `BLOCKED` outcome is a genuine HTTP 409 with `success:false`, unlike every other Production write action's `SAP_FAILED`/HTTP-201 convention** — preserved exactly as a real, deliberate difference in Node itself (`res.status(409).json({ success: false, data: {...}, error: errMsg })`), not normalized away to match the wizard's `MarkMetreSapFailedAsync` pattern. `DrummingSubmitResult.Status` carries `"BLOCKED"`/`"SAP_FAILED"`/`"COMPLETE"`; the controller maps `BLOCKED` to `StatusCode(409, ...)` with an explicit `ApiResponse<T>` construction (`success:false` + populated `data` + `error`) since `ApiResponse<T>.Fail` alone can't carry a `data` payload alongside the error — every other status still returns 201.
- **`drumRef` is genuinely hyphenated (`DR-00000123`), unlike every other process's `{code}{recordId:D8}` format (`EX00000005`)** — preserved exactly per Node's own `` `DR-${String(drummingID).padStart(8, '0')}` ``. The re-drum-reversal error message quotes the *unhyphenated* form instead (`DR00000123`) — a real, separate Node inconsistency between the two, preserved as-is rather than "fixed" to match either convention, per this migration's established precedent (SapServer's `CLAUDE.md` documents several similar real-behavior-over-tidiness calls).
- **`MetreProcessHelper.RecordScrapAsync` made `internal` and reused as-is** — Drumming is never `"EX"`, so it always takes the proportional-share branch, the exact same math Node's own `submitDrumming` duplicates inline a third time (Node has no shared function between `entry`, `complete`, and `submitDrumming` for this). A real simplification over Node's structure, not a functional change, same judgment call the second slice already made for `EnterAsync`/`CompleteAsync`.
- **The concession-covered goods-movement path and the SAP-failure catch block are otherwise a straight, unremarkable reuse of the second slice's own patterns** (`BomHelper.BuildActualComponentList`, `SapGoodsMovementRequest`, the `catch (Exception sapErr) when (sapErr is not NexusApiException)` filter that lets a genuine SapServer-connectivity failure surface as a real 502/503 rather than being swallowed into a 201) — nothing new to say about these beyond "same as `MetreProcessHelper.CompleteAsync`."
- **Still not resolved**: the soft cross-department dependency the earlier research phase flagged — `AssertParentBatchesReversedAsync` depends on `prod.Drumming.IsReversed`, a flag only Warehouse/Staging's `redrumReversal.js` (not yet ported — Phase 7) ever sets. Until Warehouse ships, no drum in this new database can ever actually satisfy the re-drum check; this is expected and matches the real Node app's own inter-department ordering, not a gap introduced by this migration's phase sequencing.
- **Still real, tracked, unbuilt scope after this slice**: Failed Backflush's five-way per-process retry queue (now unblocked — its DR branch re-invokes almost all of this slice's own posting logic), Drumming coils/reversal-status/ticket-print (`GET drumming/:id/coils`, `GET drumming/:id/reversal-status`, `GET drumming/ticket/:referenceDocument/:item/print` + `loadDrummingTicketData`/`buildDrummingTicketHTML`), `GET drumming/data`, and Order Lookup. Frontend deliberately not built in this slice, same reasoning as the wizard's own second-slice note — Drumming's entry form is a substantial standalone UI (coil-length list entry, parent-batch/tub pickers, raw-material-batch entry, scrap entry) that doesn't fit any existing page shape yet.
- 5 new `DrummingHelperTests` (every request-shape validation path — empty/over-limit `coilLengths`, missing `material`/`packagingId`, non-positive `weightKg` — all before ever opening a connection, same caveat as everywhere else in this migration: the SAP-posting/hard-block/braid-consumption paths need a live SQL Server and SapServer to exercise for real).

### Sub-phase 6c, fourth slice: Failed Backflush's five-way retry queue

`GET failed-backflush` + `PATCH failed-backflush/:pc/:id/retry` + `PATCH failed-backflush/:pc/:id/cancel` (`FailedBackflushHelper`) — port of the supervisor-facing retry queue for every `Status=6` (SAP_FAILED) production record. This was the piece the first slice's own "still real, tracked, unbuilt scope" note explicitly called out as blocked on Drumming's entry flow landing first, since the DR retry branch re-invokes almost all of `DrummingHelper`'s own posting logic — it's unblocked now that the third slice shipped.

- **Five genuinely different retry shapes dispatched off one process code, matching Node's own single route handler exactly** (`ProductionSapHelpers.Process`/`MetreProcesses` already had every code/table/pk this needed): MX retries per individually-failed tub (`prod.MixingTubs.SAPSuccess=0`), never the whole mix at once — a failing tub's own SAP error is caught right there and recorded per-tub, so one bad tub never stops the rest from retrying. DR re-runs almost the exact hard-block/concession/backflush sequence `DrummingHelper.SubmitAsync` itself uses. EX/CO/BR/CL/TW re-post a plain ZF40N backflush (EX additionally re-gates on MX-tub staging via the wizard's own `ValidateMxTubLinksAsync`/`ApportionMxExpectedConsumptionAsync`, both made `internal` for this reuse). EW/HA have no SAP call at all — "retry" there just means a supervisor reviewed the record and marked it complete. FW/an unknown code returns "Retry not yet implemented," matching Node's own fallthrough.
- **A confirmed, genuine bug in Node's own DR non-concession retry branch was found and fixed, not faithfully reproduced.** Node's retry route calls `sapPost(\`/drumming/${entryType}\`, {...})` for this case — but `sapPost` always prefixes `sapConfig.url` (SapServer's own base URL, confirmed from `config.js`'s import), and SapServer has no `/drumming/*` routes at all (confirmed directly against `SapServer/Controllers/ProductionController.cs` — its real combined endpoint is `POST api/production/drumming-backflush`). The body Node sends there (`TotalLength`/`Order`) doesn't even match that endpoint's real request DTO's field names either, meaning every non-concession DR retry would 404 in production. This isn't the kind of confirmed-in-production quirk this migration otherwise preserves (like `drumRef`'s hyphenation, or `SapPad`'s real behavior documented in SapServer's own `CLAUDE.md`) — it's an unexercised, broken path that would only ever be hit on the rarer non-concession DR retry. Ported to call the correct `api/production/drumming-backflush` endpoint instead, resolving fresh traceability materials via `DrummingHelper.ResolveTraceabilityMaterialsAsync` (made `internal` for this reuse) the same way the original submission does — matching what a *working* retry needs to do, not what Node's broken one actually sends.
- **DR's hard block re-checks against the *persisted* BOM snapshot (`BomHelper.LatestBomSnapshotAsync`), not a fresh SAP re-download** — a real, correct difference from `DrummingHelper.SubmitAsync`'s own hard block (which uses the just-downloaded in-memory rows from the same request): this is a retry of the *same* job, potentially long after the original submission, so it must validate against what was actually saved (and possibly since "Refresh BOM"-ed), matching Node's own `latestBomSnapshot(pool, 'DR', id)` call in the retry route exactly.
- **DR retry's `SAPPostings` insert omits `SAPBatchNumber` entirely, unlike `DrummingHelper.SubmitAsync`'s own non-concession branch** — a real, narrower omission in Node's retry route itself (its INSERT statement genuinely has one fewer column here), preserved as-is rather than "fixed" to match the original submission's shape.
- **The `BLOCKED` outcome (DR only) is a genuine HTTP 409 with `success:false` — same real convention `DrummingSubmitResult` already established** for the original submission. One small, deliberate shape difference: Node's retry route echoes `drummingID` inside its `data` payload (`res.status(409).json({ success:false, data:{drummingID:id,status:'BLOCKED'}, error:errMsg } )`); this port's shared `FailedBackflushRetryResult` (used by every branch, not DR-specific) has no such field, so the C# response's `data` carries just `{status:'BLOCKED', ...}` without the id. Harmless in practice — the id is already in the URL the caller just PATCHed — but a real, intentional simplification worth naming rather than silently dropping.
- **EX's `Cannot retry` billet-staging failure is a plain `NexusConflictException` (409, no `data` payload)** — genuinely different in shape from DR's `BLOCKED` result (which is a *returned value*, not a thrown exception, so it can carry a `data` payload) — matches Node's own `res.status(409).json({ success:false, error:'Cannot retry — ...' })` (no `data` key at all) exactly.
- **The shared catch (every branch except MX, which handles its own per-tub failures internally) maps a genuine SAP/DB failure to a real HTTP 502** via `NexusBadGatewayException` — no `SAP_FAILED`-200 grace period here, unlike `DrummingHelper.SubmitAsync`/`MetreProcessHelper.CompleteAsync`'s own convention for a *first* submission failing. Matches Node's own `res.status(502).json({ success:false, error:errMsg })` for a retry that fails again — a supervisor retrying from this queue gets a real error, not another silent queue entry.
- **`GET failed-backflush`'s 9-way `UNION ALL` is built dynamically off `ProductionSapHelpers.Process`/a literal process-code list**, same style already established for `MetreProcessHelper.GetOpenRunsAsync`'s near-identical multi-process union query, rather than hand-written per branch — deliberately excludes FW (no `Status=6` union branch for it in Node either, matching the "not yet implemented" retry fallthrough above).
- **`PATCH .../cancel` has no rows-affected check** — an unconditional `UPDATE ... WHERE Status = 6` followed by an always-written event log entry and always-200 response, even if the record has since moved on (retried successfully, already cancelled) — matches Node's own route exactly, which never checks `rowsAffected` either.
- All three new routes are `[Authorize(Policy = "Perm:" + ProductionReportsHelper.FnSupervisor)]`-gated, reusing the existing `PROD_SUPERVISOR` policy constant already wired up for `open-runs`/`history`/`trace`/`scrap`/`reversal` — matching Node's own `requirePermission('PROD_SUPERVISOR')` on all three routes exactly.
- 2 new `FailedBackflushHelperTests` — `RetryAsync` opens a connection unconditionally before dispatching (the process code itself picks the branch, there's no request-shape check that could reject early the way every other Helper's validation does), so only `CancelAsync`'s unknown-process-code guard is unit-testable without a live SQL Server/SapServer; a second test confirms MX (special-cased with a hardcoded table/pk in Node's own cancel route, unlike this port's uniform `ProductionSapHelpers.Process` lookup) isn't mistakenly rejected as unknown. Real HTTP smoke test confirms all 3 new routes return 401 unauthenticated.

### Sub-phase 6c, fifth slice: Drumming Ticket, Order Lookup, coils/reversal-status/data

`GET drumming/data`, `GET drumming/:id/coils`, `GET drumming/:id/reversal-status`, `GET order-lookup`, `GET order-lookup/by-order/:orderNumber`, `GET drumming/ticket/:referenceDocument/:item/print` — the last of Sub-phase 6c's own "still real, tracked, unbuilt scope" list from the third slice. Closes out Drumming and Sub-phase 6c entirely; Sales/Production's shared `production-schedule` cluster and Sub-phase 6b's Batch History/Traceability reports were the only other consumers of `log.AgreementSnapshot`-style data before this.

- **`GET drumming/ticket/:referenceDocument/:item/print` is a genuinely different response shape from every other route in this controller — it returns raw `text/html`, not the JSON `ApiResponse<T>` envelope**, meant to be opened directly in a new browser tab (`window.open(..., '_blank')`) which auto-triggers `window.print()` on load — the OS-native print dialog, not the network-printer TCP flow `LabelPrintHelper` uses for production labels. `LabelsController.PreviewProcess` (Sub-phase 6b) already established this exact pattern — a data-fetch Helper (`OrderLookupHelper.LoadTicketDataAsync`) throwing the normal `NexusApiException` family on failure (still converted to JSON by `ApiExceptionMiddleware`, since this route sits under `/api/productionnexus` like everything else — a real, accepted asymmetry: only the *success* path is raw HTML) plus a separate, pure string-building Helper (`DrummingTicketHtmlHelper`, mirroring `LabelHtmlHelper`'s own split) for the render itself — followed here rather than inventing a new convention.
- **`GET api/production/order-text/{salesDocument}/{item}` is a plain path-segment SAP GET (no request body), confirmed directly against `SapServer/Controllers/ProductionController.GetOrderText`** — the first SapServer call in this migration that isn't the established "GET-with-a-JSON-body" shape (`BomQuery`/`ProfitCentreRequest`/etc.) — `ISapServerClient.GetAsync<T>`'s `body` parameter already defaults to `null` for exactly this case, so no client-side change was needed, just a call site that omits it. Deliberately **not cached** — a live lookup on every print, per the "process critical" instruction the original Node code's own comment records — a SAP failure here degrades to an inline `[Could not reach SAP for special instructions: ...]` message on the printed ticket rather than failing the whole page, matching Node's own try/catch around `sapGet` exactly.
- **`ReferenceDocument`/`Item` throughout this slice are `OriginalDoc`/`OriginalDocItem`, not `log.AgreementSnapshot`'s raw `ReferenceDocument`/`Item` columns** — without this aliasing, an operator searching by order number (or reprinting a ticket) for a line that's just been picked would get "no open items found," because SAP has already flipped that line's raw `ReferenceDocument` to a delivery number by then. `AgreementLookupRow`/`AgreementLookupColumns` (shared by `SearchAsync`, `GetByOrderAsync`, and `LoadTicketDataAsync` — one column list, three call sites, matching Node's own single `AGREEMENT_LOOKUP_COLUMNS` constant) alias this at the SQL layer so every consumer sees a plain `ReferenceDocument`/`Item` field regardless.
- **The printed ticket's `RequiredQty` formatting (`#,##0.###` under `en-GB`) approximates JS's `toLocaleString('en-GB', { maximumFractionDigits: 3 })`** — trims trailing zeros (matching a whole-number quantity rendering as `400`, not `400.000`) while keeping thousands separators, the first place in this migration a JS-style trimmed-decimal display had to be reproduced server-side in C# rather than left to a client-side JS formatter (`reports-common.js`'s `fmtNum` and friends do the equivalent client-side elsewhere) — because this whole page is rendered on the server, not hydrated from JSON.
- **The 35-slot, 3-column hand-fill coil checklist (`DrummingTicketHtmlHelper.CoilCol`) is unrelated to `prod.DrummingCoils`** — a fixed paper-grid layout matching the Excel Ticket tab's operator grid (columns 1–12/13–24/25–35, `Math.Ceiling(35/3)`), not a rendering of the coil-length rows an operator actually enters at submission time (`GET drumming/:id/coils`, a completely separate, genuinely tiny endpoint reading `prod.DrummingCoils` directly). Two unrelated "coil" concepts sharing a name, preserved as two separate, unconnected pieces exactly as Node has them.
- **`GET drumming/:id/reversal-status` is deliberately a *client-side convenience only*** — Node's own comment calls it out explicitly as "quick check for the traceability safeguard's instant client-side feedback," with `DrummingHelper.AssertParentBatchesReversedAsync` (third slice) remaining the sole authoritative, server-enforced check applied at actual submission time. This port's `GetReversalStatusAsync` throws `NexusNotFoundException` for an unknown `drummingId` (404), matching Node's own explicit `if (!r.recordset.length) return res.status(404)...` check — the one place in this slice with a real not-found guard; every other new read here (coils, drumming/data, order-lookup's plain search) just returns an empty result set for an unmatched filter, same as Node.
- **`drumming/data`'s query DTO follows the exact `MetreProcessHelper.GetDataAsync`/`MetreProcessDataQuery` precedent** (`DateTime.TryParse` for optional from/to bounds, `LIKE '%...%'` wildcarding for text filters) — a straight, unremarkable reuse of an already-established pattern, nothing new to say about it.
- 6 new tests: 4 `DrummingTicketHtmlHelperTests` (pure string-building, no I/O — same fully-real-testable category `LabelHtmlHelperTests` already established, covering the order reference/customer details, the SAP/customer-instructions empty-state placeholders, all 35 numbered coil-checklist cells across 3 tables, and HTML-encoding) + 1 `OrderLookupHelperTests` (the one pre-connection validation path — `SearchAsync` rejecting a search with neither `material` nor `customer`). Real HTTP smoke test confirms all 6 new routes return 401 unauthenticated.

**Sub-phase 6c is now complete** — BOM/concession infrastructure, the CO/BR/CL/TW/EX draft→complete wizard, Drumming's entry flow, Failed Backflush's five-way retry queue, and Drumming Ticket/Order Lookup/coils/reversal-status/data have all shipped. The soft cross-department dependency flagged in the third slice (`AssertParentBatchesReversedAsync` needs Warehouse's not-yet-ported `redrumReversal.js` to ever actually set `prod.Drumming.IsReversed`) remains open, expected, and tracked for Phase 7 — not a gap introduced by this sub-phase's own sequencing. Frontend for the entire BOM-validated wizard, Drumming's entry form, and this slice's Order Lookup/Drumming Ticket pages is still deliberately deferred, same reasoning recorded at each backend slice above: real, substantial standalone UI work that doesn't fit any existing page shape, scoped separately rather than rushed.

## Phase 7: Warehouse

Researched via a dedicated background investigation before writing any code (same discipline Phase 6 used) across `routes/deliverymain.js` (2321 lines/36 routes), `staging.js` (865), `consignment.js`+`consignmentsql.js` (520+942), `packaging.js`/`packagingdata.js`/`palletdata.js`/`palletmain.js`/`palletpackages.js`/`palletvalidation.js`/`sapStaging.js`/`deliverylink.js`/`deliveryroutes.js`, cross-checked against `private/js/warehouse.js`/`logistics.js` for live frontend callers and against SapServer's actual controllers for confirmed endpoint contracts.

- **Two whole Node route files the research initially flagged as Warehouse turned out to belong elsewhere, and are excluded from this phase entirely**: `consignment.js`/`consignmentsql.js` (Vendor Consignment Tracker) — its own file header says "Logistics > Material Planning," its only caller is `logistics.js`, and it posts nothing to SAP itself (settlement is recorded manually after a user runs MRKO in SAP GUI) — picked up under a future Logistics phase instead. `packaging.js` (SAP packaging-instruction/BOM master-data maintenance) — its own header says "Engineering > Packaging Data," called from `engineering.js`, and **already ported**: `EngineeringController.cs`/`Helpers/Engineering/EngineeringHelper.cs` (Phase 2) cover exactly this route file. Neither exclusion was assumed from the Node filename alone — both confirmed by reading the file's own header comment and its real caller.
- **Confirmed dead code, not ported**: `routes/deliverylink.js` (whole file, 4 routes — zero callers anywhere in `private/`; `log.DeliveryLink` reads/writes actually happen through `deliverymain.js`'s own `pallets` routes instead), `deliverymain.js`'s plain `GET /`, `/customer/:id`, `/operator/:name`, `/daterange`, `POST /` (generic `log.DeliveryMain` CRUD superseded by the specific routes below — `GET /id/:deliveryId` is the one generic-looking read that IS live, confirmed separately), and every `POST` (create) route on `palletdata.js`/`packagingdata.js`/`palletvalidation.js` plus their `GET /id/:id`/`GET /packaging/:id`/bare `GET /` reads that have no confirmed caller — see the first slice below.
- **Proposed build order** (smallest/most self-contained first, matching every earlier phase's precedent): **7a** read-only reports + simple shared master-data CRUD (this slice, below, plus the still-outstanding SAP-backed picking-materials panel — `open-picksheets`, `packaging-holding`, `picksheet-materials`, `linked-picksheets`, `link-search`, `landing-sparkline` — deliberately split into its own follow-up slice once research showed `getRemainingRequiredMaterials`'s SAP-orchestration complexity, European-decimal parsing, and staging-bin conflict logic is a different scale of work from plain CRUD). **7b** the pallet/package builder + staging reversal (`palletmain.js`, `palletpackages.js`, `sapStaging.js`'s shared `reverseStagedPackage`, `deliverymain.js`'s link/unlink/stage-batch/comment/cancel-picksheet). **7c** the SAP-write-heavy/stateful completion pipeline (ZDEL weight push → ZDELFLAG/ZDELPACK maintenance → Goods Issue, `sync-delivery-quantities`/BAPI_OUTB_DELIVERY_CHANGE, the ZDELFLAG/Goods-Issue warning-log queues, `staging.js`'s transfer-order/consignment-MB1B deliver route, and `lib/redrumReversal.js`).
- **`lib/redrumReversal.js` — the resolution for the soft cross-department dependency Production's Sub-phase 6c flagged** (`DrummingHelper.AssertParentBatchesReversedAsync` needs `prod.Drumming.IsReversed` to ever be set) — confirmed to live at exactly that path, exporting `maybeReverseBatchManagedReturn`: triggered when stock lands in SA/PTFE, it reverses the original 131 backflush (MF41), tidies the WM bin, and — only if the batch came from this system's own Drumming feature — sets `IsReversed=1` (plus reversing any braid-component backflushes and undoing the order's `DockStockAllocated` bump). Called from `staging.js`'s deliver route and `sap.js`'s general Stock Transfer tool. **Owned by Sub-phase 7c**, since it's triggered from that same cluster's SAP-posting deliver endpoint — until 7c ships, no drum in this new database can actually satisfy Drumming's re-drum check, matching the real Node app's own inter-department ordering rather than a gap this migration introduced.

### Sub-phase 7a, first slice: pallet/packaging master data

`GET/PUT api/palletdata`, `GET/PUT api/packagingdata`, `GET api/palletvalidation/pallet/:palletId` (`WarehouseMasterDataHelper`) — port of `palletdata.js`/`packagingdata.js`/`palletvalidation.js`, chosen first within Phase 7 as the genuinely smallest, simplest, most self-contained piece (three ~80-line Node files, no SAP calls, no `writeEvent`-style infrastructure) to prove the Warehouse pattern out before anything SAP-facing.

- **Genuinely shared between Warehouse and Logistics — confirmed via frontend grep, not assumed** — both `private/js/warehouse.js`'s pallet builder and `private/js/logistics.js`'s "Update Pallet/Packaging Data" admin screens call all three Node files directly, and `production-nexus.js`'s Drumming entry form (Sub-phase 6c, frontend still deferred) separately calls `GET /api/packagingdata` for its packaging-type dropdown. **No `Dept:warehouse` policy was added** — a deliberate departure from Phase 6's own precedent of tightening a department-gate-less Node route once confirmed genuinely single-department (e.g. Production's `history`/`trace`) — here the opposite is confirmed true: adding a department gate would incorrectly lock out real Logistics users before that department even exists in this migration. Node's actual gate (`requireLogin` only, no department check, in `server.js`) is kept exactly.
- **`PUT` on both `palletdata`/`packagingdata` is gated `[Authorize(Policy = "Perm:LOG_ADMIN")]`** — `LOG_ADMIN` is a Logistics-owned legacy permission code with no C# constant yet (Logistics hasn't been ported), so this is the literal string, matching how Sub-phase 6a similarly deferred splitting `PROD_SUPERVISOR` until the department that owns a code actually gets its own phase — not a decision this slice should make on Logistics's behalf.
- **Every route with a confirmed live caller is ported; every one without isn't** — see the Phase 7 overview's "confirmed dead code" bullet above for the specific list (all three files' `POST`/create actions, plus `GET /id/:id`-style single-record reads on `palletdata`/`packagingdata` and `palletvalidation`'s bare `GET /`/`GET /packaging/:id`). `palletvalidation.js`'s own code comment at the `GET /pallet/:palletId` call site explains directly why the bare, un-scoped table read is never used: it would show every packaging option for every pallet type at once, not just the ones valid for the selected type.
- **No validation anywhere in this Helper, and the `UPDATE` queries have no rows-affected check — both match Node exactly**, same "preserve, don't tighten, an already-confirmed real behavior" precedent as `FailedBackflushHelper.CancelAsync`. Consequently there's nothing pre-connection to unit-test here (every method opens a connection unconditionally) — no new Helper tests this slice, same caveat class as most of Finance/Sales's plain SQL-touching Helpers. Real HTTP smoke test confirms all 5 new routes return 401 unauthenticated.
- Frontend deliberately not built yet — these three endpoints are consumed by the pallet builder (Sub-phase 7b) and Drumming's entry form (Production, already deferred), neither of which exists as a page in this migration yet; building this cluster's own frontend in isolation would have nothing real to plug into.

### Sub-phase 7a, second slice: Open Picksheets, Packaging Holding, and the SAP-backed picking-materials panel

`GET api/deliverymain/id/:id`, `/open-picksheets`, `/packaging-holding`, `/:id/picksheet-materials`, `/:id/linked-picksheets`, `/link-search` (`WarehousePicksheetHelper`) — the read-only cluster split off from the master-data slice once research showed `getRemainingRequiredMaterials`'s SAP orchestration, European-decimal parsing, and staging-bin conflict logic is a different scale of work from plain CRUD. `landing-sparkline` (also in `deliverymain.js`) is deliberately excluded — confirmed via `private/js/landing.js` that it's one of three per-department sparkline widgets on the portal's own dashboard page, a cross-cutting concern nothing in this migration has built yet (Production's own `landing-sparkline` wasn't ported during Phase 6 either), not something for one department's own phase to pick up piecemeal.

- **A significant, genuine discrepancy from Node was found here and deliberately NOT silently perpetuated into more code: Node's own SAP calls all authenticate as a fixed placeholder, not the real calling user.** `deliverymain.js`'s local `makeSapToken()` (and the identical one in `productionnexus.js`, and `sap.js`'s own exported version — confirmed by reading all three) always signs `{ userId: 0 }`, with `productionnexus.js`'s own copy carrying an explicit `// TODO: include user info in token if needed for SAP logging; currently just a placeholder`. This port's `ISapServerClient`/`SapServerClient.MakeSapToken(userId)` has embedded the *real* calling user's ID in every SAP call since Sub-phase 6b's own `MakeSapToken` doc comment claimed it "matches sap.js's makeSapToken exactly" — which, per this direct read of Node's actual source, it does not. **Not changed here** — reverting to Node's placeholder-0 behavior would be a cross-cutting change touching every previously-shipped department (Engineering through Production), not something to slip in as a side effect of one Warehouse slice, and it's plausible the real-userId design was a deliberate, considered improvement over Node's own TODO-flagged wart rather than an oversight (SapServer's permission model is genuinely built around real per-user `PortalUserDepartments` resolution, which a fixed userId:0 could never exercise correctly). Flagged here prominently, unresolved, for a deliberate decision once real login/permission data against a live SapServer exists to actually observe which behavior the two systems' current production deployment relies on.
- **`ParseSapQuantity` deliberately does NOT port Node's own `parseSapNum` bug-compatible.** Node's `parseSapNum` (duplicated identically in `getRemainingRequiredMaterials` and `getDeliveryQuantityMatch`) assumes every SAP quantity string is European-grouped, unconditionally stripping any period whenever no comma is present — the exact fixed-format assumption **SapServer's own `CLAUDE.md` already documents as confirmed wrong** for this SAP system's genuinely mixed-format data (the real, production-confirmed 1000x-inflation bug that motivated `RfcRowExtensions.ParseSapDecimal`'s per-value last-separator-wins fix). SapServer's `PicksheetHelpers.ParseStockRows`/`ParseLipsRows` return these exact same raw, unparsed strings with no server-side correction (confirmed by reading that source directly), so the client is responsible for parsing them either way — this port applies the already-proven-correct algorithm instead of Node's, ported directly from `RfcRowExtensions.ParseSapDecimal`'s own logic. Verified with a real unit test (`ParseSapQuantity_resolves_the_real_decimal_point_per_value`) covering the exact case Node's version would get wrong: a plain invariant decimal like `"1234.56"` with no comma at all.
- **`GET id/:id` keeps Node's real `SELECT *` scope as a full known-column `DeliveryMainRow` DTO, not trimmed to the one field (`picksheetComment`) every confirmed frontend caller actually reads** — a generic "get delivery by ID" read is exactly the kind of endpoint a not-yet-built page is likely to need more of later; trimming it now would just mean re-widening it in a future slice for no real benefit today.
- **`WAREHOUSE_OP` is Warehouse's own widest-reaching legacy code, kept unsplit for this slice** — same deferred-per-tile-split precedent Production's Sub-phase 6a set for `PROD_SUPERVISOR`; `WarehousePicksheetHelper.FnOp` holds the literal string for now, reused via `[Authorize(Policy = "Perm:" + WarehousePicksheetHelper.FnOp)]` on `open-picksheets`/`packaging-holding`/`picksheet-materials`/`link-search` — matching Node's own per-route `requirePermission('WAREHOUSE_OP')` calls exactly. `linked-picksheets` and the generic `id/:id` read have no permission gate in Node either, and none was added here.
- **`link-search`'s required `excludeDeliveryId` is bound as a nullable `long?`, not a bare `long`** — a required non-nullable `[FromQuery]` value type on an `[ApiController]` would trigger ASP.NET Core's own automatic 400 (a generic `ValidationProblemDetails`, not this app's `ApiResponse<T>` envelope) before the action even runs. Validated inside `LinkSearchAsync` instead (`NexusValidationException` when null), matching this migration's own established "validate in the Helper, not via framework auto-validation" convention everywhere else, and preserving Node's own custom error message shape.
- 9 new `WarehousePicksheetHelperTests` — 8 cover `ParseSapQuantity` directly (pure logic, no I/O, fully real-testable — European-grouped, comma-only, plain-invariant, integer-only, US-grouped, and empty/null/unparseable inputs) + 1 covers `LinkSearchAsync`'s pre-connection validation path. Real HTTP smoke test confirms all 6 new routes return 401 unauthenticated.

### Sub-phase 7b: the pallet/package builder + staging reversal

`GET/POST/PATCH api/palletmain`, `GET/POST/PATCH/DELETE api/palletpackages` (`PalletMainHelper`/`PalletPackagesHelper`), plus `routes/sapStaging.js`'s shared `reverseStagedPackage` (`SapStagingHelper`) — port of `palletmain.js`/`palletpackages.js`, the core of the pallet builder. Neither Node file imports `requirePermission` at all — both mounts are `requireLogin`-only in `server.js`, kept exactly (no policy added on any action).

- **Only the routes with a confirmed live frontend caller are ported, same discipline as every earlier Warehouse slice** — `GET id/:id`, `POST /`, `PATCH /:id` on both files, plus `palletpackages.js`'s `GET pallet/:palletId` and `DELETE /:id`. `palletmain.js`'s bare `GET /`, `GET /category/:c`, and `GET /location/:l`, and `palletpackages.js`'s bare `GET /`, `GET /id/:id`, `GET /sapdelivery/:id`, and `GET /sapmaterial/:id` all have zero confirmed callers and are not ported. `landing-sparkline` (also in `palletmain.js`) is excluded for the same cross-cutting-dashboard-widget reason as `deliverymain.js`'s own copy in the previous slice.
- **`reverseStagedPackage` is genuinely shared, load-bearing infrastructure, ported once as `SapStagingHelper.ReverseStagedPackageAsync`** — both a pallet removal (`PalletMainHelper.UpdateAsync`, reversing every package on the pallet) and a single package delete (`PalletPackagesHelper.DeleteAsync`) depend on it. `SapServerClient.PostAsync` already throws a `NexusApiException` for SapServer's real 422-rejection case, which this method catches and converts into the graceful `Attempted:true/Success:false` result the caller needs — mirroring Node's own try/catch shape without needing to duplicate SapServer's success/failure envelope inspection by hand.
- **Both "fails closed" reversal paths are preserved exactly**: a pallet is not marked removed, and a package is not deleted, unless every SAP reversal it depends on actually succeeded — matching Node's own explicit reasoning comments (there's no atomic multi-call SAP transaction available, so a partial failure must leave the app and physical/SAP reality unable to disagree about where stock is, rather than silently proceeding). Pallet removal's 422 response carries a structured `Failures` list (one entry per package that couldn't be reversed); package delete's 422 is a plain message, matching the real difference in Node's own two response shapes (`{success:false,error,failures:[...]}` vs `{success:false,error}` — a single string with `${reversal.error} — package was not removed}` appended).
- **`PalletUpdateResult` is a Helper-returned value, not a thrown exception, for the reversal-blocked case** — same "Helper returns a result, controller maps it to the right HTTP status with a manually-constructed `ApiResponse`" pattern Sub-phase 6c's `DrummingSubmitResult`/`FailedBackflushRetryResult` already established for a `success:false` response that also needs to carry a structured `data` payload alongside its error, which `ApiResponse<T>.Fail` alone can't do.
- **`UpdatePalletRequest`'s partial-update semantics collapse "field omitted" and "field explicitly `null`" into the same behavior (leave the column alone), via `COALESCE(@x, column)` for every field** — a deliberate, small simplification over Node's own `!== undefined` presence check, which would let an explicitly-sent JSON `null` attempt to null out a `NOT NULL` column; no confirmed caller ever does this, and the simplified version is strictly safer. The "nothing to update at all" 400 is preserved via an explicit `HasAnyUpdateValue` check across every field before the `UPDATE` runs, rather than trying to infer it from the `SET` clause the way Node's dynamically-built `sets[]` array does.
- **`palletFinish`'s side effect (also stamping `palletFinishDate = GETDATE()`) only fires on an explicit `true`, matching Node's own truthy check** — sending `palletFinish: false` (a real, valid value, not an absence sentinel) correctly leaves `palletFinishDate` untouched, same as omitting the field entirely; only `palletFinish: true` stamps it.
- 4 new `PalletPackagesHelperTests` (every pre-connection validation path on `PalletPackagesHelper.UpdateAsync` — neither field provided, non-positive `palletLayer`, blank `packagingID`). `PalletMainHelper.UpdateAsync` has no pure pre-connection validation path to test (every call opens a connection first, same as Node's own `getPool()`-before-anything-else shape) — no forced test written for it. Real HTTP smoke test confirms all 7 new routes return 401 unauthenticated.

### Sub-phase 7c, first slice: the delivery completion pipeline

`PATCH api/deliverymain/:id/complete` + `POST api/deliverymain/:id/sync-delivery-quantities` (`DeliveryCompletionHelper`) — port of `completeOneDelivery`/`runZdelflagMaintenance`/`runGoodsIssueApproval`/`getDeliveryQuantityMatch` and their two route handlers. The single highest-risk cluster in Warehouse — real SAP goods movements (ZDEL weight push, ZDELFLAG/ZDELPACK maintenance, Goods Issue, delivery-quantity correction), not just reads or reversible reversals.

- **A confirmed, live bug in Node's own Goods Issue call was found and fixed — in Node itself, not just this port — per explicit user direction ("update node to match sap server, as this was confirmed working using manual curls").** `runGoodsIssueApproval` was posting `POST /api/warehouse/goods-issue` with only `{DeliveryNumber}`. SapServer's own integration moved from `BAPI_DELIVERYPROCESSING_EXEC` (never worked live) to `BAPI_OUTB_DELIVERY_CONFIRM_DEC` (`GoodsIssueModels.cs`'s header comment), confirmed live 2026-08-28 that `POST_GI_FLG` alone isn't enough — SAP rejects with "Delivery has not yet been put away / picked (completely)" unless `Items` also carries each real delivery item's picked quantity (`ITEM_DATA_SPL-QTY_POST`). Node's `routes/deliverymain.js` was updated directly (not just this C# port) to build `Items` from `log.PalletPackages` (`SUM(sapQuantity)` grouped by `sapDeliveryItem`, real batches only — `sapMaterial IS NOT NULL`), the same source/grouping `getDeliveryQuantityMatch` already uses; its 3 `goods-issue/reprocess` tests reaching this code path were updated to match and now assert the real `Items` payload is sent, not just deleted/skipped. **This landed on `dotnet-rewrite`, not `main`** — flagged to the user as a genuine production bug fix that won't reach the live Node app until this branch merges (not before cutover per the PR description), pending their decision on whether to cherry-pick it to `main` directly.
- **A second, separate, NOT-yet-confirmed contract gap was found while porting `sync-delivery-quantities` and deliberately left unfixed, unlike Goods Issue** — `POST /api/warehouse/delivery-change` (`BAPI_OUTB_DELIVERY_CHANGE`) needs `BaseUom`/`SalesUnit`/`FactUnitNom`/`FactUnitDenom` per `DeliveryChangeModels.cs`'s own extensively-documented live diagnosis timeline (confirmed 2026-08-28 that `BASE_UOM` alone caused `VLBAPI 004`/`VL 268` until those other fields were also supplied); `SapDeliveryChangeItem` on this side (and Node's own `sync-delivery-quantities` route) has only ever sent `ItemNumber`/`Material`/`Quantity` — no `BaseUom` at all, which `BuildDeliveryChangeRequest` defaults to a bare empty string when absent. Unlike Goods Issue, this was **not** confirmed working via a live curl test, so — per the explicit distinction the user drew between the two — this port faithfully mirrors Node's current (quite possibly still-broken) behavior rather than guessing at a fix. Documented prominently on `SapDeliveryChangeRequest`'s own doc comment in `WarehouseSapModels.cs` and here, for the same kind of deliberate live-curl verification Goods Issue just got, whenever that's next convenient.
- **A third, systemic finding, orthogonal to both of the above: `SapServerClient.PostAsync`/`GetAsync` discard SapServer's structured error `data` payload on any failure response, keeping only the flat `error.Message` string.** Confirmed by reading `SapServerClient.SendAsync`'s exception path directly (`throw new SapProxyException(statusCode, error?.Code, error?.Message)` — `envelope.Data` is never read on the failure branch) against SapServer's own `WarehouseController.PostGoodsIssue`, which returns `ApiResponse<GoodsIssueResponse>.Fail("422", "...", response)` — a real 3-argument `Fail` overload that embeds the full `GoodsIssueResponse` (including its own granular per-item SAP `RETURN` messages) in `data` alongside the generic error string. Node's own `runGoodsIssueApproval`/`sync-delivery-quantities` explicitly prefer `err.response?.data?.data?.messages` over the generic error message specifically to surface that per-item detail — this port's equivalent `catch` blocks only ever see the flattened `err.Message`, a real (if graceful) loss of diagnostic granularity on every 422 across every department this migration has built so far, not something introduced by this slice. Not fixed here — a `SapServerClient` redesign (an `object? Data` on `SapProxyException`, or a `TryPostAsync` returning the failure envelope structurally) is a cross-cutting architectural decision affecting every previously-shipped phase, out of scope for one Warehouse slice — flagged for a deliberate decision, same treatment as the `userId:0` finding from the previous slice.
- **`GetDeliveryQuantityMatchAsync`'s per-item classification (`exact`/`within-tolerance`/`exceeds-tolerance`, `Epsilon`/`Tolerance` constants) is a direct, faithful port of Node's own math** — including the `pctDiff` special case for `requiredQty == 0` with something picked (treated as maximally over tolerance, `decimal.MaxValue`, rather than dividing by zero) — nothing simplified or changed here, unlike the two BAPI-contract items above.
- **`CompleteGroupAsync`/`SyncDeliveryQuantitiesAsync` both use the established "Helper returns a result, controller maps it to the right HTTP status" pattern** (`CompleteDeliveryGroupResult`/`SyncDeliveryQuantitiesResult`) — `CompleteGroupAsync`'s `BLOCKED` status carries the same structured `Outstanding` per-delivery breakdown Node's own 409 response does; `SyncDeliveryQuantitiesAsync` collapses Node's three distinct failure shapes (two different 409 messages, one 422-with-per-delivery-prefix) into one `(Success, StatusCode, Error)` triple, since — unlike `CompleteGroupAsync`'s `Outstanding` — none of Node's three failure cases actually needs a structured data payload beyond the message itself.
- **`RunZdelflagMaintenanceAsync`'s row-building (`BuildDelflagRows`) is a direct, unabridged port of Node's own pallet/package iteration** — the owner-vs-borrowed-pallet header-row skip, the `PACKID = palletID*1000 + n` numbering, the `delpackWarnings` data-quality checks (a missing packaging instruction, or one with no `ZBOM_INFO` components, silently sends an empty `T_DELPACK` that SAP itself won't complain about) all preserved exactly, per `ZdelflagHelpers.cs`'s own header comment on the SapServer side confirming this exact row shape.
- **No new Helper tests this slice** — every method here (`GetDeliveryQuantityMatchAsync`, `RunZdelflagMaintenanceAsync`, `RunGoodsIssueApprovalAsync`, `CompleteOneDeliveryAsync`) takes a `SqlConnection` directly rather than `INexusOperationsDb`, and `CompleteGroupAsync`/`SyncDeliveryQuantitiesAsync` both open a connection unconditionally as their first statement with no validation ahead of it — matching Node's own `getPool()`-before-anything-else shape exactly, so there's no pure pre-connection path to test, same caveat class as `PalletMainHelper.UpdateAsync` in the previous slice. Real HTTP smoke test confirms both new routes return 401 unauthenticated; the full existing suite (196 tests) still passes unchanged.

### Sub-phase 7c, second slice: the ZDELFLAG/Goods Issue warning-log queues

8 routes (`GET zdelflag/warnings`, `GET :id/zdelflag/status`, `POST :id/zdelflag/reprocess`, `POST :id/zdelflag/resolve`, and the same 4 for `goods-issue`) — port of `routes/deliverymain.js`'s last 8 handlers (lines ~1721–1970), all still on `DeliveryCompletionHelper`/`DeliveryMainController` since they're just a thin read/gate layer directly on top of the same `log.DeliveryZdelflagRun`/`log.DeliveryGoodsIssueRun` tables and the same `RunZdelflagMaintenanceAsync`/`RunGoodsIssueApprovalAsync` methods the first slice already built.

- **Self-caught bug, fixed before this slice's own new code could compound it**: the first slice's `RecordRunAsync` closures (inside `RunZdelflagMaintenanceAsync`/`RunGoodsIssueApprovalAsync`) were serializing `messages` with bare `JsonSerializer.Serialize(messages)` — default PascalCase — into the exact same `messages` NVARCHAR(MAX) column Node's own `JSON.stringify(messages)` writes lowercase `type`/`message` keys into, and that this slice's own new `GetWarningsAsync`/`GetStatusAsync` now read back. Left as-is, every row this app itself wrote would have silently failed to deserialize correctly for Node's still-running `JSON.parse`, and — the more immediate risk — for this slice's own `DeserializeMessages`, since a PascalCase `Type`/`Message` payload doesn't bind onto `SapReturnMessage`'s JSON contract without the same options being used both ways. Fixed by adding a shared `MessagesJsonOptions = new(JsonSerializerDefaults.Web)` field and applying it at both write call sites; this slice's own new `DeserializeMessages` helper uses the same field, so read and write are now guaranteed to agree.
- **The warnings queues (`GetWarningsAsync`) are a latest-row-per-delivery join, not a plain `WHERE status IN (...)` scan** — mirrors Node's own subquery (`INNER JOIN (SELECT deliveryID, MAX(ranAtUtc) ... GROUP BY deliveryID) latest`) exactly: a delivery that failed once but has since succeeded (a newer row with `status='Success'`) must not still show up in the warnings list just because an old `Failed` row exists in its history. ZDELFLAG's queue includes both `Failed` and `Warning` statuses; Goods Issue's includes only `Failed` — there's no `Warning` outcome for Goods Issue in either app, confirmed by re-reading Node's own route (`WHERE r.status = 'Failed'`, no second value).
- **The 4 reprocess/resolve gates are read-then-throw, using the same `NexusConflictException` → 409 pattern already established across every other Warehouse Helper this phase** (`PalletMainHelper`'s reversal-blocked path, etc.) rather than a new result-wrapper type — unlike `CompleteGroupAsync`'s `BLOCKED` status, none of these four needs to carry a structured payload beyond the message, so throwing (letting `ApiExceptionMiddleware` do the mapping) is the simpler and more consistent choice here. Gate conditions ported exactly from Node: ZDELFLAG reprocess 409s only when the latest run is already `'Success'`; Goods Issue reprocess 409s when ZDELFLAG's latest run is anything other than `'Success'` *or* when Goods Issue's own latest run is already `'Success'` (both checked, first failure wins — matches Node's two sequential `if` blocks); both resolve endpoints 409 unless the latest run is in the resolvable state for that queue (`'Failed'`/`'Warning'` for ZDELFLAG, `'Failed'` only for Goods Issue — consistent with the narrower Goods Issue status set above).
- **Resolve inserts a synthetic `'Resolved'` row rather than updating the failed one in place** — matching Node's own `INSERT`-not-`UPDATE` (the run history table is append-only; "resolved" is itself just a new latest-status row, so the same latest-row-per-delivery join used everywhere else picks it up automatically without a schema or query change). Note text falls back to `"Manually marked resolved by {username}."` when the caller sends a blank/omitted note, exactly matching Node's `note || `Manually marked resolved by ${username}.`` — both `ResolveZdelflagAsync`/`ResolveGoodsIssueAsync` take the new `ResolveRunRequest(string? Note)` DTO.
- Both resolve actions are gated `[Authorize(Policy = "Perm:LOG_SUPER")]` (a literal string, matching `WarehouseMasterDataController`'s existing precedent for a permission code with no per-tile Helper constant of its own) rather than `WarehousePicksheetHelper.FnOp` — mirrors Node's `requirePermission('LOG_SUPER')` on exactly these two routes (a stricter gate than the plain `WAREHOUSE_OP` every other route in this controller uses), confirmed by re-reading the Node source directly rather than assuming the same permission as its sibling reprocess/status routes.
- **No new Helper tests this slice either** — same reasoning as the first slice (every method takes a `SqlConnection`/opens one unconditionally as its first statement, no pure pre-connection validation path exists to test in isolation). Real HTTP smoke test (`dotnet run` + `curl`) confirms all 8 new routes return 401 unauthenticated with no 404/500; `dotnet build`/`dotnet test` both clean (196 existing tests still pass, unchanged from the first slice since none of this slice's new code is reachable from a pure/testable entry point).

### Sub-phase 7c, third slice: `RedrumReversalHelper` (`lib/redrumReversal.js`)

Port of the shared `maybeReverseBatchManagedReturn` re-drum reversal — triggered whenever a batch-managed product is transferred back into `SA`/`PTFE` via any transfer-order path. Infrastructure only this slice, not yet wired to a caller: Node's own `lib/redrumReversal.js` is called from both `staging.js`'s deliver route (not yet ported — the next, largest remaining piece of 7c) and `sap.js`'s general Stock Transfer tool (not yet scoped), and this port's `RedrumReversalHelper.MaybeReverseBatchManagedReturnAsync` has the identical signature-shaped role, just with no caller yet. Building it now, ahead of its caller, since it's self-contained (only needs a `SqlConnection`/`ISapServerClient`/`IAuditLogger` plus the batch/destination-bin details any transfer path already has) and unblocks Production's own flagged dependency the moment the first caller lands, rather than bundling three new SAP request/response DTO families into the same slice as an 800+ line route port.

- **New SAP-facing DTOs added to support this**: `FindBackflushDocumentRequest`/`BackflushDocumentRow` (`ProductionSapModels.cs`, mirrors SapServer's `Models/Bapi/ProductionModels.cs` — SapServer returns a plain HTTP 400, not a 200 with an empty row, when no movement-131 document exists for a batch, which is the normal non-redrum case for most transfers) and `CreateTransferOrderRequest`/`CreateTransferOrderResponse` (`WarehouseSapModels.cs`, mirrors SapServer's `Models/Bapi/WarehouseModels.cs` — `POST /api/warehouse/transfer-order`, LT01/LT04). `Mf41Request`/`BdcResponse`/`ProfitCentreRequest` already existed from Production's own reversal/cost-collector work and are reused as-is.
- **The MF41-response classification (`ReverseSapMaterialDocumentAsync`'s `Type=='S'&&MessageClass=='RM'&&MessageNumber=='196'` success / `'E'/'RM'/'210'` already-reversed check) is copied verbatim from `ReversalHelper.ExecuteAsync`/`BulkReverseOneAsync`**, not reimplemented independently — same BAPI, same response shape, already proven correct by that Helper's own tests and real usage; duplicating the constants here would just be a second place for them to drift.
- **A confirmed, source-level bug in Node's own `lib/redrumReversal.js` was found and deliberately NOT ported bug-compatible, unlike the two live-curl-confirmed items in the first slice.** Node's `submitDrumming` (`routes/productionnexus.js`, and this port's own already-shipped `DrummingHelper.SubmitAsync`) increments `log.AgreementSnapshot.DockStockAllocated` keyed on `WHERE OriginalDoc = @ref AND OriginalDocItem = @item`, with a comment explaining exactly why: the raw `ReferenceDocument`/`Item` columns "may already have flipped to a delivery number for this line by the time the drum is posted." `lib/redrumReversal.js`'s own decrement, immediately adjacent in spirit, ignores that same reasoning and keys on `ReferenceDocument`/`Item` instead — the two ends of the same increment/decrement pair use different, incompatible column pairings, so the decrement can silently fail to find the row the increment actually touched. This is confirmed by directly comparing the two pieces of source (the increment's own comment states the reasoning the decrement contradicts), not by a live SAP/curl test — a different confidence class than the Goods Issue `Items` fix, but still strong enough that porting the mismatch forward here would mean deliberately writing a bug this port can see and explain. Fixed in this port (`OriginalDoc`/`OriginalDocItem`, matching the increment); Node's `lib/redrumReversal.js` itself was left unchanged — low real-world risk (affects only a local dock-stock display figure that self-heals on the next 30-min sync, and a miss here only ever produces a warning, never blocks the reversal) — flagged here for a deliberate decision on whether to backport the same one-line fix to Node, same treatment as the unresolved `SapServerClient` findings from the first slice.
- **Every failure path degrades to a `Warning` string rather than aborting, matching Node's own defensive design exactly**: a failed cost-collector lookup, a failed WM tidy-up transfer order, a failed `DockStockAllocated` correction, and a failed braid-component reversal all leave the *primary* MF41 reversal's success standing and get appended to one running `Warning` message instead (`AppendWarning`, mirroring Node's own `warning = (warning ? warning + ' ' : '') + ...` string-building) — only the primary MF41 reversal itself can produce `Status: "failed"`.
- **`PadCostCollectorBin` is the one pure, directly-testable piece** (`RedrumReversalHelperTests`, 5 theory cases covering under-length zero-padding, exact-length, over-length truncation to the last 10 characters, and an empty string) — everything else needs a live `SqlConnection`/`ISapServerClient`, same testability ceiling as the rest of this Sub-phase's Helpers.
- **No controller route, no smoke test this slice** — there is genuinely nothing to route to yet; `dotnet build`/`dotnet test` both clean (201 tests, 5 new).

### Sub-phase 7d: Staging Post (`routes/staging.js` + `routes/stagingsql.js`)

A full, previously-unscoped standalone tile — the background research agent that originally scoped Sub-phase 7c as "the deliver route + redrum reversal" undersold this: `staging.js` is actually 865 lines / 19 routes covering material requisitions end-to-end (search, request CRUD, stock lookups, SAP-posting delivery, KPIs, bin-restriction admin), not just one route. Given the size, it's its own sub-phase rather than folded into 7c. `RedrumReversalHelper` (previous slice) gets its first real caller here — `DeliverAsync` wires it in exactly where Node's own deliver route does.

- **Mounted with no department/permission gate at all on its main actions** — confirmed via `server.js`'s `app.use('/api/staging', requireLogin, stagingRoutes)` (no `requireDepartment` the way most department mounts have) and re-confirmed by re-reading every route handler in `staging.js` itself (only the four `bin-restrictions` writes call `requirePermission('LOG_SUPER')`, everything else has zero permission gate). This is deliberate, not an oversight in Node — Staging Post requests are raised BY Production floor staff FOR Stores, so it can't be gated to just one department. `StagingController` mirrors this exactly: plain `[Authorize]` (inherited from `NexusControllerBase`) on every action except the four bin-restriction writes (`Perm:LOG_SUPER`).
- **Three new cross-cutting/shared dependencies this slice needed and built minimally, each scoped to exactly what Staging Post needs — not the full tile/feature Node has**:
  - **`INotificationService`/`NotificationService`** (`Services/Notifications/`, new — this migration's first port of `lib/notify.js`, a genuinely cross-cutting service several future phases will also need). One deliberate, considered improvement over Node: Node's own fan-out for `target: {type: 'permission'}` only queries `dbo.PortalUserPermissions` (the flat direct-grant table) — this app additionally has the permission-GROUP mechanism (`PortalUserPermissionGroups`/`PortalPermissionGroupPermissions`, see `PermissionResolver`'s own effective-permission union) that Node has no equivalent of at all. Porting Node's flat-table-only fan-out query here would silently under-notify anyone who holds the target permission only via group membership — a real gap against *this app's own* already-built permission model, not a behavior change relative to Node (which has nothing to be inconsistent with, since groups don't exist there). Every other target type (`user`/`department`/`role`/`all`) is an exact, unmodified port.
  - **`StockCountGuardHelper.AssertTransfersAllowedAsync`** (`Helpers/Warehouse/`, port of `lib/stockCountGuard.js`) — blocks transfer-order creation for a storage location with an active stock count. Throws a plain `TransferBlockedException` (deliberately NOT a `NexusApiException` — see its own doc comment) rather than baking in one HTTP status, mirroring Node's own bare `TransferBlockedError extends Error` exactly: Node's real behavior folds this into whichever try/catch happens to wrap the call, which for Staging Post's deliver route is the same generic 422 "SAP rejected the transfer order: ..." bucket a real SAP rejection also produces (confirmed by reading the route's actual try/catch scope, not assumed) — so `StagingHelper.CreateSapTransferOrderAsync` catches it and rethrows as a plain `InvalidOperationException`, letting `DeliverAsync`'s existing SAP-call catch block produce the identical message shape. No 409 anywhere in this path, even though a "blocked by an active count" condition might look 409-shaped in isolation — matching Node's actual code structure was prioritized over what might look like the "more correct" status code in isolation.
  - **`MaterialRequestUnitsHelper`** (`Helpers/Warehouse/`, minimal — `GetConversionQtyAsync`/`GetByMaterialAsync` only) — Node's `routes/materialRequestUnits.js` is a full standalone admin tile (add/edit/delete/CSV-bulk-upload, all `LOG_ADMIN`-gated) that Node's own header comment says is "maintained via the Material Request Units admin tile in **Logistics**," not Warehouse. Only the two read-only lookups Staging Post's create-request flow actually calls were ported here; the full CRUD admin surface is deferred to Phase 8d (Logistics reference data) — same "port only what the current slice needs, defer the rest to the phase that actually owns it" precedent `RedrumReversalHelper` already set by reusing Production's existing `Mf41Request` instead of redefining it.
- **`StoresWorkingHoursHelper`** (`Helpers/Warehouse/`, new, fully pure) — port of `isStoresWorkingDay`/`atLocalTime`/`nextStoresOpen`/`clampToStoresWindow`/`previousStoresClose`/`snapToStoresWindow`/`addStoresLeadTime`/`formatStoresTime`/`formatLocalDateTime`. Stores work 05:45–17:00 Monday–Friday; a request's 4-hour minimum lead time is counted in *working* time, rolling weekends/after-hours forward to the next open. **Flagged, not changed**: every method operates on plain local wall-clock `DateTime` arithmetic (`DayOfWeek`/`Hour`/`Minute`), exactly mirroring Node's own `getDay()`/`setHours()` (never `getUTCDay()`/`setUTCHours()`) even though the values flowing through this (`dueAtUtc`, etc.) are named and stored as if UTC elsewhere in the same feature (`GETUTCDATE()`). This looks like a naming mismatch but may be intentional (the server and the physical Stores building share a timezone, so "local server time" and "the real Stores opening hours" are meant to coincide) — ported faithfully rather than silently switched to real UTC arithmetic, since that would change which wall-clock hour a real request's deadline lands on for real users, and this was never confirmed as a defect the way the Goods Issue `Items` gap was. Fully unit-tested (`StoresWorkingHoursHelperTests`, 11 tests against a known real week starting Monday 2024-01-01) — the richest pure-logic surface in this whole Warehouse phase (14 test cases, `StoresWorkingHoursHelperTests`) — a rare case in this Sub-phase where the business logic itself, not just a parsing/padding helper, is fully test-covered.
- **`DeliverAsync` reproduces Node's two genuinely distinct SAP-failure shapes exactly, not collapsed into one** — a guard-blocked or HTTP-level-failed SAP call throws `NexusUnprocessableEntityException` (plain message, no `data`); a protocol-successful-but-semantically-rejected call (`transferOrder.success === false` — SAP's own RETURN messages said no) returns a `Status: "REJECTED"` result instead, carrying the messages list, so `StagingController.DeliverRequest` can build the exact `{success:false, error, data:{messages}}` shape Node's own `!transferOrder.success` branch produces. This mirrors the same "Helper returns a result for outcomes needing structured data, throws for the ones that don't" split `DeliveryCompletionHelper`'s `CompleteGroupAsync`/`SyncDeliveryQuantitiesAsync` already established.
- **Consignment MB1B (`isConsignment` — `SpecialStockIndicator == "K"` and destination `SA`) intentionally does NOT go through `StockCountGuardHelper`, matching a real, confirmed asymmetry in Node's own two SAP-caller wrapper functions** — `createSapTransferOrder` calls `assertTransfersAllowed` first, `createSapConsignmentMb1b` does not, confirmed by reading both functions directly. Ported faithfully as an asymmetry, not "fixed" to apply the guard uniformly, since there was no live/source confirmation this is actually a bug rather than a deliberate distinction (e.g. consignment issue-from-vendor-stock may not conflict with a physical count the same way an ordinary bin-to-bin transfer does).
- **`GET /kpi/export` (an ExcelJS-generated 3-worksheet `.xlsx`) was NOT ported** — matching Finance's own established precedent (this same file's Phase 5 section) of deliberately deferring server-side spreadsheet export rather than half-building a ClosedXML replacement under time pressure with no real convention decided yet for this app. `GET /kpi` (the underlying data both the on-screen KPI panel and the export would consume) is ported in full — only the xlsx-formatting/download mechanics are deferred.
- **`ConsignmentMb1bRequest`/`ConsignmentMb1bResponse` and `SapStockQuery`/`SapStockRow` added to `WarehouseSapModels.cs`**, mirroring SapServer's `Models/Bapi/WarehouseModels.cs` field-for-field. `SapStockQuery` is the first SAP GET call in this port sent as a real query string (`QueryHelpers.AddQueryString`) rather than a JSON body — `GET /api/warehouse/stock` model-binds `[FromUri]`, unlike `find-cost-collector`/`check-profit-centre`'s established `[HttpGet]`+`[FromBody]` pattern used everywhere else so far.
- **`RedrumReversalHelper.Result` stayed `internal`, not made `public`, to keep every Helper's internal-static convention intact** — `DeliverStagingRequestResult` (a public DTO, like every DTO in this codebase) needed a same-shaped public `RedrumReversalResult` record in `StagingModels.cs` instead, with `StagingHelper.DeliverAsync` mapping one onto the other. The alternative (making `RedrumReversalHelper` itself `public static`) would have been a one-line fix but broken the "every Helper is `internal static`, reachable only via `InternalsVisibleTo`" convention every other Helper in this codebase follows — the extra mapping step was judged worth it to keep that convention uniform rather than carve out one exception.
- Real HTTP smoke test (`dotnet run` + `curl`): all 18 new `api/staging/*` routes return 401 unauthenticated with no 404/500, confirming routing/DI wiring; `dotnet build`/`dotnet test` clean (215 tests, 14 new — all `StoresWorkingHoursHelperTests`, since every other new method needs a live `SqlConnection`/`ISapServerClient`/`INotificationService`, the same testability ceiling every other slice in this Sub-phase has hit).

### Flagged, not fixed: the permission-code migration step was skipped for Production AND has not been done for Warehouse either

Every Warehouse controller across 7a-7d reuses Node's own existing coarse codes directly as literal strings (`WarehousePicksheetHelper.FnOp = "WAREHOUSE_OP"`, `"Perm:LOG_SUPER"`, `"Perm:LOG_ADMIN"`) rather than going through the new `PortalPermissionGroups` schema the migration plan's "Authorization model" section calls for — no `SeedWarehousePermissions`-style EF migration exists, no default group bundles `WAREHOUSE_OP` (or the Warehouse-relevant slice of `LOG_SUPER`), and no existing direct-grant holder has been migrated into one.

**Checked whether this is a new gap specific to this slice — it isn't.** `Data/Migrations/` has exactly four department permission-seed migrations (`SeedEngineeringPermissions`, `SeedQualityPermissions`, `SeedSalesPermissions`, `SeedFinancePermissions`) — Production, the single largest already-shipped phase (6a/6b/6c, spanning `PROD_SUPERVISOR`/`QUAL_CONCESSION` across dozens of routes), has **no** `SeedProductionPermissions` migration at all; only `SeedSalesPermissions` touches `PROD_SUPERVISOR`, and only for the one narrow cross-department action it shares with Sales (`PROD_SCHEDULE_EDIT`). So this step has already been silently skipped for the biggest phase shipped so far, not just overlooked here.

**Deliberately not fixed in this slice** — a partial fix (Warehouse only, Production still skipped) would leave the codebase in a worse, more inconsistent state than either "nobody's migrated" or "everybody's migrated," and Node's own `WAREHOUSE_OP`/`LOG_SUPER`/`LOG_ADMIN` boundaries are already reasonably tile-cluster-coherent (unlike `PROD_SUPERVISOR`, which genuinely sprawls across unrelated Production features) — so even the *shape* of a correct fix here (reuse the existing codes as-is vs. split further) is a real design decision, not just a mechanical migration to write. Flagged here for a deliberate, single pass covering both Production and Warehouse together (natural fit for Phase 10's cross-cutting closeout, which the original plan already earmarks for exactly this kind of catch-up work) rather than three more one-off partial migrations as each further department ships.

## Phase 8: Logistics

The largest department (13,904 JS lines, 32 tiles per the original migration plan). Split into 4 sub-phases per the plan: 8a Shipping (`shipmentmain.js` 3,428 LOC/41 routes + `shipmentcost.js` 1,250/15), 8b Purchasing/Performance (`performance.js` 4,745/75 + `performancesql.js` 2,928, plus `mrpanalysis.js` 412/10 and `inboundcosts.js` 243/4), 8c Customs (`customsreport.js` 614/1 + `customsreportadmin.js` 229 + `freightbooking.js` 558/2 + `clearportexport.js` 111/1 — Kuehne+Nagel OAuth, ClearPort bearer-token, real external-API contract verification needed), 8d Reference data (started first, per the plan's own "fastest, uniform CRUD — could go first to build momentum" suggestion). The Vendor Consignment Tracker (`consignment.js` 520 LOC/15 real routes + `consignmentsql.js` 942) is a confirmed Logistics tile (not Warehouse — corrected during Warehouse's own Phase 7 research), assigned as Sub-phase 8e (see its own section below) rather than folded into 8a-8d; `deliverylink.js` is confirmed dead code (Warehouse Phase 7 research), not ported anywhere.

### Sub-phase 8d: Reference data

Port of routes/costtypes.js, costelements.js, costcenters.js, forwarders.js, forwarderapproval.js, forwardermodemapping.js, materialRequestUnits.js (finishing the full CRUD — Warehouse Sub-phase 7d had already built a minimal read-only slice, `GetByMaterialAsync`/`GetConversionQtyAsync`, as Staging Post's own in-process dependency, before this phase had started), incoterms.js, rateskn.js, ratestpn.js, assignmenttpn.js, deliveryroutes.js — 12 small, largely uniform CRUD tiles against simple `log.*` reference tables, one `LogisticsReferenceController`/`LogisticsReferenceHelper` for all of them (53 routes total), matching `WarehouseMasterDataController`'s established "`[Route(\"api\")]`, per-action full path, no blanket department gate" pattern.

- **`MaterialRequestUnitsHelper` relocated from `Helpers/Warehouse/` to `Helpers/Logistics/`** now that this phase actually owns it — Staging Post's `StagingHelper.cs` picked up a `using NormantonNexus.Helpers.Logistics;` instead. `MaterialRequestUnitRow` itself stayed in `StagingModels.cs` (built there first) rather than being shuffled into a new file purely for tidiness — reused across the namespace as-is, matching how `SapReturnMessage` and other cross-department DTOs already work in this codebase.
- **Every response standardized on this app's own `ApiResponse<T>` envelope — several of Node's own reference-data files do NOT use it.** Confirmed by reading every one of the 12 files directly: `costtypes.js`/`incoterms.js`/`forwarderapproval.js`/`rateskn.js`/`ratestpn.js`/`assignmenttpn.js` return a bare `res.json(result.recordset)` array (or `{error: msg}` on failure) instead of `{success,data,error}`; the rest are already on the standard shape but inconsistently (`forwarders.js`'s error field is sometimes a plain string, `costelements.js`'s is `{message}`). This port doesn't perpetuate any of that per-file inconsistency — every action wraps its result in `ApiResponse<T>`, matching every other department already built.
- **Permission gates ported exactly per resource, preserving a real, confirmed asymmetry in Node itself rather than normalizing it** — `CostTypes`/`ForwarderApproval`/`Incoterms`/`RatesKN`/`RatesTPN`/`AssignmentTPN` have literally NO gate on writes in Node (any logged-in user can create a row); `CostElements`/`CostCenters`/`Forwarders`/`DeliveryRoutes`/`MaterialRequestUnits` gate writes with `LOG_ADMIN`; `ForwarderModeMapping` is the only one gated `LOG_ADMIN` even on its GET actions. Confirmed by reading every route handler's `requirePermission(...)` call directly rather than assuming a uniform "reference data = admin-only" rule — several of these tables genuinely aren't treated as sensitive in Node today.
- **`ForwarderRow`'s update needs the row's CURRENT `ForwarderMode` (`OriginalMode`) to identify which row to touch — `forwarderID` alone is not unique.** A vendor with several shipping modes (Road/Sea/Air) gets one row per mode, all sharing the same `forwarderID`/`forwarderName` (the duplication Node's own `dedupeForwardersByName` in `private/js/logistics.js` works around in the dropdown UI) — `UpdateForwarderAsync`'s `WHERE forwarderID = @forwarderId AND (forwarderMode = @originalMode OR (forwarderMode IS NULL AND @originalMode IS NULL))` mirrors Node's own guard exactly, returning `NexusNotFoundException` ("Row not found — it may have been changed by someone else...") on a zero-row update, same as a concurrent edit landing first in Node.
- **`LookupModeOfTransportAsync`/`LookupTransitDaysAsync` are the two in-process (non-HTTP) exports this phase's Helper needs to expose for later sub-phases** — Node's `forwardermodemapping.js` exports `lookupModeOfTransport()` directly for `shipmentmain.js`'s mark-booked flow to call, and `deliveryroutes.js`'s `/lookup` logic will be reused the same way once Shipping (8a) lands. Both already built and public/internal-callable now, ahead of their real callers, same "build the shared piece the moment the phase that needs it is scoped, even before its caller ships" precedent `RedrumReversalHelper` set in Warehouse.
- **A confirmed, but NOT live-tested, data-integrity question flagged rather than silently resolved either way**: `log.CostElements`/`log.CostCenters`'s `elementID`/`centerID` columns are captured in the EF migration DDL as plain nullable `BIGINT` — no `IDENTITY` property — yet Node's own INSERT omits the column and reads back `OUTPUT INSERTED.elementID`/`centerID` expecting the database to have generated a value, with a comment explicitly saying "left to the database." If the live table genuinely has no `IDENTITY` (schema drift between what's captured in migration tooling and the real production table is a known, repeatedly-confirmed pattern in this codebase — see SapServer's own equivalent discoveries), every new row would get a `NULL` id back from both Node and this port alike. Ported faithfully (the same `OUTPUT INSERTED.x` pattern) rather than guessing at a fix (e.g. adding real `IDENTITY`) that would require a schema change outside this migration's own "schema stays as-is" principle — flagged for verification against the real live table before this tile is trusted for real cost-element/cost-centre creation.
- **No new Helper tests this slice** — every method is a direct 1-3 line Dapper call against `log.*` with no non-trivial pure logic ahead of the query (unlike `RatesKN`'s lookup, whose prefix/rounding math is simple enough — 2 lines — that extracting it into a separately-tested pure function was judged not worth the ceremony, unlike `StoresWorkingHoursHelper`'s genuinely complex date arithmetic in the previous phase). Real HTTP smoke test (`dotnet run` + `curl`) confirms all 53 new `api/*` routes return 401 unauthenticated with no 404/500; `dotnet build`/`dotnet test` both clean (215 tests, unchanged).

### Sub-phase 8a.1: Shipping lifecycle core

`shipmentmain.js` (3,428 LOC/41 routes) turned out far bigger and riskier than the plan's own top-line estimate — it bundles three genuinely unrelated hard problems (SAP-adjacent cost posting, a hand-rolled raw-socket SMTP client, the ClearPort customs API) inside one route file alongside ordinary CRUD. A dedicated research pass (full read of both `shipmentmain.js` and `shipmentcost.js`, 4,678 LOC combined) confirmed a 5-slice breakdown is needed rather than the 3-4 sub-phases Production/Warehouse each used: **8a.1 lifecycle core** (this slice — queues, mark collected/delivered/booked/unbooked, cancel, create, delivery membership, corrections, search, events), **8a.2** manual cargo lines + document-folder filesystem management, **8a.3** PDF generation (packing list/loading list/packaging declaration — rebuild in QuestPDF, don't port the hand-rolled raw-PDF-object-model code literally), **8a.4** the hand-rolled SMTP collection email (highest "novel infrastructure" risk — no existing C# SMTP client anywhere in this codebase, though `LabelPrintHelper.cs`'s `TcpClient`-based raw-socket precedent from Production's label printing is the right idiom to follow), **8a.5** ClearPort customs + SAP cost/MIGO posting (highest business risk — real SAP purchase-order/goods-receipt creation under the calling user's own credentials, plus a brand-new external HTTP integration). See the research findings inline in this section for exactly what's confirmed dead code, what's bidirectional, and what each later slice needs.

- **Confirmed dead code, excluded from the port**: `GET /` (bare list-all), `GET /id/:shipmentId`, `GET /forwarder/:forwarderId`, `GET /destination/:destinationId`, `GET /daterange`, `POST /` (bare generic create) — all 6 share the old undifferentiated `res.json(result.recordset)`/`{error: msg}` response shape (not the `{success,data,error}` envelope every other route in the file uses), the same stylistic tell that flagged `routes/deliverylink.js` as dead during Warehouse's own research. Verified via grep across every `private/js/*.js` file, not assumed from the shape alone.
- **`/search` is genuinely bidirectional and was ported outbound-only, with the inbound leg deliberately deferred, not guessed at.** Node's real route UNIONs `log.ShipmentMain` (outbound) with `log.PurchaseOrderShipment`/`PurchaseOrderSuggestion`/`Vendor` (inbound) — none of which are ported yet; that table belongs to the not-yet-scoped Purchasing/Performance sub-phase (8b). `ShipmentHelper.SearchAsync`'s own doc comment flags this explicitly. `deliveryNumber` (an outbound-only search field even in Node — the inbound leg forces `1=0` for it) needed no special-casing here as a result.
- **`mark-booked`'s ShipmentCost-row insertion is the first real caller of `LogisticsReferenceHelper.LookupModeOfTransportAsync`** (built ahead of its caller in Sub-phase 8d, per that slice's own anticipation) — translates the booked forwarder's own `forwarderMode` into the canonical `ModeOfTransport` value, best-effort (never throws, falls back to the raw value). Cost-row inserts happen in a second pass *after* the booking transaction commits, each wrapped in its own swallowed try/catch, matching Node's structure exactly — a failed cost-row insert must never undo a booking that already committed.
- **`unbook` fixes a real double-cost bug Node itself already fixed and documents inline** (not something this port found independently) — deletes any not-yet-`migoStatus`'d `ShipmentCost` rows for the shipment as part of unbooking, so re-booking doesn't insert a second freight-cost row on top of one nothing ever cleared. `migoStatus=1` rows (already processed in SAP) are left untouched.
- **`SyncShipmentAggregateDataAsync` (the delivery add/remove weight/pallet/volume resync) intentionally returns `void`, not Node's full `getShipmentContext`-shaped result** — every current 8a.1 caller (delivery add/remove) discards Node's return value too, and building the full pallets/manualCargo-joined context here would mean pulling PDF-generation-slice (8a.3) territory into this one. Extend the return type if/when a real caller in a later slice needs it, rather than building it speculatively now.
- **`GetShipmentByIdAsync` binds `SELECT *` against a hand-written property-per-column class (`ShipmentRawRow`) rather than a positional record** — `log.ShipmentMain`'s 32 columns mix `camelCase` (originID, plannedCollection) and `PascalCase` (PlannedDelivery, ActualDelivery, DeliveryStatus) inconsistently in the real schema (confirmed against the EF migration DDL directly), which a Dapper-bound positional record can't cleanly represent without either fighting the naming or losing clarity about which casing each real column actually uses — a plain mutable class with property names copied verbatim from the DDL was judged clearer here than fighting that with column aliases in every query.
- **`getLogisticsSettings()`'s origin-address/export-root fields got a new `LogisticsOptions`/`appsettings.json` `Logistics` section** (`Services/LogisticsOptions.cs`), following `LabelPrinterOptions`/`SapServerOptions`'s established `IOptions<T>` pattern exactly — defaults match Node's own hardcoded fallbacks (the real Kongsberg Actuation System Ltd registered address). Deliberately scoped to only what 8a.1 needs; SMTP/ClearPort settings from the same Node function are added to this same class when 8a.4/8a.5 land, not spoken for now.
- **New shared, cross-department service: `IDataChangeLogService`/`DataChangeLogService`** (`Services/DataChangeLogService.cs`) — port of config.js's `stampDbChange`, a best-effort (fire-and-forget, swallows its own errors) attribution correction for `dbo.DataChangeLog` rows a database trigger writes with no notion of which application user made the change. Generic and reusable — not Shipping-specific — the first port of this utility in the C# app; future departments needing the same attribution fix should reuse it rather than reimplementing.
- **`GetShipmentFolderInfo`/`FormatShipmentRef`/`SanitizeFolderSegment`/`AssertValidExportRoot` (all pure, no filesystem I/O) were pulled into this slice even though they're nominally 8a.2/8a.3 territory** — `create-from-deliveries`'s response needs a real `folderPath` value, and these four functions are pure path-string construction with zero coupling to the actual `mkdirRecursiveSafe`/`ensureShipmentFolder` disk-touching code (left for 8a.2). `AssertValidExportRoot`'s misconfiguration guard (a real, documented production incident in Node — a stray machine environment variable shadowing the real export-root config) is preserved exactly, mapped to `NexusBadGatewayException` (502) since nothing existing in this app's exception hierarchy carries a custom message on a 500.
- Fully unit-tested where pure (`ShipmentHelperTests`, 23 new test cases: `FormatShipmentRef`, `IsExWorks`, `SanitizeFolderSegment`, `AssertValidExportRoot`, `GetShipmentFolderInfo`) — everything else needs a live `SqlConnection`, the same testability ceiling as every other slice this phase. Real HTTP smoke test (`dotnet run` + `curl`) confirms all 19 new `api/shipmentmain/*` routes return 401 unauthenticated with no 404/500; `dotnet build`/`dotnet test` both clean (238 tests, 23 new).

### Sub-phase 8a.2: manual cargo lines + create-folder

Port of `manual-cargo` GET/POST/PATCH/DELETE and `:shipmentId/create-folder`, added onto the same `ShipmentMainController`/new `ShipmentManualCargoHelper`.

- **Re-scoped from the original research pass's proposed boundary**: `documents/folder`/`documents/:fileName`/`documents/upload` were originally grouped into this slice, but re-reading `documents/folder` directly showed it calls Node's `generateShipmentDocuments` to regenerate the packing-list PDF *before* listing the folder, so the listing is always current — that's PDF-generation-slice (8a.3) territory, not filesystem-only work. Moved all three document routes to 8a.3 instead; this slice is manual-cargo + create-folder only.
- **`UpdateManualCargoItemRequest` collapses "field omitted" and "field explicitly null" into one behavior ("leave unchanged")** — the same deliberate simplification Warehouse's `UpdatePalletRequest` already established and documented (a plain nullable C# property can't distinguish the two the way Node's `req.body.x !== undefined` check can, without a wrapper type this codebase doesn't otherwise use). The one real capability this loses, flagged rather than silently dropped: Node lets an operator explicitly clear `Length`/`Width`/`Height` back to blank on an edit (`req.body.length === '' ? null : ...`); this port can't represent "clear it" separately from "leave it alone." Low-stakes — a mis-entered dimension can still be corrected to a different value, just not blanked out — but a real, intentional reduction, not an oversight.
- **`create-folder`'s `mkdirRecursiveSafe` (Node's manual per-directory-level mkdir loop) was NOT ported** — Node built it specifically to avoid a documented libuv bug in `fs.mkdir(path, {recursive:true})` on Windows (a mis-resolved long-path prefix past the drive root). .NET's `Directory.CreateDirectory` is a distinct BCL implementation with no known equivalent issue, so this port calls it directly. The underlying *intent* (defensive folder creation) is preserved; the specific workaround code isn't, because the bug it worked around is Node/libuv-specific and doesn't apply here — this is a case where "port the defensiveness, don't drop it" (the standing rule from this same file's Warehouse-adjacent notes on `assertValidExportRoot`) means porting the *goal*, not blindly copying platform-specific workaround code that has no target to work around.
- **No new Helper tests this slice** — `ComputeVolume`'s L×W×H/1,000,000 math is pure but, like `RatesKN`'s lookup rounding in Sub-phase 8d, judged too trivial (one line) to warrant extracting into a separately-tested method. Real HTTP smoke test confirms all 5 new routes return 401 unauthenticated; `dotnet build`/`dotnet test` both clean (238 tests, unchanged).

### Sub-phase 8a.3: PDF generation

Port of `generate-packing-list`, `generate-packaging-declaration`, `documents/folder`, `documents/:fileName`, `documents/upload` — the three document routes re-scoped out of 8a.2 above, plus the two PDF-generation routes. New Helper `ShipmentDocumentHelper` (route orchestration: folder resolution, file naming, filesystem I/O) plus two new pure PDF-builder Helpers, `ShipmentPackingListPdfHelper` and `ShipmentPackagingDeclarationPdfHelper`.

- **Both PDFs rebuilt in QuestPDF's declarative layout, not ported from Node's raw pdfkit/hand-rolled PDF-object-model code** — same "deliberate non-pixel-perfect, QuestPDF-idiomatic re-layout" precedent as `LabelPdfHelper.cs` (Production, Phase 6). `ShipmentPackagingDeclarationPdfHelper`'s checkbox rendering is simplified further still: a solid-fill square instead of Node's drawn tick-mark glyph inside the box — the filled/unfilled state alone (not the glyph shape) is what actually carries the meaning on the printed page.
- **Node's `createSimplePdfBuffer` and `buildShipmentPackingListLines` (two whole functions in `shipmentmain.js`) confirmed genuinely dead code and excluded** — declared but never called anywhere in that file or the rest of the repo, verified by direct grep before excluding (same discipline as every other dead-code exclusion this phase).
- **`ShipmentHelper.SyncShipmentAggregateDataAsync`'s return type changed from `Task` to `Task<ShipmentContext>`** (was deliberately left `void` in 8a.1, with a note to extend it once a real caller needed the full context) — `GeneratePackingListAsync`/`GetDocumentFolderAsync` are that caller: both need to regenerate the packing-list PDF from a just-resynced, fully-joined shipment context (deliveries+pallets or manual cargo, depending on `IsManual`) in one round trip, not a resync followed by a second separate fetch. `GetShipmentContextAsync` itself (the full port of Node's `getShipmentContext`) was added as a new standalone method that `SyncShipmentAggregateDataAsync` now calls internally and returns.
- **`documents/folder` stays coupled to packing-list regeneration, confirming 8a.2's own re-scoping note** — it calls `SyncShipmentAggregateDataAsync` (which now regenerates+returns the context) before listing the folder, so the listing an operator sees is always backed by a current PDF, not a stale one from before the last delivery/cargo edit.
- **`documents/:fileName` resolves a path only — the controller does the actual file streaming**, via ASP.NET Core's built-in `PhysicalFile(path, "application/pdf", downloadName)`, no bespoke file-serving code needed. Path traversal via the `fileName` route value is neutralised with `Path.GetFileName()` before it's ever combined with the shipment's own folder path, and the resolved path must both end in `.pdf` and actually exist (404 otherwise) — mirrors Node's own guard.
- **`documents/upload` has no established raw-body-binding precedent anywhere in this codebase (Node uses `express.raw({type:'application/pdf', limit:'20mb'})`)** — ported by reading `Request.Body` directly into a `MemoryStream` in the controller action (no `[FromBody]` binder involved) and handing the raw bytes to the Helper, which enforces the same 20MB cap and empty-body rejection Node's own validation does. The original filename (for the invoice's own `-invoice-<name>-<timestamp>.pdf` naming) is passed as a query parameter rather than parsed out of a `Content-Disposition` header, since Node's own route reads it from `req.query.fileName`, not a multipart upload — confirmed by reading the route directly rather than assuming a multipart shape.
- **`GetUserDisplayNameAsync` (the packaging declaration's e-signature name) is a second, Nexus-database connection inside a Helper whose other methods all use NexusOperations** — `PortalUsers` lives in the Nexus (portal/auth) database, not NexusOperations, so `GeneratePackagingDeclarationAsync` takes both `INexusOperationsDb` and `INexusDb` explicitly. Deliberately best-effort (swallows its own lookup failure, falling back to the bare username claim) — a lookup glitch must never block generating a legally-relevant signed declaration.
- Fully unit-tested where pure (`ShipmentPackingListPdfHelperTests` 4 cases, `ShipmentPackagingDeclarationPdfHelperTests` 3 cases — both check real, verifiable PDF structure: the `%PDF` magic header and a sane byte length, same precedent as `LabelPdfHelperTests`, since no PDF viewer exists in this sandbox to check pixel fidelity). `ShipmentDocumentHelper` itself needs a live `SqlConnection`+filesystem, the same testability ceiling as every other route-orchestration Helper this phase. Real HTTP smoke test (`dotnet run` + `curl`) confirms all 5 new routes return 401 unauthenticated with no 404/500; `dotnet build`/`dotnet test` both clean (245 tests, 7 new).
- **`POST /loading-list` (multi-shipment loading list) was missed by the original research pass's route inventory and by this slice's own first commit — caught while re-checking `shipmentmain.js`'s full route list against the controller afterward, not by any test.** It's a pure PDF download (no on-disk shipment folder involved, unlike the other two PDF routes), reusing the `BuildLoadingListPdf` builder already written and tested in the first 8a.3 commit — only the missing route wiring and a new `ShipmentHelper.GetShipmentsForLoadingListAsync` (silently skipping any requested shipment ID that's missing or cancelled, matching Node's own `continue`) needed adding. Folded into this same sub-phase rather than opened as a separate slice, since it shares 100% of its PDF-building code with what's already here. **Also found during that same re-check and deliberately deferred, not built**: `GET /otif-report` — it UNIONs `log.ShipmentMain` (outbound) with `log.PurchaseOrderShipment`/`Destinations` (inbound), the exact same not-yet-ported inbound tables `ShipmentHelper.SearchAsync` already deferred to Sub-phase 8b for in 8a.1 — same reasoning applies, so this report is deferred to 8b alongside `/search`'s own inbound leg rather than guessing at a real-vs-fake outbound-only substitute. `dotnet build`/`dotnet test` both clean after the loading-list addition (245 tests, unchanged — no new pure logic worth a dedicated test beyond what `BuildLoadingListPdf`'s existing tests already cover); real HTTP smoke test confirms the new route returns 401 unauthenticated.

### Sub-phase 8a.4: collection email

Port of `POST /:shipmentId/send-collection-email` — the one remaining Ex-Works-only tile that emails the customer their packing-list PDF as an SMTP attachment once ready for collection. New `Helpers/Logistics/SmtpMailer.cs` (the raw-socket SMTP protocol client) and `Helpers/Logistics/ShipmentCollectionEmailHelper.cs` (MIME message building + route orchestration), plus SMTP settings added onto the existing `LogisticsOptions`.

- **`SmtpMailer` is a hand-rolled raw-socket SMTP client, same idiom as `LabelPrintHelper.cs`'s raw-TCP printer send from Production (Phase 6)** — no drop-in NuGet SMTP client was substituted; `System.Net.Mail.SmtpClient` is long obsolete and .NET has no first-party replacement, and this port's job is a faithful behavioral port of Node's own hand-rolled `net`/`tls` client, not a library swap. Same command sequence as Node: read the 220 greeting, `EHLO` with a `HELO` fallback (capturing the EHLO failure to decide later whether it's fatal), optional `AUTH LOGIN` when `SmtpUser` is set, otherwise treat a captured EHLO failure as fatal only when `SmtpSecure` is also true (mirrors Node's exact "an EHLO rejection during secure relay setup is suspicious enough to abort on, but an EHLO rejection during a plain unauthenticated relay isn't" reasoning), `MAIL FROM`/`RCPT TO` per recipient/`DATA`/message body/`QUIT`. TLS (`SmtpSecure`) wraps the socket in an `SslStream` with `SmtpAllowInvalidCert` (default `true`, matching Node's own permissive default — this is expected to talk to an internal/self-signed relay, not a public mail provider) controlling certificate validation, the direct analog of Node's `tls.connect({rejectUnauthorized: !smtpAllowInvalidCert})`.
- **Multi-line SMTP response handling ported exactly, not simplified away**: a reply line is only treated as final once it matches `\d\d\d ` (three digits then a *space*, not a dash) — a continuation line (`250-PIPELINING`) keeps accumulating. Both Node's and this port's response reader would silently hang/misparse on a server that never sends the final space-delimited line, but that's the real, if narrow, protocol assumption Node's own implementation already made — not a new gap introduced here.
- **Error-code mapping preserved, using this app's existing exception hierarchy rather than inventing a new HTTP status this app doesn't otherwise use**: "not configured" (blank `SmtpHost`/`MailFrom`, 503 in Node) → `NexusBadGatewayException` (502) — same "closest existing mapping for an external-dependency misconfiguration" precedent `ShipmentHelper.AssertValidExportRoot` already established in 8a.1; "no recipients" (400 in Node) → `NexusValidationException` (400, an exact match); any other connection/protocol failure (502 default in Node) → caught in `ShipmentCollectionEmailHelper.SendAsync` and wrapped into `NexusBadGatewayException`, the same `catch (ex is not NexusApiException)` pattern `LabelPrintHelper.PrintAsync` already established for its own raw-socket failure path, rather than duplicating that wrap inside `SmtpMailer` itself.
- **The PDF attachment is generated once and both written to disk and attached from the same in-memory buffer** — Node's `generateShipmentDocuments` writes the packing-list PDF to the shipment's export folder, then `fsp.readFile`s it straight back for the MIME attachment; that round trip exists in Node only because `generateShipmentDocuments` is shared with the `documents/folder`-regeneration call site (8a.3) which really does need the write, so this port keeps the write (the shipment's saved packing list must still exist on disk afterward) but skips the pointless re-read, attaching the bytes `ShipmentPackingListPdfHelper.BuildPackingListPdf` already returned.
- **`BuildCollectionEmailBody`/`BuildMimeMessage`/`SplitBase64Lines` are `internal`, not `private`, specifically so they're unit-testable** — same "expose the pure logic for `InternalsVisibleTo` rather than leaving it locked inside a `private` method untestable" precedent `RedrumReversalHelper.PadCostCollectorBin` (Warehouse, Phase 7) already established. 7 new `ShipmentCollectionEmailHelperTests` cover the base64 76-char line-splitting boundary (under/at/over the boundary, and a multi-line remainder case), the CRLF line-ending discipline, and MIME structure (headers present, `Cc:` header omitted entirely when there's no cc, one PDF attachment's `Content-Type`/`Content-Disposition`/base64 body all present). `SmtpMailer`/`ShipmentCollectionEmailHelper.SendAsync` themselves need a live SMTP relay + `SqlConnection`, the same testability ceiling as every other route-orchestration Helper this phase — genuinely unverified against a real mail server, flagged alongside this app's other "confirmed compiling, not confirmed against a live external dependency" gaps.
- Real HTTP smoke test (`dotnet run` + `curl`) confirms the new route returns 401 unauthenticated with no 404/500; `dotnet build`/`dotnet test` both clean (252 tests, 7 new).

### Sub-phase 8a.5a: freight cost lines (ShipmentCost CRUD)

Port of the CRUD/read subset of `routes/shipmentcost.js` — a genuinely separate router file/mount (`api/shipmentcost`, not `api/shipmentmain`) covering `log.ShipmentCost`, deliberately scoped narrower than the plan's original 8a.5 description. New `Helpers/Logistics/ShipmentCostHelper.cs` + `Controllers/ShipmentCostController.cs`.

- **Deliberately excludes `POST /post-migo`, `POST /:costId/reverse` (real SAP purchase-order/goods-receipt creation under the calling user's own credentials) and `GET /analytics`, all flagged for a dedicated follow-up rather than rushed into this slice.** `post-migo`/`reverse` landed separately as Sub-phase 8a.5b below, once the shared `SapProxyException` gap it needed was fixed properly rather than worked around. `shipmentmain.js`'s own separate ClearPort declaration submission (`/customs/create`) landed as Sub-phase 8a.5c. `GET /analytics` remains unported (needs the same not-yet-ported inbound leg as everywhere else in this controller).
- **`GET /unprocessed`/`GET /processed` are a real, but partial, port of Node's 3-way `UNION ALL`** — outbound (`shipmentID` set) and manual (both FKs NULL) rows are included; the inbound leg (`poShipmentID` set, joined against `log.PurchaseOrderShipment`) is silently absent, the same not-yet-ported-table gap `ShipmentHelper.SearchAsync`'s own inbound leg and the `otif-report` route already deferred to Sub-phase 8b. `ShipmentCostHelper`'s own header comment flags this explicitly so it isn't mistaken for a complete list once 8b lands the inbound side.
- **`UpdateShipmentCostRequest`'s null-means-unchanged fields (`CostElement`/`CostCenter`/`CostType`) still reject an explicit blank string** — same "omitted vs. explicitly blank are different, and only the latter is rejected" behavior Node's own `!== undefined` + blank-check preserves, not just the by-now-standard "null means unchanged" simplification alone.
- **`POST /manual`/`PATCH /manual/:costId` share one `ValidateManual`/`ResolveCostElementAsync`/`ManualParameters` implementation** — Node's own two route handlers are near-verbatim copies of each other (create vs. edit-in-place over the same fields); this port collapses that duplication into one `Helper` implementation called from two `internal` entry points, rather than porting the duplication as literally as everything else in this migration usually preserves — a case where Node's own duplication was clearly accidental (identical validation blocks, not a deliberately-different set of rules), not a real behavioral asymmetry worth preserving the way e.g. Sub-phase 8d's permission-gate asymmetries were kept.
- **`GetEstimateAsync` (the KN freight-cost auto-calculation used by the booking modal) is a straight port**, including the forwarder-name-normalization heuristic (`kuehnenagel`/`kuehneandnagel` string matching after stripping non-alphanumerics) and the volumetric-weight/postcode-prefix/rate-band lookup against `log.RatesKN` — no logic changes, since none of this involves an external call or elevated SAP write.
- 11 new `ShipmentCostHelperTests` cover every pre-connection validation path on `UpdateAsync`/`CreateAsync`/`CreateManualAsync`/`UpdateManualAsync` (same "assert a mock `INexusOperationsDb` whose `CreateConnectionAsync` throws is never actually called" precedent `PalletPackagesHelperTests` established in Warehouse) — `GetEstimateAsync`/the list/read methods all open a connection unconditionally, same testability ceiling as everywhere else in this migration. Real HTTP smoke test confirms all 12 new `api/shipmentcost/*` routes return 401 unauthenticated with no 404/500; `dotnet build`/`dotnet test` both clean (263 tests, 11 new).

### Sub-phase 8a.5b: real SAP PO/goods-receipt posting + reversal

Port of `POST /post-migo` and `POST /:costId/reverse` — the piece deliberately deferred out of 8a.5a. New `Helpers/Logistics/ShipmentCostSapPostingHelper.cs`, plus a cross-cutting fix to `Services/SapServerClient.cs` this slice needed to be faithful rather than lossy.

- **Fixed a real, previously-flagged gap in `SapServerClient` before porting the route that actually needed it, rather than working around it locally.** `SapProxyException` used to discard the failure response's own `data` payload entirely — every SAP-proxy caller in this app only ever saw the generic wrapper text (`error.message`), never SapServer's real diagnostic underneath it. Node's `extractSapErrorMessage` (routes/shipmentcost.js) is the one caller that actually depends on that detail: SapServer returns PO-creation failures as an HTTP 400 with `success:false` and the real SAP RETURN-table messages under `data.poMessages` (see `PurchasingController.CreatePurchaseOrderAndReceipt` — `PoMessages` is present on the SAME `CreatePoAndReceiptResponse` shape whether the PO succeeded or not), and without that detail every failed PO post in this app would show nothing more useful than "Purchase order creation failed." `SapProxyException` now carries `ResponseData` — the envelope's `Data` typed as whatever `T` the failing call asked for (`SendAsync<T>` already deserializes the full envelope generically, so this is a same-call-site type, not a raw `JsonElement`) — and `ShipmentCostSapPostingHelper.ExtractSapErrorMessage` casts it back to `SapCreatePoAndReceiptResponse` and appends the joined `PoMessages` text, matching Node's own `[type] message; [type] message` join exactly. (Named `ResponseData`, not `Data` — `System.Exception.Data` already owns that name; `CS0114` caught this immediately at compile time.) All 4 existing `SapProxyException` call sites needed no changes — the new parameter is optional and defaults to `null`.
- **Same not-yet-ported-inbound-leg gap as 8a.5a**, both flagged inline in `ShipmentCostSapPostingHelper`'s own header comment: the cost-line lookup behind `post-migo` is a 2-way `UNION ALL` (outbound + manual), not Node's 3-way (+ inbound, `log.PurchaseOrderShipment`) — a cost ID that only exists on the inbound side currently gets "No unprocessed records found for the given IDs" instead of a real posting attempt, until Sub-phase 8b lands that table.
- **The three-outcome shape Node's route has — 404 "nothing found at all", 400 "found but nothing postable" (with `blockedCostIDs`), 200 "posted, per-line results" — is preserved via a `PostMigoResult` that carries an optional `Error` string**, rather than throwing a `NexusValidationException` for the 400 case and losing the accompanying `BlockedCostIds` data it needs to carry alongside the message. Same "Helper returns a result, controller maps to the right HTTP status" pattern this codebase already uses for `ShipmentHelper.MarkCollectedBulkAsync`'s `BulkActionOutcome` — the controller checks `result.Error is not null` and returns 400 with the full result as `data`, otherwise 200. The genuinely-nothing-found case (zero rows fetched at all, not even a blocked one) is the one outcome that still throws (`NexusNotFoundException`), since there's no partial result to carry in that case.
- **`resolveMaterialGroup`'s SAP-Material-Group-code validation (`log.CostTypes.typeID` lookup) runs per line, before any SAP call, exactly matching Node's own fail-before-SAP ordering** — a bad Cost Type on one line rejects that line's whole group with a clear, specific error rather than sending SapServer a code that would just roll back with a confusing SAP-side message. Node's own `costType`/`log.CostTypes.typeID` column-type mismatch (`NVARCHAR(3)` vs `BIGINT` — a 7-character code like `"ITLG01A"` can't actually fit in either) is preserved exactly as Node's own `WHERE typeID = @typeID` does, relying on SQL Server's implicit conversion — flagged in the Helper's header comment as a pre-existing oddity, not something to silently "fix" here per this migration's "schema stays as-is" principle.
- **`reverse`'s SAP call failure is deliberately NOT caught and wrapped locally** — `SapProxyException` already carries the right HTTP status from SapServer itself (mapped through `ApiExceptionMiddleware` like any other `NexusApiException`), so letting it propagate naturally is more accurate than Node's own blanket `res.status(500)` for every failure mode. `post-migo`'s per-group `SapProxyException` catch is different and deliberate: it keeps processing the remaining groups (matching Node's own per-group try/catch — one shipment's PO failure must not abort every other shipment's cost lines in the same batch request).
- 8 new tests (`ShipmentCostSapPostingHelperTests`) cover `PostMigoAsync`'s pre-connection `costIds` empty-list guard (same mock-`CreateConnectionAsync`-throws precedent as 8a.5a) plus `Prefix2`/`ExtractSapErrorMessage`, both made `internal` (not `private`) specifically for this — same "expose the pure logic for testability" precedent `SplitBase64Lines` established in Sub-phase 8a.4. `PostMigoAsync`'s SAP-calling branches and `ReverseAsync` all need a live `SqlConnection` + real SapServer, the same testability ceiling as every other elevated-SAP-call Helper in this migration — **genuinely unverified against a live SAP system**, and given the stakes (real PO/goods-receipt creation under a real user's credentials), this should be the first thing validated for real once a live SAP+SapServer environment is reachable, ahead of every other still-unverified endpoint in this app. Real HTTP smoke test confirms both new routes return 401 unauthenticated with no 404/500; `dotnet build`/`dotnet test` both clean (271 tests, 8 new).

### Sub-phase 8a.5c: ClearPort customs declaration submission

Port of `POST /customs/create` (`shipmentmain.js` — mounted under `api/shipmentmain`, not `api/shipmentcost`, unlike the rest of Sub-phase 8a.5) — the last piece of Shipping's own ClearPort integration. Four new pieces: `Services/ClearPortOptions.cs` + `Services/ClearPortClient.cs` (the typed-HttpClient wrapper, following `SapServerClient`'s established pattern but with ClearPort's own `X-API-Key` header scheme, not JWT), `Helpers/Logistics/SapCustomsDataHelper.cs` (the SAP LIPS/LIKP/VBFA/MARC/KNA1 lookup), `Helpers/Logistics/ClearPortShipmentPayloadHelper.cs` (pure export-declaration payload construction), `Helpers/Logistics/ShipmentCustomsHelper.cs` (route orchestration).

- **UNVERIFIED against a live ClearPort sandbox or real credentials — flagged explicitly in every new file's header comment, not just here.** No live ClearPort access (or Kuehne+Nagel-adjacent contract detail) has been reachable in any environment this migration has been developed in. Same caveat class this migration's other external integrations carried before real-world confirmation (`SapServerClient`, `SmtpMailer`) — the status-code/error-message mapping in `ClearPortClient` is a faithful *port of Node's own handling*, not independently re-verified against ClearPort's actual API docs. Per the plan's own explicit "budget real time for external-API contract verification" guidance (aimed at Sub-phase 8c's Kuehne+Nagel/ClearPort work, but equally true of this separate ClearPort integration) — this should be the first thing validated for real once ClearPort access exists, alongside 8a.5b's own "verify against live SAP" flag.
- **`fetchSapCustomsData`'s Node implementation makes a self-referential HTTP loopback call to this same app's own `/api/sap/lips` etc. (routes/sap.js), which then forwards to SapServer's real `api/customs/*`** — an artifact of Express having no easy way to call another route's handler logic directly without a real HTTP round trip. `SapCustomsDataHelper` skips that workaround entirely and calls SapServer directly via the already-shared `ISapServerClient`, matching SapServer's own `CustomsController` (`api/customs/lips/likp/vbfa/marc/kna1`) — same "port the intent, not the Express-specific workaround" precedent `ShipmentManualCargoHelper.CreateFolderAsync` set in 8a.2 for Node's `mkdirRecursiveSafe`.
- **KNA1 is fetched even though `buildClearPortShipmentPayload` never reads it** — confirmed directly in Node: `fetchSapCustomsData` returns `kna1Data`, but the payload builder only destructures `lipsData`/`likpData`/`vbfaData`/`marcData`. A real, vestigial fetch in the original app, not a mistake in this port — preserved (not dropped) in case a future caller starts relying on it, flagged inline in `SapCustomsDataHelper`'s own comment.
- **Two confirmed Node oversights are deliberately NOT replicated literally, both flagged in `ClearPortShipmentPayloadHelper`'s header comment rather than silently "fixed" without comment**: (1) `declarant` is built from raw, fallback-less `process.env.LOGISTICS_ORIGIN_*` (unlike `exporter`, built from the shipment's own already-resolved origin fields) — if those exact env vars aren't set, Node's declarant silently comes out all-null; this port uses the same resolved `LogisticsOptions` origin fields for declarant as for exporter (the standard "own registered address as both exporter and declarant" pattern, almost certainly the intended behavior). (2) `ducr` is built from raw `process.env.CLEARPORT_EORI` with no fallback — unset, Node's DUCR would literally contain the string `"undefined"`; this port uses `ClearPortOptions.Eori` (the same resolved default the rest of the payload already uses). Both are genuinely unexercised code paths (this route has never run against live ClearPort), and neither has a clean C# equivalent to JavaScript's `undefined`-string-concatenation footgun worth deliberately reproducing.
- **`Math.round(value, 2)` in Node is a confirmed, preserved bug**: JavaScript's `Math.round` only ever takes one argument, so `statisticalValue`/`totalInvoice` are actually rounded to the nearest WHOLE number, not 2 decimal places, as the `, 2` argument is silently ignored. `Math.Round(value, MidpointRounding.AwayFromZero)` reproduces this exactly (0 decimal places, round-half-up matching JS's `Math.round` rather than C#'s own default round-half-to-even) — same "document actual behavior, even if it's a bug" precedent as `SapPad`'s XML-doc mismatch and the old `RfcRowHelpers.GetDecimal` writeup in SapServer's own CLAUDE.md.
- **`ParseEuropeanDecimal` (applied to `VbfaRow.StatisticalValue`, raw unparsed SAP text) carries the exact same naive "always European-grouped format" assumption that was confirmed WRONG for other SAP fields on the SapServer side** (the old `RfcRowHelpers.GetDecimal` bug — some values come back as plain invariant-culture text with no thousands grouping at all, and this exact stripping logic would misparse those by 10x/100x/1000x). This is Node's own separate copy of that same naive assumption (not SapServer's already-fixed `RfcRowExtensions.ParseSapDecimal`), preserved exactly since this route has never run against live SAP to confirm whether `StatisticalValue` needs the same per-value separator-detection fix — flagged inline as the first thing to check if a real ClearPort submission's statistical value looks 10x/100x too large.
- **`itemConsignee`/`carrier` (both computed in Node but never actually used — commented out in the real payload, `//consignee: itemConsignee` and `//carrier`) are not built at all in this port** — confirmed dead by reading the function directly, not assumed; `ClearPortExportItem`'s DTO has no `Consignee` field for exactly this reason.
- **`ClearPortShipmentPayloadHelper` has zero I/O of its own** (no DB, no HTTP) — everything it needs is passed in by `ShipmentCustomsHelper`, so it's fully unit-testable without a connection or a real ClearPort/SapServer. 20 new tests (`ClearPortShipmentPayloadHelperTests`) cover the two validation guards, single- and multi-line grouping (mass/statistical-value summing, invoice-number dedup via the line's own delivery+commodity key), the commodity-code fallback when MARC has no row, the DDP-consignee header logic (both "every line is DDP" and "some but not all" cases), the preserved `Math.round` rounding bug, `NormalizeCountryCode`'s name-mapping table, `ParseEuropeanDecimal`, and `ToNameAndAddress`'s blank-to-null handling. `SapCustomsDataHelper`/`ShipmentCustomsHelper`/`ClearPortClient` all need a live SapServer/ClearPort, the same testability ceiling as every other external-integration Helper in this migration. Real HTTP smoke test confirms the new route returns 401 unauthenticated with no 404/500; `dotnet build`/`dotnet test` both clean (291 tests, 20 new).

## Sub-phase 8e: Vendor Consignment Tracker

`consignment.js` (520 LOC/15 real routes) + `consignmentsql.js` (942 LOC — the DB/algorithm layer) — a confirmed Logistics tile (Material Planning), not a Warehouse one (corrected during Phase 7 research). Replaces three manually-maintained vendor consignment workbooks (Chemours, Fothergill/FCF, Raaj) with SQL-backed tracking of vendor-owned stock physically on-site. Split into two sub-phases matching the same "DB/algorithm core first, external I/O second" precedent Sub-phase 8a.5 already used for Shipping.

### Sub-phase 8e.1: DB/algorithm core

New `Helpers/Logistics/ConsignmentTrackerHelper.cs` + `Controllers/ConsignmentTrackerController.cs`, mounted at `api/consignment` (matching Node's own separate mount — note SapServer has its own, unrelated `api/consignment` controller in a completely different app/URL space, no real collision).

- **`sql/migrate_consignment_tracker.sql` describes a stale, superseded design — a separate "kongsberg" database, `dbo.*` schema — that was never actually deployed.** Confirmed by cross-checking against `migrations/nexus_operations/20260804120000_initial_schema.cjs` and `.../20260827150000_consignment_reversal_tracking.cjs`, and against what `routes/consignmentsql.js` itself actually queries: the real, currently-deployed tables are `log.ConsignmentVendorConfig`/`ConsignmentDelivery`/`ConsignmentDeclaration`/`ConsignmentDeclarationLine`/`ConsignmentStockSnapshot`, all in NexusOperations, same as every other Logistics table in this migration. Same "a `.sql` migration file goes stale relative to the real deployed schema" pattern already confirmed elsewhere (SapServer's CostElements/CostCenters IDENTITY mismatch) — ported against the real `log.*` schema, not the stale draft, flagged in the Helper's own header comment so a future reader isn't misled by the `.sql` file either.
- **`ComputeReversalCancellations` — the highest-stakes pure algorithm in this slice — is a faithful, line-for-line port of a function with real, documented production incidents behind it** (Raaj Ratna doc 5005206623 cancelled same-day by 5005206624, and the 3-link chain 5005174284 → 5005203102 → 5005203103, both confirmed showing false "overdue" expiry warnings before this fix). The parity-walk logic (root starts live, chain length determines whether the root ends live or cancelled, every non-root row in a chain is always cancelled, a row already touched by a real declaration is reported in `NeedsReview` instead of silently zeroed) is preserved exactly. 6 dedicated tests cover the 2-link (root cancelled) and 3-link cancel-of-cancel (root restored) cases by name, the already-declared-against guard, the multiple-reversers-of-the-same-target edge case, and a reversal pointing outside the given row set.
- **`BuildAllocationProposal` (FEFO/FIFO greedy walk) and `ComputeReassignmentPlan` (shared-mutable-pool reassignment across multiple cancelled lines) are both pure and fully unit-tested** — 9 more tests cover greedy multi-row consumption, shortfall reporting, skipping exhausted rows, 3-decimal-place rounding, the shared-pool behavior across two cancelled lines for the same material, FEFO ordering (expiry date then document date), and declaration/declaration-line processing order. `ComputeReassignmentPlan`'s mutable shared pool is modelled as a `Dictionary<long, decimal>` of remaining-qty-by-DeliveryId threaded through the loop, rather than mutating record instances in place (C# records are immutable) — same net effect as Node's own in-place array mutation.
- **`BuildReassignmentPlanForVendorAsync`/`ApplyReassignmentPlanAsync` are ported faithfully even though no route in Node calls them at all** — confirmed by grepping the whole Node repo: they're only reachable from Node's own unit tests (`test/unit/consignmentsql.test.js`). Real, deliberately-built, unit-tested recovery logic (reassigning a declaration's allocation off stock that turned out to be SAP-cancelled) that was simply never wired to an HTTP route in the original app either — kept available as Helper methods the same way, not exposed via `ConsignmentTrackerController` since Node's own controller doesn't expose it either.
- **Vendor detail (`GET /vendors/:id`) nests `{Vendor, Materials}` instead of Node's flattened `{...vendor, materials}`** — a deliberate, low-risk simplification (avoids duplicating 12 vendor fields on a wrapper DTO) matching this migration's `ShipmentContext`-style "nest related data" convention elsewhere, acceptable since frontend work for this migration is deliberately deferred everywhere, not just here.
- **Deliberately excludes** (Sub-phase 8e.2): the SAP GR sync (`fetchSapVendorGr`/`mapGrRows`, `POST /vendors/:id/sync` + the daily cron entry point `runConsignmentSync`), the live-stock refresh (`fetchSapConsignmentStock`, `POST /stock/refresh` — this slice only reads the already-populated `ConsignmentStockSnapshot` cache via `GetStockSnapshotAsync`/`GetStockSnapshotMetaAsync`, it doesn't write it), and the declaration PDF (`GET /declarations/:id/pdf`, `lib/consignmentDeclarationPdf.js`) — all confirmed real SapServer endpoints already exist for the SAP calls (`ConsignmentController.cs`'s `api/consignment/gr`/`api/consignment/stock`, confirmed by reading that controller directly), so 8e.2 is "wire it up," not "figure out if it's possible."
- 15 new `ConsignmentTrackerHelperTests` (the algorithm coverage above) plus real HTTP smoke test confirming all 15 new `api/consignment/*` routes return 401 unauthenticated with no 404/500; `dotnet build`/`dotnet test` both clean (306 tests, 15 new).

### Sub-phase 8e.2: SAP sync + PDF

New `Helpers/Logistics/ConsignmentSapSyncHelper.cs` (GR sync + stock refresh + the daily-cron entry point), `Helpers/Logistics/ConsignmentDeclarationPdfHelper.cs` + `ConsignmentDeclarationDocumentHelper.cs` (declaration PDF), plus the two Sub-phase 8e.1-deferred DB write methods (`ConsignmentTrackerHelper.ReplaceStockSnapshotAsync`, `.UpsertDeliveriesFromSapAsync`) added onto that same Helper. Wires the 3 routes 8e.1 deliberately left out.

- **Both SAP calls (`api/consignment/gr`, `api/consignment/stock`) go through the already-shared `ISapServerClient` directly against SapServer's real, already-existing `ConsignmentController`** — confirmed by reading that controller directly (not assumed from the route name), same "SapServer already has the endpoint, this is 'wire it up' not 'figure out if it's possible'" confidence 8e.1's own header comment flagged ahead of this slice landing.
- **`GET /api/consignment/gr` needed the query string built directly into the request path, not passed via `GetAsync`'s optional body parameter** — SapServer's `GetVendorGr` action binds `sapVendorNumber`/`sinceDate` via `[FromUri]` (query string), unlike the handful of `SapServerClient` GET calls elsewhere in this app that read a JSON body on GET (matching Node's own `sapGet()` axios helper) — a real, confirmed difference between SapServer endpoints that needed checking per-call, not assumed uniform. Given a JSON body on a GET request, Web API 2's `[FromUri]` binder simply never looks at it, so getting this wrong would have silently sent `sapVendorNumber` as nothing and 400'd on SapServer's own required-field check — caught by reading `ConsignmentController.GetVendorGr`'s signature directly before writing the call, not by trial and error.
- **`FetchVendorGrAsync`/`FetchConsignmentStockAsync` both use `longRunning: true`** (SapServerClient's 120s timeout, not the 30s default) — matching Node's own confirmed-in-production timeout increases for these two exact calls (3 minutes for a first-ever vendor sync with no `sinceDate` floor exceeding the original 45s; 10 minutes for the unfiltered plant-wide MKOL stock scan, matching the existing turns-valclass MRP sync's own established timeout for the identical RFC). `SapServerOptions.LongRunningTimeoutSeconds` (120s default) is shorter than Node's own 3/10-minute figures — flagged, not silently widened beyond what this app's existing shared option already provides; a real, live first sync exceeding 120s would need `LongRunningTimeoutSeconds` raised specifically, or a dedicated timeout for these two calls, once that's confirmed against real vendor GR history.
- **`RunDailySyncAsync` (the cron entry point, Node's `runConsignmentSync`) is ported as a callable Helper method only — no Quartz.NET job schedules it** — no cron job has been wired up anywhere in this whole migration yet (deferred to Phase 10's cross-cutting closeout per the plan), so this isn't a Consignment-specific gap. Its return shape (`DailySyncResult` — a list of typed `VendorSyncOutcome` plus one `StockSnapshotSyncOutcome`) is a cleaner, fully-typed replacement for Node's own untyped, heterogeneously-shaped result array (`{vendor,skipped}` / `{vendor,pulled,...}` / `{vendor,error}` / `{stockSnapshot:true,...}` all pushed into one array) — safe to diverge here since nothing consumes the literal JSON shape yet (unlike every routed HTTP endpoint elsewhere in this migration, where the response shape is preserved exactly even when Node's own shape is awkward).
- **The declaration PDF is a QuestPDF rebuild, not a literal port of `lib/consignmentDeclarationPdf.js`'s pdfkit code** — same "deliberate non-pixel-perfect, QuestPDF-idiomatic re-layout" precedent as every other PDF Helper in this migration (`LabelPdfHelper`, `ShipmentPackingListPdfHelper`, `ShipmentPackagingDeclarationPdfHelper`); QuestPDF's own automatic pagination replaces Node's manual `ensureSpace()`/buffered-page-footer-loop machinery. Same content and section grouping (header band, supplier/date/status info blocks, stock summary table, per-material delivery matrix with a section band + subtotal row, total, footer).
- **`ConsignmentDeclarationDocumentHelper.GeneratePdfAsync` derives Consumption/Ending Stock from the declaration's own lines rather than re-querying them**, matching Node's own route exactly — only Starting Stock/Deliveries actually need a DB round trip (`GetDeclarationStockSummaryAsync`, already built in 8e.1), since `QtyAllocated` per material is already fully known from the declaration detail just fetched.
- 4 new `ConsignmentDeclarationPdfHelperTests` (real, verifiable PDF structure — the `%PDF` magic header and a sane byte length, same precedent as every other PDF Helper's tests — covering a populated declaration, an empty one, multiple material groups, and a Confirmed declaration with a settlement document number). `ConsignmentSapSyncHelper`/`ConsignmentDeclarationDocumentHelper` themselves need a live SapServer + `SqlConnection`, the same testability ceiling as every other external-integration Helper in this migration — **genuinely unverified against a live SAP system**, same caveat class as Sub-phase 8a.5b/8a.5c. Real HTTP smoke test confirms all 3 new routes return 401 unauthenticated with no 404/500; `dotnet build`/`dotnet test` both clean (310 tests, 4 new).

## Sub-phase 8b: Purchasing/Performance

`performance.js` (4,745 LOC/75 routes) + `performancesql.js` (the DB layer) + `mrpanalysis.js` (412 LOC/10 routes) + `inboundcosts.js` (243 LOC/4 routes) — by far the largest single file in the app and the last major piece of Logistics. Scoped via a background research pass (route inventory, schema confirmation, 11 flagged risks) into 7 slices ordered lowest-risk-first, with every real-SAP-write route deliberately sequenced last (8b.7) — same "DB/algorithm core first, external I/O second, SAP writes last" precedent 8a.5/8e already established. Supporting files beyond the four originally scoped: `performanceallocation.js`, `performanceforecast.js`, `performanceorderlink.js`, `performancesap.js`, `performancesync.js`, `performancevaluestream.js`, `isoparConstants.js`, `isoparPeriods.js`, `lib/unitConversion.js`, `lib/poPdf.js`.

### Sub-phase 8b.1: Shared forecast core + read-only dashboard routes

New `Helpers/Logistics/ForecastMathHelper.cs` (the pure stock-forecast math shared across the whole sub-phase), `Helpers/Logistics/UnitConversionHelper.cs`, `Helpers/Logistics/IsoparPeriodHelper.cs`, `Helpers/Logistics/PerformanceDashboardHelper.cs` + `Controllers/PerformanceController.cs` (`api/performance`), `Helpers/Logistics/MrpAnalysisHelper.cs` + `Controllers/MrpAnalysisController.cs` (`api/mrp-analysis`).

- **`ForecastMathHelper` is ported in full now (MakeDailyUsageFn/MakeIsoparDailyUsageFn/BuildWeeklyStockForecast/MergeWeeklyForecasts/AddWorkingDaysUtc/DemandOverDays/FindStockBelowThresholdDate) even though only BuildWeeklyStockForecast/MergeWeeklyForecasts are wired to a live route yet** (`/turns-valclass/history`, deferred to 8b.2 — it needs open-order/demand-adjustment CRUD that doesn't exist until then). DemandOverDays/FindStockBelowThresholdDate/AddWorkingDaysUtc exist purely for the order-suggestion engine (8b.3) and Isopar stock-risk (8b.2), both of which build directly on this module — matches the established "port the shared piece before its real caller ships" precedent (8e.1's `BuildReassignmentPlanForVendorAsync`, `LogisticsReferenceHelper`'s reassignment functions).
- **JS `Date` objects map to UTC-kind `DateTime` throughout this module, not `DateOnly`** — `FindStockBelowThresholdDate`'s same-week linear interpolation genuinely produces a sub-day time component that `AddWorkingDaysUtc`'s day-stepping must preserve (JS steps via `setUTCDate`, which leaves the time-of-day untouched, and that fractional offset feeds real Overdue/DueSoon before/after comparisons downstream in 8b.3's `buildSuggestionForRow`) — confirmed by reading Node's own function bodies, not assumed. Matches the existing `AddWorkingDaysUtc` precedent already in `ProductionScheduleHelper`.
- **Rounding uses `Math.Round(x, 2, MidpointRounding.AwayFromZero)` throughout**, matching this migration's established emulation of JS `Math.round`'s round-half-up behavior (see `ShipmentCostHelper`'s freight-cost rounding) rather than .NET's default banker's rounding — deliberate, not left to the runtime default.
- **`/turns-valclass` (the full filtered data table) ported as a single `[FromQuery]` complex-type query object** (`TurnsValClassQuery` — Plant/ValuationClass/MrpController/MaterialType/ProfitCentre/Search/Material/MaterialText, all independently combinable, `search` OR's Material/MaterialText while `material`/`materialText` are separate AND'd filters — confirmed by reading the route directly, not assumed uniform with `search`). Null-coalesced to an all-null `TurnsValClassQuery` in the controller action, matching SapServer's own confirmed `[FromUri]`-complex-type-binds-null-when-no-query-keys-match gotcha (see SapServer's CLAUDE.md) even though ASP.NET Core's model binder doesn't share that exact defect — cheap insurance either way.
- **`/value-metrics`/`/otif-metrics`'s dynamic per-ValueStream pivot (Node: `result[date][vs] = {...}`, arbitrary string keys, not a fixed set) is modelled as `IReadOnlyDictionary<string, ValueMetricStream|OtifMetricStream>` per date row**, not a fixed record — the only way to keep the same dynamic-key JSON shape (`{date, PTFE: {...}, PV: {...}}`) the frontend already expects without a custom `JsonConverter`. Confirmed ASP.NET Core's default camelCase `PropertyNamingPolicy` does NOT touch `Dictionary<string,TValue>` keys (only `DictionaryKeyPolicy` would, which isn't set) — so `"PTFE"`/`"PV"` pass through unchanged while `ValueMetricStream`'s own properties still camelCase normally, exactly matching Node's mixed-case output.
- **The pivot/shaping logic for `/value-metrics`, `/otif-metrics`, and the shared refresh-status "latest run per dataset, no false confidence" logic (`/refresh-status` and `/turns-valclass/refresh-status` both call the same `ShapeRefreshStatus`, exactly like Node's own near-duplicate route bodies) are all extracted as pure `internal static` functions** (`ShapeValueMetrics`/`ShapeOtifMetrics`/`ShapeRefreshStatus`) separate from their DB-fetching callers — same "expose pure logic as internal for testing" precedent as `ClearPortShipmentPayloadHelper`/`RedrumReversalHelper.PadCostCollectorBin`, and the only practical way to get real coverage on this sub-phase's DB-heavy routes without a live SQL Server.
- **`log.TurnsValClassSnapshot`'s full ~36-column schema was read directly from the real deployed migration** (`migrations/nexus_operations/20260804120000_initial_schema.cjs`), not guessed from the SELECT list's column names — `DeletionFlag` is `BIT` (bool), every quantity/value column is `DECIMAL`, dates are `DATETIME`, confirming the C# `TurnsValClassRow` record's types exactly.
- **Deliberately excludes** (later 8b slices): Vendor/VendorMaterial/DemandAdjustment CRUD, Isopar readings/planning-rate/stock-risk (all landed next in 8b.2) and Isopar declarations (8b.6), `/turns-valclass/history` (needs the forecast core wired to real open orders, 8b.3), `/turns-valclass/refresh` + `/turns-valclass/change-valuation-class` (both stubbed 501 here — real SAP calls, 8b.6/8b.7), the entire order-suggestion engine, Inbound Costs, the ExcelJS orderbook export/upload, consignment-customers CRUD, and every other `mrpanalysis.js` route beyond `/trends`.
- 48 new tests (`ForecastMathHelperTests`, `UnitConversionHelperTests`, `IsoparPeriodHelperTests`, `PerformanceDashboardHelperTests`) — all pure, no DB/SAP needed. Real HTTP smoke test confirms all new `api/performance/*`/`api/mrp-analysis/trends` routes return 401 unauthenticated with no 404/500; `dotnet build`/`dotnet test` both clean (358 tests, 48 new).

### Sub-phase 8b.2: Vendor master data + demand adjustments + Isopar readings/planning-rate/stock-risk

New `Helpers/Logistics/VendorMasterDataHelper.cs` (vendors/vendor materials/demand adjustments), `Helpers/Logistics/IsoparHelper.cs` (meter readings/planning rate/stock risk), `Helpers/Logistics/PurchaseOrderSuggestionHelper.cs` (`ListOpenIncomingOrdersAsync` only — the order-suggestion engine itself lands in 8b.3), all wired onto the existing `PerformanceController`. New DTO files `Models/Dto/VendorMasterDataModels.cs` + `Models/Dto/IsoparModels.cs`.

- **`ListOpenIncomingOrdersAsync` is ported now, ahead of the order-suggestion engine that's its main consumer**, because Isopar stock-risk needs the identical "what's already incoming" query too — same "port the shared piece before its real caller ships" precedent `ForecastMathHelper` itself established in 8b.1. Lives in a new `PurchaseOrderSuggestionHelper.cs` (matching where it'll gain the rest of the order-suggestion data layer in 8b.3) rather than duplicated into `IsoparHelper.cs`.
- **`VendorMaterialRow` was already taken** — `ConsignmentTrackerModels.cs` (Sub-phase 8e.1) defines a narrower same-named record for its own, unrelated Vendor Consignment Tracker feature. This slice's vendor-material-assignment row is named `VendorMaterialAssignmentRow` instead of colliding; the two features track genuinely different data (this one is manually-maintained MRP planning config — MOQ/lead time/safety stock overrides — the other is physical vendor-owned stock on-site) and were never meant to share a type.
- **Every Create/Update path validates before ever opening a DB connection** (`vendorName`/`material` required, `usagePercent`/`readingQty`/planning-rate fields required-and-non-negative where Node checks `== null || < 0`) — `UsagePercent`/`ReadingQty` are modelled as nullable `decimal?` in the request DTOs specifically so a missing value can be rejected distinctly from a literal, valid `0` (a non-nullable `decimal` would silently default-bind a missing JSON field to `0` and pass the negativity check Node explicitly guards against). Matches `ShipmentCostHelper`'s established "validate first, DB second" testability precedent — `VendorMasterDataHelperTests`/`IsoparHelperTests` assert every guard rejects with `NexusValidationException` against a mock whose `CreateConnectionAsync` throws if ever actually called.
- **`UQ_Vendor_Name`/`UQ_VendorMaterial` unique-constraint violations are caught and rephrased into a plain, actionable `NexusValidationException`** (`"A vendor named \"X\" already exists."` / `"X is already assigned to this vendor."`) via a `catch (Exception ex) when (ex.Message.Contains(...))` filter around the INSERT — matching Node's own `/UQ_Vendor_Name/i.test(err.message)` regex-on-error-message rephrasing exactly, including relying on the constraint name surfacing in the driver's exception text (unverified against a real SQL Server error message shape — flagged the same way Node's own approach already was fragile, not a new risk introduced by this port).
- **Demand adjustment overlap rejection (`findOverlappingDemandAdjustment`) is ported verbatim**, including the double-`NOT` null-as-infinity date-range-overlap SQL and the `excludeId` self-exclusion for updates — a business rule with real teeth (this is what stops two adjustments silently fighting over the same material/date range), so the exact SQL shape was kept rather than reimplemented in C#.
- **`GetPlanningRateWithRecommendationAsync`/`ComputeStockRiskAsync` both split into a thin DB-fetching wrapper + a pure `internal static` compute function** (`ComputePlanningRateRecommendation`/`ComputeStockRisk`) taking already-fetched rows — same "expose pure logic as internal for testing" precedent as 8b.1's `Shape*` functions, and the only way to get real coverage on Isopar's two genuinely non-trivial algorithms (consecutive-day-interval averaging with a >5%-difference-and-≥5-samples recommendation gate; the bounded-review-window stockout/overcapacity forecast check) without a live SQL Server. `ComputeStockRisk` reuses `ForecastMathHelper.BuildWeeklyStockForecast`/`MakeIsoparDailyUsageFn` directly, `bucketDays: 1`, exactly as Node's `computeIsoparStockRisk` does.
- **Deliberately excludes** (later slices): Isopar declarations (`ISOPAR_DECL`-gated, 8b.6) and `checkIsoparDeclarationDue`'s `INotificationService` wiring; the order-suggestion engine itself (`computeOrderSuggestions`/`computeVendorOrderBuild`/accept/manual, 8b.3) despite now having `ListOpenIncomingOrdersAsync` available to build on.
- 27 new tests (`VendorMasterDataHelperTests`, `IsoparHelperTests`) — all pure/pre-connection-guard, no DB/SAP needed. Real HTTP smoke test confirms all 20 new routes return 401 unauthenticated with no 404/500; `dotnet build`/`dotnet test` both clean (385 tests, 27 new).

### Sub-phase 8b.3: Order suggestion engine

`PurchaseOrderSuggestionHelper.cs` extended in place (it already held `ListOpenIncomingOrdersAsync` from 8b.2) with the full order-suggestion engine: `BuildSuggestionForRow`/`ComputeOrderSuggestionsGroupedAsync`/`ComputeVendorOrderBuildAsync`/`GroupSuggestionsByVendor`/`EnforceMaterialQty`/`ValidateVendorCombinedQty`/`BuildAcceptPayload`/`PreviewAsync`/`AcceptAsync`/`AcceptBatchAsync`/manual entry (single + bulk CSV)/Tracked Orders/`AssignScheduleAgreementAsync`/status-update/PO-item-patch/delete. New `Models/Dto/OrderSuggestionModels.cs`. `VendorMasterDataHelper.ListDemandAdjustmentsAsync` (the forecast-facing raw read, no `MaterialText` join) added alongside the existing admin-list version. `create-po`/`regenerate-pdf` (real elevated SAP PO creation) remain deferred to 8b.7; the Inbound Shipment tracking routes that follow this section in `routes/performance.js` are 8b.4.

- **Found and fixed a real fidelity gap in `ForecastMathHelper.DemandOverDays` while wiring this slice**: it was typed `int days`, but `buildSuggestionForRow`'s `coverageDays = leadTimeDays + ORDER_COVERAGE_BUFFER_DAYS` is genuinely fractional whenever `leadTimeDays` is (e.g. a vendor with a 2.5-day lead time) — Node's `addDaysUtc` adds fractional days in milliseconds with no truncation. Widened to `decimal days` (`from.AddDays((double)days)`) before this slice's own tests were written, so nothing shipped depending on the truncating version. A concrete instance of this migration's established discipline: a pure-math Helper ported ahead of its real caller (8b.1's own header comment) still needs re-checking against that caller's actual call shape once it arrives, not assumed correct just because it built and passed its own unit tests in isolation.
- **A public DTO can't expose a member of a type nested inside an `internal` class (CS0053)** — `OrderSuggestionPreviewResult.StockForecast` needed `ForecastMathHelper.WeeklyStockForecast` (an internal nested record) but couldn't reference it directly. Fixed with a small public mirror (`WeeklyStockForecastDto`/`ForecastWeekDto`/`ForecastDeliveryDto`) plus a `ToDto` mapping function in the Helper, rather than making `ForecastMathHelper` itself public (which would leak Helper-internal implementation types across the whole app's public surface just to satisfy one DTO).
- **`Preview`'s Isopar override requires a real meter reading to exist, not just a configured planning rate — a genuine, confirmed asymmetry with `BuildSuggestionForRow`** (which only requires the rate). Read directly from Node's two function bodies side by side, not assumed uniform; preserved exactly rather than "fixed" into consistency, since preview's stricter requirement is deliberate (no point simulating a hypothetical order against a rate with no real data point behind it yet).
- **`EnforceMaterialQty`/`ValidateVendorCombinedQty`/`BuildAcceptPayload`/`BuildSuggestionForRow`/`GroupSuggestionsByVendor` are all pure `internal static` functions**, fully unit-tested in isolation (lot-size snap-to-nearest/never-zero/clamp-to-cap behavior; exact-quantity/min/max vendor validation with the `toLocaleString()`-equivalent `#,##0.###` formatting; working-days delivery-date/ready-to-collect-date math with an override; the full Urgency state machine — NotDue/Overdue/DueSoon/Upcoming — plus the Isopar meter-reading branch and spot-PO detection; vendor-level MOQ shortfall/overage tallying and cross-vendor sort order) — same "pure business logic gets full coverage independent of any DB" precedent as 8e.1's `ComputeReversalCancellations`, the highest-value tests in this slice by far given how much real business logic (safety-stock breach timing, lot-size rounding, unit conversion at the vendor-order-unit boundary) lives here.
- **Every write path validates before ever opening a DB connection** (required fields, non-empty arrays, positive quantities, a valid target status) — same `ShipmentCostHelper`/8b.2 precedent, tested against a mock whose `CreateConnectionAsync` throws if ever actually called.
- **`ManualBulkAsync`'s per-row error handling deliberately catches bare `Exception`, not just `NexusApiException`** — matches Node's own unconditional per-row try/catch exactly (`insertManualOrderRow` can throw a validation/not-found error, and a CSV row's own bad data could in principle surface anything); the explicit business requirement is "a typo in row 12 shouldn't block rows 1-11 from saving," so isolating the *whole* row rather than a specific exception type is the correct, deliberate design here, not a caught-general-exception smell.
- **`AssertOrderEditableAsync`'s completed-order lock (Received/Booked) is enforced inside the shared `UpdateStatusInternalAsync`/delete path**, so `AssignScheduleAgreementAsync` (which reuses the same status-update helper) automatically inherits the same lock without needing its own copy — though in practice its own upstream guard (`already have a PO number or aren't in Accepted status`) always catches a locked row first.
- **Deliberately excludes** (later slices): `create-po`/`regenerate-pdf` (real elevated SAP PO creation + PO PDF, 8b.7) and everything in the Inbound Shipment tracking section of `routes/performance.js` (8b.4) — `ShipmentId`/shipment fields on `OrderSuggestionTrackedRow` are read (LEFT JOIN, already present in the query) but nothing here creates/updates a shipment yet.
- 36 new tests (`PurchaseOrderSuggestionHelperTests`) — all pure/pre-connection-guard, no DB/SAP needed. Real HTTP smoke test confirms all 12 new routes return 401 unauthenticated with no 404/500; `dotnet build`/`dotnet test` both clean (421 tests, 36 new).

### Sub-phase 8b.4: Inbound shipment tracking + Inbound Costs

New `Helpers/Logistics/InboundShipmentHelper.cs` (shipment list/create/manual-create/detail/update/manual-items/documents/assign/cancel) + `Helpers/Logistics/InboundCostHelper.cs` ("Associated Costs" lines) + `Controllers/InboundCostsController.cs` (`api/inboundcosts`, matching Node's own separate mount) + shipment/document/manual-item/cost-line actions added onto the existing `PerformanceController`. New `Models/Dto/InboundShipmentModels.cs` + `Models/Dto/InboundCostModels.cs`. Filesystem-only document handling — Mark Received/Undo Received (the real SAP goods-receipt write, `postGoodsReceiptToSap`/`reverseGoodsReceiptToSap`) is deferred to 8b.7 along with `create-po`/`regenerate-pdf`.

- **`ShipmentCostHelper.ResolveCostElementAsync` was refactored from a `ManualShipmentCostRequest`-shaped private method into a `(direction, tier, explicitCostElement)`-shaped `internal` one**, exactly as flagged when 8b.3's research was scoped — `InboundCostHelper.InsertLineAsync` now calls the same shared lookup instead of duplicating the `log.CostElements` query a third time (the manual-freight-cost flow and Inbound Costs both need the identical "standard vs premium GL code" resolution). The manual-cost call sites were updated to the new signature; all their existing tests still pass unchanged.
- **`insertInboundCostLine`'s `information` parameter is accepted by Node but never actually written anywhere** — its own SQL INSERT's column list never includes it, a genuine dead parameter in the original. Confirmed by reading the real INSERT statement directly, not assumed; not ported, rather than perpetuated as a placebo parameter that looks load-bearing but silently does nothing — same "don't replicate an accidental no-op" judgement call this migration has made before (the two deliberately-unreplicated env-var footguns flagged in `ClearPortShipmentPayloadHelper`'s own header comment).
- **A new `NexusPayloadTooLargeException` (413) was added** — the document-upload size guard is the first genuinely-413 case in this migration; every existing exception type maps to a different status, so this is a real gap filled, not a workaround.
- **`AssertOrderEditableAsync` (the Received/Booked completed-order lock, added in 8b.3) was widened from `private` to `internal`** so `InboundShipmentHelper.AssignShipmentAsync` — which links/unlinks a tracked order to a shipment — can reuse the exact same lock rather than re-deriving it; a locked order can't be reassigned to a different shipment either, matching Node's own `assignOrderShipment` calling the identical `assertOrderEditable` guard.
- **`autoFileShipmentPoDocuments`'s best-effort PO-PDF auto-filing is ported now even though it will find nothing to copy until 8b.7 lands** (`GetPoPdfPath`/`SavePoPdf`'s write side don't exist yet) — safe to ship ahead of its real trigger since the read side already degrades gracefully (a missing source PDF is silently skipped, matching Node's own `ENOENT`-swallowed behavior), and `GetPoPdfPath` is exposed `internal` specifically so 8b.7's Create PO in SAP route can reuse the identical path derivation rather than re-deriving it.
- **`LogisticsOptions` gained `ImportRoot`/`PoRoot`**, mirroring `ExportRoot`'s existing "must be a real absolute Windows/UNC path in production" contract and `AppContext.BaseDirectory`-based default exactly — `InboundShipmentHelper.AssertValidRoot` is the same regex guard as `ShipmentHelper.AssertValidExportRoot`, parameterized over which setting name to report in the error.
- **The 20MB upload-size guard, the empty-file guard, and the file-extension allow-list all validate before opening a DB connection** — same `ShipmentCostHelper`/8b.2/8b.3 "validate first, DB second" precedent, directly testable against a mock whose `CreateConnectionAsync` throws if ever actually called; `GetPoPdfPath`'s path-sanitization/misconfigured-root behavior is pure and tested the same way `ForecastMathHelper`'s functions are, with no DB involved at all.
- **Deliberately excludes** (8b.7): Mark Received/Undo Received and their real SAP goods-receipt post/reverse calls; `create-po`/`regenerate-pdf`'s PO-PDF write side (`SavePoPdf`).
- 15 new tests (`InboundCostHelperTests`, `InboundShipmentHelperTests`) — all pure/pre-connection-guard, no DB/SAP needed. Real HTTP smoke test confirms all 17 new routes return 401 unauthenticated with no 404/500; `dotnet build`/`dotnet test` both clean (436 tests, 15 new).

### Sub-phase 8b.5: MRP Analysis forecasting

New `Helpers/Logistics/MrpForecastHelper.cs` (Percentage method preview+save, Sales Breakdown method export/upload/save, snapshot history) + `MrpAnalysisHelper.cs` extended with `GetRefreshStatusAsync`, all wired onto the existing `MrpAnalysisController`. New `Models/Dto/MrpForecastModels.cs`. New `ClosedXML` package reference — the first Excel-handling code in this migration (C# equivalent of Node's ExcelJS), used for both the template export and the upload-side header-matched read; the far larger, live-formula orderbook Excel export (8b.6) is a separate, dedicated-time item — this was the simpler of the two round trips per the Sub-phase 8b research.

- **`POST /forecast/bom/upload` is a real SAP call (`SapServer's MrpAnalysisController.ExplodeBom`) implemented now, not deferred to 8b.7** — a deliberate distinction from that slice's "real SAP writes, sequenced last" scope: BOM explosion is a read (querying SAP's BOM structure, profit centre 2012), not a write, so it carries none of the mutating-SAP-call risk 8b.7 exists to isolate. Matches this migration's own established precedent of implementing SAP reads as they come up (8b.2's Isopar stock fetch, 8e.2's consignment sync) rather than bundling every SAP call — read or write — into one high-scrutiny slice.
- **`POST /refresh` is stubbed 501, matching `/turns-valclass/refresh`'s own 501 stub from 8b.1** — `runMrpHistoryRefresh` (performancesync.js) is a real SAP-pulling sync job, deferred to 8b.6's refresh-orchestration slice; `GET /refresh-status` is implemented for real since it's a pure DB read with no SAP dependency. Unlike performance.js's multi-dataset "no false confidence" refresh-status shape, this one is a single named dataset row (or null) — no failures-array/lastRefreshUtc computation needed, ported as the simpler shape it actually is rather than forcing it into the other routes' pattern.
- **`MrpAnalysisHelper.GetConsumptionByYearAsync` was widened from `private` to `internal` with a new standalone-connection overload**, so the Percentage forecast's preview route can reuse the exact same consumption query 8b.1's Trends route already established, rather than re-deriving it a second time.
- **The annualisation math (`AnnualisationFactor`/`DaysInYear`) and `ApplyPercentage` are pure `internal static` functions**, fully unit-tested — including the specific partial-current-year-baseline behavior that's the whole reason this function exists (annualising a year-to-date consumption total before applying the percentage change, so choosing this year as the baseline doesn't understate the prediction just because the year isn't finished yet) and the always-null `Uom` field in the output (a kept-as-is dead field Node's own `applyPercentage` never actually populates, not a gap in this port).
- **`UploadBomForecastAsync` needs no database at all** — confirmed by reading Node's own route body directly: the upload route is pure Excel-parse + SAP call, with nothing persisted until the separate `/forecast/bom/save` route is called with the previewed data fed back. This made it possible to test the full parse-workbook-then-call-SAP round trip with only a mocked `ISapServerClient`, no DB mock needed at all — real ClosedXML-built `.xlsx` bytes in, asserting on `Products`/`RawMaterials`/`Unresolved` in the result.
- **The two forecast-save routes (`SavePercentageAsync`/`SaveBomForecastAsync`) both call `IAuditLogger.LogAsync`**, matching Node's own `auditQuery('MRP_FORECAST_SAVE', ...)` calls — the first MRP Analysis routes in this migration to write an audit entry, since Trends (8b.1) has nothing to audit and the upload/preview routes don't persist anything.
- **`CreateRunAsync`'s child-row inserts use Dapper's built-in list-parameter multi-exec** (`connection.ExecuteAsync(sql, materials)` runs the INSERT once per item) instead of reproducing Node's `upsertBatch` helper's batching mechanism verbatim — same net effect (every material/product row persisted under the new RunId), simpler in this net10 codebase's toolset than porting a batching abstraction that exists in Node mainly to work around `mssql`'s own parameter-count limits.
- **Deliberately excludes** (8b.6): `runMrpHistoryRefresh` itself; the orderbook Excel export/upload, production-plan print, consignment-customers CRUD, refresh orchestration, and Isopar declarations that make up the rest of Sub-phase 8b.
- 24 new tests (`MrpForecastHelperTests`) — pure math, Excel round-trip with a mocked `ISapServerClient`, and pre-connection validation guards; no live DB/SAP needed. Real HTTP smoke test confirms all 9 new/changed routes return 401 unauthenticated with no 404/500; `dotnet build`/`dotnet test` both clean (460 tests, 24 new).

### Sub-phase 8b.6 (partial): consignment-customers CRUD + printable Production Plan

First slice of 8b.6 — the tractable, DB-only subset scoped out ahead of the harder pieces (live-formula orderbook Excel export/upload + its dual-auth-scheme requirement, refresh orchestration, Isopar declarations — all still pending, see below). Both routes ported straight from `routes/performance.js`'s "Consignment customers" and "Printable Production Plan" sections, with their `performancesql.js` backing queries (`listConsignmentCustomers`/`upsertConsignmentCustomer`/`deleteConsignmentCustomer`/`listOrderBookLineNotes`), onto the existing `PerformanceController`.

- **Consignment customers** (`log.ConsignmentCustomer`) — new `PerformanceDashboardHelper.ListConsignmentCustomersAsync`/`UpsertConsignmentCustomerAsync`/`DeleteConsignmentCustomerAsync`. Read (`GET /consignment-customers`) is any logged-in user, matching Node's loose gating on the rest of this router's orderbook-breakdown routes; write (`PUT`/`DELETE /consignment-customers/{customer}`) is `Perm:LOG_ADMIN`, the same admin-tier code already used elsewhere in this router (`/turns-valclass/aggregates`, `/value-by-price`). `UpsertConsignmentCustomerAsync` validates a non-blank customer number before opening a connection (the established `ShipmentCostHelper` precedent), then does its own `SELECT 1 ... WHERE Customer = @customer` existence check to decide INSERT vs UPDATE — a plain parameterised upsert-via-existence-check, not `MERGE` (SQL Server 2005 compatibility, same reason as every other upsert in this codebase). Both writes call `IAuditLogger.LogAsync` (`CONSIGNMENT_CUSTOMER_SAVE`/`CONSIGNMENT_CUSTOMER_DELETE`), matching Node's own `auditQuery` calls — `PerformanceController` picked up its first `IAuditLogger` constructor dependency for this.
- **Printable Production Plan** (`GET /orderbook-breakdown/production-plan/print`) — new `Helpers/Logistics/ProductionPlanHelper.cs`, split the same way Production's `DrummingTicketHtmlHelper`/`LabelHtmlHelper` split data-fetch from HTML-building: `BuildPlanAsync` fetches `GetOrderBookBreakdownAsync` (already existed, 8b.1) + a new `PerformanceDashboardHelper.ListOrderBookLineNotesAsync` (`log.OrderBookLineNotes`, keyed `"${ReferenceDocument}||${Material}"` matching Node's own Map key) concurrently via `Task.WhenAll`, then hands the raw rows to a pure `BuildPlanFromRows` (filter to `ValueStream == "PTFE"` + `LastDay == "x"` + `WontGet != "x"`, compute quantity/value with the `PlannedProductionQty` override falling back to `OrderQty`, sort by `ParseLastDayTimeMinutes` then raw time text) — split out specifically so the filter/sort/quantity-scaling logic is unit-testable without a DB mock, the same "expose pure logic as internal for testing" precedent used everywhere else in this migration. `BuildHtml` is bare standalone HTML (no app chrome), auto-firing `window.print()` on load exactly like Node's version; `WebUtility.HtmlEncode` for escaping and `DateTime.Now.ToString("dd/MM/yyyy, HH:mm", EnGb)` for the "Generated" timestamp match `DrummingTicketHtmlHelper`'s existing conventions exactly. No further policy beyond this controller's base `[Authorize]`, matching Node (no department/permission gate on this route at all).
- **Number formatting**: quantity uses `"#,##0.###"` (trims trailing zeros, up to 3 decimals) matching JS `toLocaleString('en-GB', {maximumFractionDigits:3})`; value uses `"N2"` (always exactly 2 decimals + thousands separator) matching `{minimumFractionDigits:2, maximumFractionDigits:2}` — same distinction `ForecastMathHelper`/`PurchaseOrderSuggestionHelper.FormatQty` already established between the two JS option shapes.
- **`Risk` is read but never round-tripped from an upload any more** (`OrderBookLineNote.Risk`) — kept only because Node's own `listOrderBookLineNotes` still returns it; the Data-sheet's Risk Value is calculated fresh on every export now, not read back from a prior upload. Not consumed by the production-plan print (which only reads `LastDay`/`WontGet`/`LastDayTime`/`PlannedProductionQty`), carried in the DTO purely for parity with the export route 8b.6's next slice will build.
- **Deliberately still excludes** (rest of 8b.6): the live-formula orderbook Excel export/upload (`GET /orderbook-breakdown/export`, `POST /orderbook-breakdown/upload-notes` — the latter needs a new dual-auth-scheme policy, session cookie OR a short-lived API bearer token minted by a not-yet-ported `POST /api/auth/orderbook-token`, before it can be built at all); Isopar declarations + `checkIsoparDeclarationDue`.
- 22 new tests (`ProductionPlanHelperTests` + one pre-connection guard added to `PerformanceDashboardHelperTests`) — `BuildPlanFromRows`/`ParseLastDayTimeMinutes`/`BuildHtml` fully exercised pure, no DB needed. Real HTTP smoke test confirms all 4 new routes return 401 unauthenticated with no 404/500; `dotnet build`/`dotnet test` both clean (482 tests total, 22 new).

### Sub-phase 8b.6 (continued): refresh orchestration

The largest piece of 8b.6 — the real SAP-pulling sync engine behind every "Refresh Now" button and cron job across Purchasing/Performance/MRP Analysis. Port of `routes/performancesync.js` (`runFullRefresh`/`runTurnsValClassRefresh`/`runMrpHistoryRefresh`) + its three small algorithm-module dependencies (`performanceallocation.js`, `performanceorderlink.js`, `performancevaluestream.js`) + `performanceforecast.js`'s seasonal-index predicted-usage math + the ~15 DB write functions in `performancesql.js` this all depends on. Replaces the `POST /turns-valclass/refresh` and `POST /api/mrp-analysis/refresh` 501 stubs from 8b.1/8b.5 with real implementations, and adds the previously-missing `POST /api/performance/refresh` route.

- **New DTOs** (`Models/Dto/PerformanceSyncModels.cs`) mirror SapServer's raw response shapes field-for-field: `SapPerformanceStockRow` (renamed from a first-draft `SapStockRow` — collided with an unrelated Warehouse LQUA DTO of that exact name), `SapAgreementRow`, `SapVbfaOrderLinkRow`, `SapInvoiceRow`, `SapOtifRow`, `SapTurnsValClassRow`, `SapValuationClassRow`, `SapConsumptionByYearRow`, `SapGoodsReceiptHistoryRow`. Unlike every other DTO in this migration, several of these are **mutable classes, not records** — `SapPerformanceStockRow`/`SapAgreementRow`/`SapInvoiceRow`/`SapOtifRow` all get a `ValueStream` property stamped onto them after the SAP pull, and `SapAgreementRow` additionally gets `OriginalDoc`/`OriginalDocItem` (delivery→order resolution) and `DockStockAllocated`/`PickedStockAllocated` (FIFO allocation) mutated in across three sequential enrichment passes — matching Node's own plain-object-mutation pipeline (`allocateStock`/`resolveDeliveryReferenceDocuments`/`enrichWithValueStream` all mutate rows in place) rather than fighting it with an immutable-record redesign.
- **`SnapshotTableWriter.cs`** — a new generic `ReplaceAsync<T>`/`UpsertAsync<T>` engine, C# port of `performancesql.js`'s `replaceTable()`/`upsertBatch()` column-metadata-driven helpers (a declarative `(ColumnName, ValueSelector, MaxLen)` list per table, the same parameterised UNION-ALL-SELECT staging idiom, the same `Math.Max(1, 2000/columnCount)` batch-size math, no explicit transactions — SQL Server 2005 has no MERGE and this codebase's established "TRUNCATE+batched-INSERT via DynamicParameters" idiom, already used once for `ConsignmentTrackerHelper.ReplaceStockSnapshotAsync`, generalised here since 6 different tables need it (one of them, `log.TurnsValClassSnapshot`, has ~85 columns — hand-writing that INSERT would have swamped the file). `UpsertAsync` additionally supports `insertOnly` (skips the UPDATE branch entirely — used to freeze `log.ForecastAccuracyLog`'s current-month forecast at whatever it was on the first sync after the month started, and for `log.DeliveryOrderLink`'s pure insert-if-missing cache).
- **`PerformanceSnapshotHelper.cs`** — every DB write `performancesync.js` depends on, built on `SnapshotTableWriter`: `ReplaceStockSnapshotAsync`/`ReplaceAgreementSnapshotAsync`/`ReplaceInvoiceSnapshotAsync`/`ReplaceOtifSnapshotAsync` (each filters out rows with no `ValueStream` mapping first, matching `replaceTable()`'s own "only for tables that carry that column" filter), `DedupeTurnsValClassRows`+`ReplaceTurnsValClassSnapshotAsync` (merges split-valuation-type SAP rows for the same Material+Plant, recomputing `UnitPrice` from the summed totals — `ConsignmentQty` deliberately NOT summed, left as the first-seen row's value, since MKOL isn't split by valuation type the way MBEW is), `ReplaceValuationClassCatalogAsync`, `UpsertStockValuationHistoryAsync`/`UpsertForecastAccuracyLogAsync` (both append-only, never truncated), `UpsertMaterialConsumptionHistoryAsync`/`UpsertMaterialReceiptHistoryAsync` (the latter resolves `VendorId` from `SapVendorNumber` via one shared `log.Vendor` lookup per batch, left `null` when no matching vendor exists yet — row kept, not dropped), `ListRohMaterialsAsync`, `StartRefreshAsync`/`CompleteRefreshAsync`/`FailRefreshAsync` (`dbo.RefreshLog`, **Nexus** database — everything else in this file is NexusOps, so every sync method needs both `INexusDb` and `INexusOperationsDb` connections open at once), `RecomputeDailyInvoicedAsync`/`RecomputeDailyOtifAsync`/`UpsertTodayStockAndPickedAsync` (`log.DailyPerformance`, the shared daily fact table three different syncs write into), `GetCachedDeliveryOrderLinksAsync`/`InsertDeliveryOrderLinksIfMissingAsync` (`log.DeliveryOrderLink`, defensively deduped by (DeliveryNumber,DeliveryItem) before insert — VBFA can occasionally surface more than one predecessor row for the same delivery item within one batch).
- **`StockAllocationHelper.cs`** — pure port of `performanceallocation.js`'s FIFO waterfall: `PackagingCustomer` (regex-extracts the customer token from an `IB_CUSTOMER_...` packaging material name — **note this is a completely different "customer" concept from `AgreementRow.Customer`**; the two only pool together when a packaging-material naming convention happens to embed the same customer code, confirmed directly against Node's own two independently-keyed pools), `StagingBin` (left-pads a reference document to 10 chars — the real staging-bin naming convention), `AllocateStock` (two independent pools — available dock stock keyed by material+packaging-customer, staged/picked stock keyed by material+storage-bin — walked oldest-`RequestDate`-first, same `Math.Min(orderQty, remainingPool)` waterfall as Node).
- **`ValueStreamHelper.cs`** — pure port of `performancevaluestream.js`: the full 27-entry `profitCentre -> {PTFE,PV}` lookup table transcribed verbatim, `MapProfitCentreToValueStream` (strips leading zeros first — SAP sends PRCTR either bare or zero-padded to 10), `EnrichWithValueStream` overloads per row type, `ComputeTodayStockAndPickedTotals` (implied unit price = `LocalAmount/OrderQty` per allocated row, bucketed by value stream — an unmapped row buckets under `"UNKNOWN"`, matching Node's fallback key exactly).
- **`PredictedUsageHelper.cs`** — pure port of `performanceforecast.js`'s seasonal-index-decomposition demand forecast (documented in detail in the source file's own header comment, transcribed near-verbatim into the C# XML doc since the algorithm's *why* — filling the 2+ month gap between SAP's own short-visibility demand forecast and this app's ~4-month supplier lead time — doesn't change in translation). `MAX_SEASONAL_INDEX = 4` clamp and `Math.Max(0, ...)` floor both preserved exactly; fully unit-tested including the clamp and the year-weighting (3x/2x/1x, most-recent-first).
- **`DeliveryOrderLinkHelper.cs`** — port of `performanceorderlink.js`'s `resolveDeliveryReferenceDocuments`: stamps a pass-through `OriginalDoc`/`OriginalDocItem` default on every agreement row first, then VBFA-resolves only the delivery-shaped ones (`ReferenceDocument` starting `"008"`) not already cached in `log.DeliveryOrderLink` — modest concurrency (5 at a time via chunked `Task.WhenAll`, not one unbounded fan-out) since this can mean dozens of individual RFC round-trips in one sync cycle. A VBFA lookup failure is caught per-delivery and skipped (best-effort — see the class's own header comment for why: a stale delivery number until the next sync is a much smaller problem than aborting the whole sync).
- **`PerformanceSyncHelper.cs`** — the orchestration itself: `RunFullRefreshAsync` (Stock/Agreements/Invoicing/Otif, the 30-min cron + Management page's manual refresh — **no shared-in-flight-run guard**, matching Node's own `runFullRefresh` exactly, which has none either, unlike the two below), `RunTurnsValClassRefreshAsync` and `RunMrpHistoryRefreshAsync` (each guarded by a static shared `Task`, C# port of Node's module-level `let xRefreshPromise = null` singleton pattern — a second concurrent caller gets the same in-flight `Task` instead of racing the first, which matters here specifically because `SnapshotTableWriter.ReplaceAsync`'s TRUNCATE+batched-INSERT has no transaction/lock of its own). Every SAP fetch goes through `ISapServerClient.GetAsync` with `longRunning: true` (Node's own `axios.create({timeout: 10*60*1000})` applies uniformly to every one of these calls, not just the historically-slow ones). `userId` is the authenticated caller triggering a manual refresh, not a true system/cron identity — same "no Quartz.NET job exists yet" gap as 8e.2's `RunDailySyncAsync`, not a new one.
- **Controllers**: `PerformanceController` gained `ISapServerClient` + a new `POST /refresh` action (Node's route existed but had no C# counterpart until now — `requireLogin`-only, no extra policy); `/turns-valclass/refresh`'s 501 stub replaced with a real call. `MrpAnalysisController`'s `/refresh` 501 stub replaced with a real call (it already had `ISapServerClient` injected from 8b.5's BOM-explosion route).
- **Not a new architecture, but worth flagging**: this is the first Helper file pairing in the migration built around *mutable* DTOs threaded through multiple sequential enrichment passes rather than one Helper method computing a single output from immutable inputs — a deliberate divergence from this migration's usual "pure `internal static` functions over records" style, chosen because the underlying algorithm genuinely is a multi-pass mutation pipeline in Node and reshaping it into an immutable-record pipeline would have obscured the 1:1 correspondence with the original (and made "does this still do what Node does" much harder to verify by inspection).
- 40 new tests (`ValueStreamHelperTests`, `PredictedUsageHelperTests`, `StockAllocationHelperTests`) — every pure algorithm piece (allocation waterfall, value-stream mapping, seasonal-index forecast) fully exercised with no DB/SAP dependency; the DB-write layer (`SnapshotTableWriter`/`PerformanceSnapshotHelper`) and the orchestration layer (`PerformanceSyncHelper`) are not directly unit-tested — consistent with this migration's established precedent of not unit-testing raw Dapper query/write internals, only pre-connection guards and pure logic (see 8b.1-8b.5's own Helper files, none of which test their SQL-executing methods directly either). Real HTTP smoke test confirms all 3 new/changed routes (`POST /api/performance/refresh`, `POST /api/performance/turns-valclass/refresh`, `POST /api/mrp-analysis/refresh`) return 401 unauthenticated with no 404/500; `dotnet build`/`dotnet test` both clean (515 tests total, 33 new — one initial test-data mistake in `StockAllocationHelperTests` self-caught and fixed: forgot to set a matching `IB_<customer>_` packaging material alongside the agreement's `Customer`, so the dock-stock pool never matched and every allocation came back zero).
- **Unverified** (same class of gap as everywhere else in this migration touching real SAP data): the exact shape of SapServer's `/api/performance/*`/`/api/mrp-analysis/*` JSON responses was confirmed by reading the real SapServer C# source directly (not by hitting a live endpoint — no live SAP or SapServer instance in this environment), so a genuine field-name mismatch would only surface against a real deploy. The `ISapServerClient` JSON-deserialization case-insensitivity this relies on is already proven working elsewhere in this migration (e.g. `ConsignmentGrRow`), so this is a much narrower risk than the mechanism itself.

### Sub-phase 8b.6 (continued): HMRC Tied Oil declarations + checkIsoparDeclarationDue

Isopar Tied Oil's meter-reading/planning-rate/stock-risk routes were already ported in 8b.2 — this slice is just the "Declarations" section deferred at the time (`routes/performance.js`'s `/isopar/declarations*` routes + `checkIsoparDeclarationDue`), gated `ISOPAR_DECL` rather than the rest of Isopar planning's `LOG_MRP` (only whoever actually files the HMRC return needs to see/confirm a declaration).

- **New `Helpers/Logistics/IsoparDeclarationHelper.cs`**: `GetReadingOnOrBeforeAsync`/`ListReceivedDeliveriesInRangeAsync`/`ComputePeriodFiguresAsync` (shared by the live outstanding-period preview and the frozen submit path, so the two can never drift apart — `Complete: false`, meaning a meter reading is missing for the opening or closing date, blocks submission), `ListDeclarationsAsync`/`ListOutstandingPeriodsAsync` (diffs `IsoparPeriodHelper.IsoparPeriodsEndedBefore` — already existed from 8b.1 — against already-declared `(PeriodStart,PeriodEnd)` pairs in memory, rather than one query per period), `CreateDeclarationAsync` (re-validates the period is genuinely outstanding and re-computes figures fresh at submit time rather than trusting whatever the client's preview showed — closes the gap between "what the preview displayed" and "what's persisted" if a reading changes in between), `CheckDeclarationDueAsync`.
- **`CheckDeclarationDueAsync` is the cron entry point — ported as a callable Helper method only, same as 8e.2's `RunDailySyncAsync` and this sub-phase's own `PerformanceSyncHelper` refresh functions**: unlike those three, though, Node itself has **no manual-trigger HTTP route for this one either** (`checkIsoparDeclarationDue` is called only from `server.js`'s own cron registration) — so there's genuinely nothing to wire up controller-side yet, not even a "Refresh Now"-style button, until Phase 10's Quartz.NET job scheduling exists. Self-healing/idempotent rather than exact-day-gated: checks `dbo.Notifications` for a title naming the specific period's end date before sending, so a missed/late run just catches up the next day instead of silently skipping a quarter — same insert-once philosophy as `PerformanceSnapshotHelper.UpsertForecastAccuracyLogAsync`'s current-month freeze (8b.6's refresh-orchestration slice, above). Only the oldest outstanding period is ever notified per run — if several have piled up, one clear notification beats a flood of near-duplicates.
- **First real caller of `INotificationService` in Logistics** (the interface itself already existed from Foundation/Phase 1, and Warehouse's `StagingHelper` was already using it — not a new architecture, just a new consumer). `Target: new NotificationTarget(NotificationTargetType.Permission, "ISOPAR_DECL")` — the C# `NotificationService`'s permission-target fan-out is a **deliberate improvement over Node's own** `notify()` (documented on `NotificationService.FanOutAsync`'s own comment): it additionally reaches anyone who holds `ISOPAR_DECL` only via a permission GROUP, a mechanism Node has no equivalent of, whereas Node's own fan-out only checks the flat direct-grant table.
- **New DTOs** (`Models/Dto/IsoparModels.cs`, extending 8b.2's file rather than a new one — same table family): `IsoparReceivedDeliveryRow`, `IsoparPeriodFigures`, `IsoparDeclarationRow`, `IsoparOutstandingPeriod`, `IsoparCurrentPeriodPreviewResult`, `CreateIsoparDeclarationRequest`/`Result`, `IsoparDeclarationDueCheckResult`.
- **Controller**: `PerformanceController` gained `GET /isopar/declarations`, `GET /isopar/declarations/outstanding`, `GET /isopar/declarations/current-period-preview`, `POST /isopar/declarations` — all `Perm:ISOPAR_DECL`.
- **Deliberately still excludes** (the last piece of 8b.6, done in the next slice below): the dual-auth-scheme upload-notes route and the live-formula orderbook Excel export.
- 2 new tests (`IsoparDeclarationHelperTests`) — `CreateDeclarationAsync`'s pre-connection null-period guard only; the rest of this Helper is DB-read-driven with no meaningfully pure logic to isolate (same precedent as `PerformanceSyncHelper`/`PerformanceSnapshotHelper` above — not directly unit-tested). Real HTTP smoke test confirms all 4 new routes return 401 unauthenticated with no 404/500; `dotnet build`/`dotnet test` both clean (517 tests total, 2 new).

### Sub-phase 8b.6 (final): dual-auth upload-notes route + orderbook-token credential exchange

The last piece of Sub-phase 8b.6 — everything except the live-formula Excel export itself (see "Still deliberately deferred" below), which stays out of scope for this migration for now. Port of `middleware/auth.js`'s `requireSessionOrApiToken`, `routes/auth.js`'s `POST /api/auth/orderbook-token`, and `routes/performance.js`'s `POST /orderbook-breakdown/upload-notes` + its `performancesql.js` backing `upsertOrderBookLineNotes`.

- **New second authentication scheme, `OrderbookBearerScheme`** — registered in `Program.cs` via `AddJwtBearer(OrderbookBearerScheme.Name, ...)` alongside the existing cookie scheme (`NexusAuthScheme`), validating issuer `kongsberg-portal`/audience `orderbook-notes-upload` against the exact same secret `SapServerClient` already signs with (`SapServerOptions.JwtSecret` — Node's own `sapServerSecret`, shared with SapServer, scoped apart only by issuer/audience so a token minted for one can never be replayed against the other). Needed adding the `Microsoft.AspNetCore.Authentication.JwtBearer` NuGet package explicitly — unlike `Authentication.Cookies`, it doesn't ship as part of the ASP.NET Core shared framework even under `Microsoft.NET.Sdk.Web`. This scheme is registered but never made the default challenge/forbid scheme, so it has zero effect anywhere else in the app unless a controller action names it explicitly.
- **`PerformanceController.UploadOrderBookLineNotes`** is the one action in the whole migration so far with `[Authorize(AuthenticationSchemes = $"{NexusAuthScheme.Name},{OrderbookBearerScheme.Name}")]` overriding `NexusControllerBase`'s cookie-only default — listing two schemes on one `[Authorize]` is an OR in ASP.NET Core (either succeeding authenticates the request), the direct C# equivalent of `requireSessionOrApiToken`'s "session cookie OR bearer token" check. No further permission gate on this route, matching Node exactly (no `requirePermission` call on the real route either).
- **`OrderbookTokenService.CreateToken`** mints the bearer token with `ClaimTypes.NameIdentifier`/`ClaimTypes.Name` claims — not the custom `"userId"`/`"username"` claim names Node's own JWT payload uses — specifically so `NexusControllerBase.GetUserId()`/`GetUsername()` resolve identically regardless of which of the two schemes actually authenticated a given request, without either controller action or base class needing to know which one did.
- **`IAuthService.VerifyOrderbookCredentialsAsync`** is a deliberately narrower sibling of `LoginAsync`, not a call into it — matching Node's own `orderbook-token` route exactly: no `FailedLogins` increment or lockout escalation on a bad password (Node's route has no counter-increment logic at all, unlike `/login`), no department/permission resolution, no session ticket — just a yes/no over username+password plus the account being active/unlocked. Reuses `LoginAsync`'s exact dummy-bcrypt-compare-on-unknown-username defense and audit action-name convention (`ORDERBOOK_TOKEN_FAIL`/`ORDERBOOK_TOKEN_OK`, mirroring `LOGIN_FAIL`/`LOGIN_OK`).
- **`POST /api/auth/orderbook-token`** is a minimal-API endpoint in `Program.cs` (next to the existing `/Logout` minimal-API route), not a Razor Page or MVC controller action — matches Node's own "single stateless JSON request/response, no session at all" framing exactly, deliberately separate from the real `/Login` page's ticket-issuing flow. Rate-limited via a new `RateLimitPolicies.OrderbookToken` policy — same 10-attempts/15-minutes/IP shape as `Login`, but a genuinely separate counter bucket, matching Node registering its own independent `rateLimit()` instance for this route rather than reusing `/login`'s.
- **New `Helpers/Logistics/OrderBookNotesUploadHelper.cs`**: `ParseWorkbook` (pure — no DB, no audit, so the header-matching/merge logic is unit-testable without a DB mock, the same "expose pure logic as internal for testing" precedent as `MrpForecastHelper`'s BOM upload parser) reads the "Data" sheet (Reason/Won't Get/Last Day/Last Day Time/Expected to Invoice Qty, keyed by Order+Material) then the optional "Next Month" sheet (only the Bring Forward confirm flag — merged into an existing Data-sheet note or creating a new one for an order/material combo Data never saw), matched by header text via ClosedXML exactly like `MrpForecastHelper.ParseBomUploadWorkbook`; `UploadNotesAsync` persists via `SnapshotTableWriter.UpsertAsync` (reused from the refresh-orchestration slice, above) and audits `ORDERBOOK_NOTES_UPLOAD`. `Risk` is always written `null` on every upload — confirmed from Node's own `upsertOrderBookLineNotes`, which never round-trips a Risk value from the upload payload at all (see `OrderBookLineNote.Risk`'s own comment from earlier in 8b.6: the Data sheet's Risk Value is calculated fresh on every export now).
- **Still deliberately deferred**: the live-formula orderbook Excel export itself (`GET /orderbook-breakdown/export`) — the genuinely hardest remaining piece of this whole sub-phase, flagged from the start of 8b.6's scoping for its own dedicated pass given ClosedXML has no direct equivalent workflow to Node's ExcelJS live-formula-cell approach proven anywhere yet in this migration. Building the upload side first was still worthwhile on its own: it's independently useful (the dual-auth architecture and the note-persistence logic are both now real and tested) and the exact header/sheet contract it parses against is fixed by Node's real export code, confirmed by reading that source directly, so nothing here depends on the export existing yet.
- 8 new tests (`OrderBookNotesUploadHelperTests`) — `ParseWorkbook` fully exercised pure (missing Data sheet, missing Order/Material, blank-cell-to-null, Next Month merge onto an existing note, Next Month creating a new note, missing Next Month sheet ignored). Real HTTP smoke test: `POST /orderbook-breakdown/upload-notes` returns 401 unauthenticated with no 404/500; `POST /api/auth/orderbook-token` (deliberately `[AllowAnonymous]`, matching Node) returns 400 for a missing username/password without touching the DB, confirming routing/DI wiring — a request with a real username/password reaches real credential-check code that needs a live SQL Server this sandbox doesn't have, the same "unverified without a live DB" gap already flagged throughout this migration, not a defect in this slice. `dotnet build`/`dotnet test` both clean (525 tests total, 8 new).

**Sub-phase 8b.6 is now fully done** except for the live-formula orderbook Excel export, which remains out of scope for this migration pass.

### Sub-phase 8b.7 (first slice): Mark Received / Undo Received — real, non-elevated SAP goods-receipt writes

First of 8b.7's two real-SAP-write pieces (the other, `create-po`/`regenerate-pdf`, needs a per-user-elevated SAP session and a new `PurchaseOrderPdfHelper` rebuilding `lib/poPdf.js`'s legal T&Cs text — flagged as the single highest-scrutiny route in this sub-phase and deliberately tackled separately). Port of `routes/performance.js`'s Inbound Log "Mark Received"/"Undo Received" actions (`postGoodsReceiptToSap`/`reverseGoodsReceiptToSap` + the routes themselves) and their `performancesql.js` backing `markShipmentReceived`/`undoShipmentReceived`, both of which post directly against SapServer's existing `PurchasingController.PostGoodsReceipt`/`ReverseGoodsReceipt` (MB01/MBST BDCs) — no new SapServer work needed, unlike `create-po`.

- **Non-elevated, unlike Create PO in SAP**: these run on SapServer's shared service-worker pool (a valid JWT checked against `dbo.SapDepartmentPermissions` for function code `MB01`, not the calling user's own SAP credentials) — MB01/MBST don't need per-user SAP authorization the way BAPI_PO_CREATE1 does, so this slice carries none of `create-po`'s elevated-session risk.
- **`InboundShipmentHelper.MarkReceivedAsync`** — stamps the shipment received, bulk-flips every linked non-cancelled order to `'Received'`, and posts each line's goods receipt to SAP. A single line's SAP failure does **not** stop the others or block the shipment being marked received — `'Received'` means "physically arrived and logged", independent of whether the SAP posting itself succeeded; the per-line outcome (`SapMaterialDocument`/`SapGrError`/`SapGrSkipped`) is stamped onto the order row so a failure can be seen and retried without re-doing the whole receive. A freshly-typed confirmed quantity is entered in the vendor's own order unit (`log.Vendor.OrderMoqUom`) and converted back to the material's SAP base unit via the existing `UnitConversionHelper.ConvertQty` (8b.1) before being stored — the no-input default (whole order assumed received) is already in the base unit, so nothing to convert there. Every line must end up with a non-blank `SupplierReference` (existing-on-file, or freshly supplied) before any SAP call is attempted, since that's what's posted to SAP as RM07M-LFSNR — a blank one throws `NexusValidationException` naming the material, exactly matching Node's own validation message.
- **`PostGoodsReceiptToSapAsync`** (widened to `internal` specifically for testability, same "expose pure-ish guard logic" precedent used throughout this migration) skips the SAP call entirely — reporting `Skipped: true` with an explanatory `Error` rather than attempting anything — for a line with no PO number on file, a PO number but no PO item number, or a confirmed-zero received quantity (SapServer's own `GoodsReceiptHelper.BuildGoodsReceiptRequest` treats a `Quantity` of exactly 0 identically to "not supplied," which would silently fall back to SAP's XFULL default and book the *entire* outstanding PO quantity — the opposite of what a confirmed 0 means, so it's caught here first). `PoItemNumber` is stored as the padded SAP item string (`"00010"`, `"00020"`, ...) — divided by 10 to recover the 1-based `LineNumber` `GoodsReceiptRequest` expects, matching the same convention `create-po`'s own PO-item numbering already established (8b.3).
- **`InboundShipmentHelper.UndoReceivedAsync`** reverses each line's posted SAP material document via MBST, clearing `ReceivedQty`/`SapMaterialDocument`/`SapGrError`/`SapGrSkipped` and reverting `Status` back to `'Ordered'` only for lines whose reversal actually succeeds — a line whose SAP reversal fails is left completely alone (still `Booked`/`Received`, only `SapGrError` updated) rather than force-clearing the portal's record of something still genuinely posted in SAP, and is safe to retry later (an already-cleared line has no `SapMaterialDocument` and is skipped straight to "nothing to reverse"). The shipment's own `ReceivedAtUtc`/`ReceivedBy` are only cleared once every line is confirmed clear (`stillPostedCount == 0`) — never leaving the Inbound Log showing a shipment as "not received" while SAP still disagrees.
- **New DTOs** (`Models/Dto/InboundShipmentModels.cs`): `GoodsReceiptRequest` (mirrors SapServer's field-for-field), `MarkShipmentReceivedRequest`/`Result`, `UndoShipmentReceivedRequest`/`Result`, `SapGrOutcome` (`Success` is `true` for both a real success and a deliberate skip, matching what's actually persisted — `SapGrError` is only ever set when `Success` is `false`).
- **Controller**: `PerformanceController` gained `POST /order-suggestions/shipments/{shipmentId}/receive` (`Perm:LOG_MRP,WAREHOUSE_OP` — also reachable by a warehouse operator, the actual "GR feature" `wdMarkInboundReceived` uses, matching Node's `requireAnyPermission` exactly) and `POST .../undo-receive` (`Perm:LOG_MRP` only), the latter auditing `INBOUND_SHIPMENT_UNDO_RECEIVE` with the same reversed/still-posted-count message Node's own `auditQuery` call builds.
- **`AutoFileShipmentPoDocumentsAsync`'s best-effort PO-PDF auto-filing (8b.4) still has nothing to copy in this slice** — `GetPoPdfPath`'s write side (`SavePoPdfAsync`) is added by the next slice below (`create-po`), not this one.
- 5 new tests (`InboundShipmentReceivingHelperTests`) — `PostGoodsReceiptToSapAsync`'s three skip-without-calling-SAP guards (no PO, no PO item, zero quantity) against a `Mock<ISapServerClient>` that throws if ever actually invoked, plus a real success and a BDC `Type: "E"` failure case. `MarkReceivedAsync`/`UndoReceivedAsync` themselves are DB+SAP orchestration with no DB mock built for them — consistent with this migration's established precedent (8b.6's `PerformanceSyncHelper`/`PerformanceSnapshotHelper`, none of which have direct DB-mock tests either). Real HTTP smoke test confirms both new routes return 401 unauthenticated with no 404/500; `dotnet build`/`dotnet test` both clean (530 tests total, 5 new).
- **Unverified** (same class of gap as every real-SAP-write route in this migration): confirmed field-for-field against SapServer's real `GoodsReceiptRequest`/`BdcResponse`/`Mf41Request` C# source directly, but never exercised against a live SAP system from this environment (no SAP or SapServer instance reachable here) — the `Type is "E" or "A"` failure-detection convention and the LineNumber/10 PO-item-number math are both already proven in this codebase elsewhere (`ShipmentCostSapPostingHelper`, `create-po`'s own PO-item numbering), so the risk here is narrower than a wholly new SAP integration would carry.

### Sub-phase 8b.7 (final): create-po / regenerate-pdf — elevated PO creation + PO PDF

The single highest-scrutiny route in the entire Sub-phase 8b, tackled last on purpose. Port of `routes/performance.js`'s `POST /order-suggestions/create-po` (raises a real PO in SAP under the calling user's own SAP credentials, via `BAPI_PO_CREATE1`) and `POST /order-suggestions/regenerate-pdf` (re-renders the PO PDF for an already-created PO, no SAP write) + `lib/poPdf.js`'s `buildPoPdf`/`savePoPdf`. Completes Sub-phase 8b (Purchasing/Performance) in full.

- **New `Helpers/Logistics/PurchaseOrderCreationHelper.cs`** — `CreatePoAsync` re-validates everything fresh from the DB before any SAP call (never trusts whatever the client had on screen): every selected line still exists, is still `Accepted` with no `PoNumber` yet, all share one `VendorId` (one PO per vendor), the vendor has a `SapVendorNumber` and a currency. Decrypts the calling user's own SAP credentials via `ISapCredentialCipher` (same `dbo.PortalUsers.SapUsername`/`SapPasswordEncrypted` lookup pattern already established by 8a.5b's `ShipmentCostSapPostingHelper`) and posts to SapServer's existing elevated `POST /api/purchasing/create-po-elevated` — no new SapServer work needed. On success, every selected line is flipped to `Status: Ordered` with the returned `PoNumber` and an x10-numbered `PoItemNumber` computed from the row's own position in the same array the SAP request was built from (SAP's `BAPI_PO_CREATE1` only ever returns the overall PO number, never per-item numbers, so this positional convention is the only way to recover them — already proven correct elsewhere in this codebase, e.g. `PurchaseOrderSuggestionHelper`'s own PO-item numbering).
- **PDF generation is deliberately non-blocking** — the PO already exists for real in SAP by the time the PDF step runs, so a PDF/filesystem failure must never make the whole request look like it failed; it's caught and surfaced as `PoPdfError` in the response instead (`PoPdfSaved: false`), matching Node's own try/catch around `buildPoPdf`/`savePoPdf` exactly.
- **`BuildPoPdfItems`** (widened to `internal` for testability) is the shared shape-builder both `CreatePoAsync` and `RegeneratePdfAsync` funnel through, so a reprint months later never drifts from the PDF generated at creation time for the same PO: quantity/unit conversion via the existing `UnitConversionHelper.ConvertQty` (8b.1), an EXW-aware delivery date (goods collected from the vendor's own site use `ReadyToCollectDate`, not `DeliveryDate` — only when *every* line on the PO is EXW, since one PO is always one vendor/one incoterm), and a price fallback chain (manual override → SAP-queried price → `null`, which the PDF itself renders as "Per SAP condition"). The SAP-queried price map is keyed by a bare integer string (`"10"`, not `"00010"`) — confirmed from SapServer's own `PurchasingHelper.ParsePoPrices` comment: POCOND's real `ITM_NUMBER` field is 6 digits, not this app's 5-digit x10 numbering, so both sides normalise to a plain integer rather than relying on matching padding widths.
- **`QueryPoPricesFromSapAsync`/`GetUserDisplayNameAsync`** are both best-effort, swallowing any failure and returning an empty map / `null` respectively — a price-lookup or display-name hiccup must never block PDF generation, matching Node's own try/catch-and-log-only handling for both.
- **New `Helpers/Logistics/PurchaseOrderPdfHelper.cs`** renders the actual document via QuestPDF — same deliberate deviation from Node's pdfkit-based fixed-x/y-coordinate original already established by `ShipmentPackingListPdfHelper`/Production's `LabelPdfHelper`: QuestPDF's declarative `Row`/`Column`/`Table` layout reflows its own pagination, so none of Node's manual "is `y` past the page height yet" checks are needed here at all. The Terms &amp; Conditions and supplier-certification notice text are transcribed **verbatim** from `lib/poPdf.js`'s own constants — both are binding legal/compliance wording copied from a real SAP-issued PO, not paraphrased, and must never be reworded independently of that source. The Kongsberg logo Node conditionally draws is omitted entirely (purely cosmetic, best-effort in Node too, and the logo asset was never copied into this project's `wwwroot`) — not pixel-verified against Node's output, same "no PDF viewer in this sandbox" caveat as every other generated-document Helper in this migration.
- **`InboundShipmentHelper` gained `SavePoPdfAsync`** — writes the freshly-built PDF bytes to `GetPoPdfPath`'s location (creating the vendor subfolder if needed), the write-side counterpart to the read-only `GetPoPdfPath` that's existed since 8b.4. This is also what finally gives `AutoFileShipmentPoDocumentsAsync` (8b.4) something real to copy — a shipment created after a PO now has a real PDF sitting at the path it already knew to look for.
- **New DTOs** (`Models/Dto/PurchaseOrderCreationModels.cs`): `PriceOverride`, `CreatePoRequest`/`Result`, `RegeneratePoPdfRequest`/`Result`, `SapPoCreateElevatedItem`/`Request`/`Response` (mirrors SapServer's `PoCreateItem`/`CreatePoElevatedRequest`/`Response` — only the 6 fields the MRP order-suggestion flow ever actually sends; `AcctAssCat`/`MaterialGroup`/`GlAccount`/`CostCenterOrOrder` are for expense-type lines, not applicable to a raw-material stock PO). `PurchaseOrderSuggestionHelper` gained `ListByPoNumberAsync` (backs `regenerate-pdf`'s "already exists" lookup — a narrower query than `ListTrackedAsync`'s own, reusing the same `OrderSuggestionTrackedRow` type since Dapper leaves the shipment/schedule-agreement columns it doesn't select at their default).
- **Controller**: `PerformanceController` gained `ISapCredentialCipher` and two new actions, `POST /order-suggestions/create-po` / `POST /order-suggestions/regenerate-pdf`, both `Perm:LOG_MRP`.
- 11 new tests (`PurchaseOrderCreationHelperTests`) — `BuildPoPdfItems` fully exercised pure (x10 PO-item-number assignment, unit conversion, override-vs-SAP-price precedence, the bare-integer price-key normalisation, EXW-vs-non-EXW delivery date), plus 3 structural `PurchaseOrderPdfHelper.BuildPoPdf` tests (priced, unpriced, zero-item — real PDF bytes checked for the `%PDF` magic header and a sane length, same precedent as `ShipmentPackingListPdfHelperTests`). `CreatePoAsync`/`RegeneratePdfAsync` themselves are DB+SAP orchestration with no DB mock built, consistent with every other real-SAP-write Helper in this migration. Real HTTP smoke test confirms both new routes return 401 unauthenticated with no 404/500; `dotnet build`/`dotnet test` both clean (541 tests total, 11 new).
- **Unverified** (same class of gap as 8b.7's Mark Received/Undo Received slice, and the highest-stakes one in this whole migration by the user's own original risk assessment): the elevated create-po-elevated round trip, the PO-item x10 numbering actually matching what `BAPI_PO_CREATE1` assigns, and the PDF's legal text/layout have never been exercised against a live SAP system or opened in a real PDF viewer from this environment. Validate a real Create PO in SAP end-to-end — including opening the generated PDF — before trusting this route in production.

**Sub-phase 8b (Purchasing/Performance) is now fully done** apart from the live-formula orderbook Excel export, which remains intentionally out of scope for this migration pass (flagged in 8b.6 above).

### Sub-phase 8c.1: Customs Report admin (VAT overrides + HS descriptions)

First slice of Sub-phase 8c (Customs) — the last remaining Logistics area. Port of `routes/customsreportadmin.js` in full: simple CRUD over two small reference/fallback tables backing the French VAT/DDP Customs Report tile (`routes/customsreport.js`, Sub-phase 8c.2, not yet built). Deliberately the lowest-risk slice of this sub-phase, same "smallest/simplest first" ordering this migration has followed for every phase so far.

- **`log.CustomsVatNumberOverrides`/`log.CustomsHsCodeDescriptions` already exist in NexusOperations** — migrated independently via `migrations/nexus_operations/20260811090000_add_customs_report_tables.cjs` (confirmed schema: `OverrideId`/`ConsigneeCode` NVARCHAR(10) UNIQUE/`VatNumber` NVARCHAR(20)/`Notes` NVARCHAR(200) nullable/`CreatedBy`/`CreatedAtUtc`/`UpdatedAtUtc` nullable; `HsCodeId`/`CommodityCode` NVARCHAR(10) UNIQUE/`Description` NVARCHAR(400)/`CreatedBy`/`CreatedAtUtc`/`UpdatedAtUtc` nullable) — nothing to add via this project's own EF migrations, same as every other pre-existing `log.*` table this migration only reads/writes (`NexusMigrationContext` only ever manages the Nexus-DB portal-auth/permission-group tables, never NexusOperations).
- **New `Helpers/Logistics/CustomsAdminHelper.cs`** — `ListVatOverridesAsync`/`CreateVatOverrideAsync`/`UpdateVatOverrideAsync`/`DeleteVatOverrideAsync` and the same four for HS descriptions, plus `LookupVatOverrideAsync`/`LookupHsDescriptionAsync` (never throw on a missing/blank key — a normal, expected outcome the eventual `customsreport.js` port will call in-process per report line, same role as Node's own exported `lookupVatOverride`/`lookupHsDescription` functions, just a direct C# method call instead of an ES module import). Unique-constraint violations on `ConsigneeCode`/`CommodityCode` are caught by matching the real constraint name in the exception message (`UQ_CustomsVatNumberOverrides_Consignee`/`UQ_CustomsHsCodeDescriptions_Code`) and rephrased into the same friendly message Node produces — same precedent as `MaterialRequestUnitsHelper`'s `IsDuplicateKeyViolation`.
- **New `Controllers/CustomsReportAdminController.cs`** at `api/customs-report-admin` (Node's exact mount path), class-level `[Authorize(Policy = "Perm:LOG_ADMIN")]` on every action — matching every `requirePermission('LOG_ADMIN')` call in Node exactly. Uses the literal `LOG_ADMIN` legacy code string directly, not a new per-tile code — same deferred-tile-split precedent every other Logistics sub-phase so far has followed (8a/8b/8d all use Logistics's legacy codes as literal strings with no `SeedLogisticsPermissions` migration yet; see the flagged cross-cutting Production/Warehouse permission-code-migration gap this phase already carries, tracked for Phase 10).
- **New `Models/Dto/CustomsAdminModels.cs`**: `CustomsVatOverrideRow`, `CreateCustomsVatOverrideRequest`, `CustomsHsDescriptionRow`, `CreateCustomsHsDescriptionRequest`.
- 8 new tests (`CustomsAdminHelperTests`) — every pre-connection validation guard on the four write methods (missing/blank `consigneeCode`/`vatNumber`/`commodityCode`/`description`) plus both lookup methods' blank-key short-circuit, all against the same `Mock<INexusOperationsDb>`-whose-`CreateConnectionAsync`-throws precedent established by `WarehousePicksheetHelperTests`/`PalletPackagesHelperTests`. The plain list/create/update/delete SQL itself is not directly unit-tested, consistent with this migration's established precedent for simple reference-data CRUD (`LogisticsReferenceHelper`/`MaterialRequestUnitsHelper` have no tests of their own either). Real HTTP smoke test confirms all 4 new routes (`GET`/`POST vat-overrides`, `GET`/`DELETE hs-descriptions/{id}`) return 401 unauthenticated with no 404/500; `dotnet build`/`dotnet test` both clean (549 tests total, 8 new).
- Next: 8c.2, the French VAT/DDP Customs Report generation itself (`routes/customsreport.js`'s Excel upload → 3-round SAP enrichment via SapServer's `/api/customs/*` endpoints → weight apportionment → CUSTOMS-format `.xlsx` export, stateless, nothing persisted) — this admin CRUD's `LookupVatOverrideAsync`/`LookupHsDescriptionAsync` are what that slice will call for its VAT/HS fallback resolution. Then `routes/freightbooking.js` (Kuehne+Nagel OAuth booking + document upload, 558 lines — budget real scrutiny, a genuinely new external OAuth integration, no existing C# client to build on) and finally `routes/clearportexport.js` (a simple `/v1/cds/exports` proxy) — **confirmed NOT reusable via the existing `IClearPortClient`/`ClearPortClient`** built for Sub-phase 8a.5c: that client authenticates with an `X-API-Key` header (matching `routes/shipmentmain.js`'s own two ClearPort calls, lines 987/1064, same env var), while `clearportexport.js` authenticates the *identical* `/v1/cds/exports` endpoint with `Authorization: Bearer <CLEARPORT_API_TOKEN>` instead — a real, confirmed inconsistency already present in the Node app itself (two different route files, two different auth header conventions, same token value), not something to silently unify when porting. `clearportexport.js` will need its own small, separate raw-proxy client preserving that exact Bearer-auth behavior, not a reuse of `CreateExportAsync`.

### Sub-phase 8c.2: French VAT / DDP Customs Report generation

Port of `routes/customsreport.js` in full. Upload a Shipments-style extract → 3-round SAP enrichment → apportion the manually-entered weight across delivery lines → resolve VAT/HS fallbacks → stream back a finished CUSTOMS-format `.xlsx`. Stateless — nothing from an upload is persisted, the one piece of this sub-phase genuinely lower-risk than a write.

- **New `Helpers/Logistics/CustomsReportHelper.cs`** — `ParseShipmentsUpload` (ClosedXML, header-text-matched columns A:D, same "match by header text not position" precedent as `MrpForecastHelper`/`OrderBookNotesUploadHelper`'s own upload parsers); `FetchSapDataAsync` (3 rounds — LIPS+LIKP parallel, then VBFA+MARC+KNA1 parallel, then VBRK keyed by invoices found in round 2 — reusing `LipsRow`/`LikpRow`/`VbfaRow`/`MarcRow`/`Kna1Row` from `ClearPortModels.cs`/Sub-phase 8a.5c unchanged, plus two new rows `VbrkRow`/`ConsignmentPriceRow`); `BuildReportRowsAsync` (assembles one row per LIPS line, the consignment-price fallback for invoice-less lines, then VAT/HS resolution via `CustomsAdminHelper`, Sub-phase 8c.1); `ApportionWeights` (pure, the workbook's exact `ROUND(SUMIFS(weight)/SUMIFS(qty)*lineQty, 2)` formula); `BuildWorkbook` (ClosedXML, same header/zebra-stripe styling — `#1F3864` header fill, `#E9EEF4`/white alternating rows — `MrpForecastHelper.BuildSalesForecastTemplate` already established for this app's Excel exports, which happens to already match Node's own `HEADER_FILL`/`ODD_FILL` constants exactly).
- **Unlike `SapCustomsDataHelper`'s ClearPort use case (8a.5c), which throws on a partial/failed SAP round since it genuinely can't proceed without complete data, every SAP round here collects a warning and keeps going** — this report is reviewed by a human before being sent on, so a bad/missing field degrades one line rather than aborting the whole run. The one true hard-failure case, matching Node exactly, is zero LIPS rows for the whole uploaded batch (`NexusUnprocessableEntityException`, 422) — nothing at all to build a report from.
- **`ParseSapNumber` is a faithful port of Node's own known-limitation decimal parser** (always assumes European grouping — strips `.` then swaps `,` for `.`) rather than the corrected last-separator-wins algorithm `WarehousePicksheetHelper.ParseSapQuantity` uses elsewhere in this migration — same faithful-port precedent this exact department already chose for `ClearPortShipmentPayloadHelper.ParseEuropeanDecimal` (8a.5c), not a fresh decision.
- **New `Models/Dto/CustomsReportModels.cs`**: `VbrkRow`, `ConsignmentPriceRow`, `CustomsShipmentUploadRow`, `CustomsReportSapData`, `CustomsReportResult`, and a mutable `CustomsReportRow` class — same "mutable DTO for a multi-pass enrichment pipeline" precedent as `PerformanceSyncModels`' `SapPerformanceStockRow`/`SapAgreementRow` family (8b.6), since the consignment-price fallback and weight apportionment both mutate a row built in an earlier pass rather than replacing it.
- **New `Controllers/CustomsReportController.cs`** at `api/customsreport` (Node's exact mount — no dash, unlike 8c.1's `api/customs-report-admin` sibling), gated `Perm:LOG_CUSTOMS_REPORT` (the literal legacy code, same deferred-tile-split precedent as 8c.1's `LOG_ADMIN`). `POST /generate` reads the raw uploaded bytes off `Request.Body` (same raw-body-read pattern as `MrpAnalysisController.UploadBomForecast`/`PerformanceController.UploadOrderBookLineNotes`, standing in for Node's `express.raw()` body parser), wraps the whole pipeline in try/catch to audit `SAP_OK`/`SAP_ERROR` around success/failure exactly like Node's own handler, then rethrows on failure so the global exception middleware still produces the correct HTTP response.
- 28 new tests (`CustomsReportHelperTests`) — `Digits`/`ParseSapDate`/`ParseSapNumber` fully exercised pure, `ApportionWeights`'s proportional-split/rounding/zero-quantity/missing-delivery cases, and `ParseShipmentsUpload` against real in-memory `.xlsx` fixtures built with ClosedXML (Shipments-sheet match, first-sheet fallback, blank-picksheet-number skip, missing-required-column validation error). `FetchSapDataAsync`/`BuildReportRowsAsync` themselves need a live SapServer + DB, the same testability ceiling as every other SAP-orchestration Helper in this migration. Real HTTP smoke test confirms `POST /api/customsreport/generate` returns 401 unauthenticated with no 404/500; `dotnet build`/`dotnet test` both clean (577 tests total, 28 new).
- **Sub-phase 8c.2 complete.** Next: `routes/freightbooking.js` (Kuehne+Nagel OAuth booking + document upload), then `routes/clearportexport.js` (its own small Bearer-auth proxy, per 8c.1's note above).

### Sub-phase 8c.3: Kuehne+Nagel freight booking

Port of `routes/freightbooking.js` in full — the genuinely new external OAuth integration this sub-phase's own scoping note flagged as needing real scrutiny. Creates a KN freight booking for a shipment (cargoItems from either its linked `PalletMain` rows or, for a Manual Outbound Shipment, its `ManualCargoItem` rows) and uploads the confirmed invoice/packing-list/customs documents against that booking afterward.

- **New `Services/KuehneNagelOptions.cs`** (`ApiUrl`/`CustomerId`/`CustomerKey`/`Secret`, config section `KuehneNagel`) + **`Services/KuehneNagelClient.cs`** (`IKuehneNagelClient`) — a fresh typed `HttpClient`, not a variant of `IClearPortClient` (a completely different external system/API). `GetAccessTokenAsync` posts `grant_type=client_credentials` to KN's hardcoded OAuth endpoint (`https://portal.api.kuehne-nagel.com/oauth2/token`, not configurable — matches Node, which never reads it from an env var either) with `Authorization: Basic <Secret>` — `Secret` is already the full base64 value Node sends as-is (`KN_SECRET_64`), never re-encoded by this app. `CreateBookingAsync`/`UploadDocumentAsync` are deliberately thin: return the deserialized 2xx response body as a loose `Dictionary<string, object?>` and let `FreightBookingHelper` decide what an in-band failure (`bookingIsSuccessful`/`uploadIsSuccessful: false` on an otherwise-2xx response) means — that's response-shaping, not a generic HTTP-client concern. Every genuine HTTP/network failure collapses to a 502 `KuehneNagelException`, embedding the real KN status/detail in the message text — same "collapse an upstream failure to 502 regardless of the real upstream status" precedent `ClearPortClient.CreateExportAsync` already established for this app's other external logistics integration, not a fresh decision. All outgoing JSON uses explicit `JsonSerializerDefaults.Web` (camelCase) — written carefully rather than copying `ClearPortClient`'s own `JsonContent.Create(payload)` call, which has no explicit options and may already be sending PascalCase to ClearPort undetected (a pre-existing, separately-flagged, unverified-either-way gap in that Helper, not something this slice's own client should risk repeating).
- **New `Helpers/Logistics/FreightBookingHelper.cs`** — `MapPalletsToCargoItems`/`MapManualCargoToCargoItems` (both convert `ShipmentContextPalletRow`/`ManualCargoItemRow`'s cm-based `PalletLength`/`PalletWidth`/`PalletHeight`/`Length`/`Width`/`Height` to KN's expected mm via ×10, matching Node's own `* 10` exactly); `BuildBookingPayload` (pure — origin/destination addresses, the shipment's own ID as the shipper's `ABO` reference, an EXW-aware pickup date — actually just whichever of an explicit override or the shipment's own `PlannedCollection` is set, no EXW branching here since KN's booking payload has one pickup location regardless of incoterm, unlike the PO-PDF's EXW/ready-to-collect distinction from 8b.7); `MaskCustomerKey`/`RedactBookingPayload` (same masking convention as the document-upload payload's own redaction — `requestPayload` is echoed back to the browser so an operator can check what was actually sent, but the raw KN secret must never leave the server to do that); `ExtractTrackingNumber` (first non-blank value across KN's several possible identifier field names, in Node's own priority order). `CreateBookingAsync`/`UploadDocumentsToKnAsync` are the async orchestration — re-fetches the shipment context fresh via `ShipmentHelper.GetShipmentContextAsync`/`GetShipmentByIdAsync` (Sub-phase 8a.1/8a.3) and `GetShipmentFolderInfo` (8a.1) rather than duplicating any of that, and reuses `ShipmentHelper.WriteShipmentEventAsync` for the `KN_DOCUMENT_UPLOAD`/`KN_DOCUMENT_UPLOAD_FAILED` events, wrapped in the same silent-catch Node's own `.catch(() => {})` uses (a ShipmentEvent write failing must never itself fail the upload response).
- **`?dryRun=true` on the document-upload route skips the actual KN POST (and skips logging any ShipmentEvent) but still resolves the shipment's export folder, stats each file on disk, and fetches a real OAuth token** — a bad `fileName`/category/token shows up in the returned preview instead of as an opaque KN error, matching Node's own reasoning for this flag exactly (safe to hit repeatedly while debugging without spamming KN with real uploads).
- **New `Models/Dto/FreightBookingModels.cs`**: the full nested KN booking-payload shape (`KnCargoItem`, `KnBookingFlags`, `KnIncoterm`, `KnAddress`, `KnPartyReference`, `KnShipperParty`/`KnConsigneeParty`/`KnPickupLocation`/`KnDeliveryLocation`, `KnBookingPayload`) rather than a loose `object`/`Dictionary`, so the payload's field names are locked in and directly assertable in tests without a JSON round-trip; `CreateBookingResult`, `UploadDocumentsToKnRequest`/`Result`, `KnDocumentUploadPayload`, `KnUploadedDocument`/`KnFailedDocument`/`KnDocumentUploadPreview`.
- **New `Controllers/FreightBookingController.cs`** at `api/freight-booking` (Node's exact mount). `POST /shipment/{shipmentId}` has **no permission gate beyond being logged in**, matching Node's own mount (`requireLogin` only — no `requirePermission` call on this specific route, unlike almost everything else in this sub-phase) — confirmed by reading the route handler directly rather than assuming a gate exists. `POST /{shipmentId}/documents/upload-to-kn` is gated `Perm:LOG_PLANNING`, matching Node exactly.
- 22 new tests (`FreightBookingHelperTests`) — `MapPalletsToCargoItems`/`MapManualCargoToCargoItems`'s field mapping and cm→mm conversion, `BuildBookingPayload`'s address/reference/pickup-date-override logic and all-flags-false assertion, `MaskCustomerKey`/`RedactBookingPayload`, and `ExtractTrackingNumber`'s key-priority-order and blank-value-skip behavior — all fully pure, no DB/HTTP needed. `CreateBookingAsync`/`UploadDocumentsToKnAsync` themselves are DB+external-API orchestration with no mock built, consistent with every other real-external-API Helper in this migration. Real HTTP smoke test confirms both new routes return 401 unauthenticated with no 404/500; `dotnet build`/`dotnet test` both clean (599 tests total, 22 new).
- **Unverified** (same class of gap as every other external integration in this migration, and explicitly the one this sub-phase's own scoping flagged as needing the most real-world scrutiny): the OAuth token exchange, the booking payload's exact field names/values against KN's real BookingRoad OpenAPI spec, and the document-upload flow have never been exercised against a live KN sandbox or real credentials. Validate a real booking + document upload end-to-end before trusting this route in production.

### Sub-phase 8c.4: ClearPort raw CDS export proxy — the last slice of Phase 8 (Logistics)

Port of `routes/clearportexport.js`, the smallest and simplest file left in Sub-phase 8c. A thin, stateless proxy: the caller assembles the full ClearPort CDS declaration payload itself (never built server-side, unlike `ShipmentCustomsHelper`'s own ClearPort flow from 8a.5c) and this route validates `items`/`exporter` are present, forwards everything else blindly to `POST {ApiUrl}/v1/cds/exports`, and returns ClearPort's response verbatim.

- **New `Services/ClearPortExportProxyClient.cs` (`IClearPortExportProxyClient`) — a genuinely separate client from `IClearPortClient` (8a.5c), not a reuse of `CreateExportAsync`, confirmed necessary rather than assumed:** both hit the *identical* `/v1/cds/exports` endpoint with the *same* `CLEARPORT_API_URL`/`CLEARPORT_API_TOKEN` config values (so this new client still binds the existing `ClearPortOptions`), but authenticate completely differently — `IClearPortClient` sends `X-API-Key` (matching `routes/shipmentmain.js`'s own two ClearPort calls, lines 987/1064), while `clearportexport.js` sends `Authorization: Bearer <token>` instead. This is a real, confirmed inconsistency already present in the Node app itself (two different route files, two different auth conventions, same token value) — preserved deliberately rather than silently unified when porting, flagged from the moment this was discovered back in 8c.1's own scoping notes. Reuses `ClearPortException` (8a.5c) for every failure path — 503 (not configured), 502 (401/network — ClearPort's real 401 is remapped to a clearer message, matching Node), 429 (rate limit, passed through as-is), and the real ClearPort status for anything else (embedded in the message text, since the global exception middleware collapses every thrown error to just `{code, message}` regardless — same established simplification as every other error path in this migration, not a regression specific to this slice).
- **New `Helpers/Logistics/ClearPortExportProxyHelper.cs`** — `ValidatePayload` (pure, takes the raw request body as a `System.Text.Json.JsonElement` rather than a typed DTO, since Node itself never models this payload's shape beyond checking `items`/`exporter`): rejects a non-object body, a missing/empty/non-array `items`, and a missing/null `exporter` — matching Node's three validation checks exactly.
- **New `Controllers/ClearPortExportProxyController.cs`** at `api/clearport` (Node's exact mount), `POST /exports`. No permission gate beyond being logged in, matching Node's own mount (`requireLogin` only) — same "no extra gate" precedent 8c.3's booking-creation route already set. Accepts `[FromBody] JsonElement` directly (ASP.NET Core's native `System.Text.Json` binding support for arbitrary JSON) rather than a typed request DTO, since the payload's exact shape is the caller's responsibility, never this app's.
- 7 new tests (`ClearPortExportProxyHelperTests`) — every validation branch of `ValidatePayload`, built against real `JsonElement` fixtures parsed from literal JSON strings rather than hand-constructed objects (there's no clean way to hand-build a `JsonElement` directly). Real HTTP smoke test confirms `POST /api/clearport/exports` returns 401 unauthenticated with no 404/500; `dotnet build`/`dotnet test` both clean (606 tests total, 7 new).
- **Unverified against a live ClearPort sandbox**, same caveat class as `IClearPortClient`/`KuehneNagelClient`.

**Sub-phase 8c (Customs) is now complete — 8c.1 (admin CRUD), 8c.2 (report generation), 8c.3 (KN freight booking), and 8c.4 (ClearPort proxy) all done. This completes Phase 8 (Logistics) in full**, across every sub-phase (8a Shipping, 8b Purchasing/Performance, 8c Customs, 8d Reference data, 8e Vendor Consignment Tracker) — apart from the pieces each sub-phase's own section above already flags as deliberately out of scope (`GET /analytics`, the live-formula orderbook Excel export) or genuinely unverified pending a live SAP/SapServer/ClearPort/KN environment. Next: Phase 9 (Admin + Management + full permission-group CRUD UI).

## Phase 9: Admin + Management

Different frontend pattern from every department phase (sidebar-sections/dashboard-panels, not tile grids) and different backend scope too — this phase covers the four Node route files server.js mounts under `/api/admin*`/`/api/deploy`, not a SAP-calling department. Sequenced last among the backend phases per the original plan (superadmin-only/lower-traffic, benefits most from every convention already being settled). Full permission-group CRUD UI (the plan's other big Phase 9 deliverable, building on Phase 1's minimal `Pages/Admin/PermissionGroups.cshtml`/`PermissionGroupAdminService`) is still pending — tracked below.

### Sub-phase 9a: User & Permission Administration

Port of `routes/useradmin.js` (1255 lines) in full — by far the largest single file in this phase, and the one the plan's "full permission-group CRUD UI" language was really describing the data layer underneath. Mounted at `api/admin` per server.js's real `app.use('/api/admin', requireLogin, requireRole('admin'), adminRoutes)` — the Node file's own header comment claiming `/api/useradmin` is stale and was **not** trusted; confirmed by reading server.js directly instead.

- **New `Controllers/UserAdminController.cs`**, class-level `[Authorize(Policy = "Role:admin")]` (matching `requireRole('admin')`), with `[Authorize(Policy = "Role:superadmin")]` overrides on permission-definition CRUD (`POST`/`PUT`/`DELETE /permissions*`) and `POST /users/bulk-create` — matching every one of the file's own per-route `requireSuperadmin` calls exactly, confirmed by reading each handler rather than assuming a uniform gate.
- **New `Helpers/Admin/UserAdminHelper.cs`** covers all thirteen endpoints: pending-user list/approve/reject, the full user list (departments/permissions joined in-memory from three parallel queries, matching Node's own `Promise.all` shape), the single-user `PUT` (the most complex handler in the file — role-hierarchy checks, username/email uniqueness, a cascade rename across `PortalAuditLog`/`PortalUsers.ApprovedBy`/`PortalUserDepartments.GrantedBy`, a full department-list replace, and up to five separate audit events depending on which fields actually changed), bulk-create (transactional per-row, bcrypt-hashed via `BCrypt.Net.BCrypt.HashPassword(password, workFactor: 12)` — same work factor `AuthService.ChangePasswordAsync` already uses — with an in-batch username/email de-dup pass before ever touching the DB), bulk-department-grant, bulk-status-set, the filterable audit-log query, permission-definition CRUD, and user↔permission grant/revoke/bulk-grant.
- **Role-hierarchy comparisons Node does inline in each handler (`ROLE_LEVEL[role] >= actorLevel`) are reused via the existing `NexusRoles.LevelOf`/`NexusRoles.Satisfies`** (Phase 1's own role-hierarchy constants, `Services/Auth/NexusClaimTypes.cs`) rather than redefining the hierarchy a second time — extracted into two pure, directly-tested helpers, `ActorCanEditTargetRole`/`ActorCanAssignRole`, since Node's own inline boolean expression appears at four separate call sites (`PUT /users/{id}` twice, `POST /users/{id}/approve` once, `POST /users/bulk-status` once) and would otherwise drift.
- **`NexusControllerBase` gained `GetRole()`** (reads `ClaimTypes.Role` off the authenticated user, mirroring the existing `GetUserId()`/`GetUsername()` pattern) — the first controller in this migration whose own handler logic needs the actor's role value directly, not just a declarative `[Authorize(Policy = "Role:x")]` gate.
- **Bulk-grant duplicate detection catches `SqlException` with error number 2627 or 2601** (unique-constraint/unique-index violation) **or a message containing "UNIQUE"/"duplicate"** — matches Node's own `dupErr.number === 2627 || dupErr.message?.includes('UNIQUE') || dupErr.message?.includes('duplicate')` check exactly (2627 is a real unique-constraint violation, 2601 its unique-*index* sibling — Node's own check only names 2627, but `Microsoft.Data.SqlClient` can raise either for `UX_UserDept`/`UQ_UserPermission`, so both are treated as "already held" here rather than risking a raw 2601 propagating as an unhandled 500 that Node's own code would never have hit under `mssql`'s slightly different error surfacing).
- **New `Models/Dto/UserAdminModels.cs`** — every request/row/result shape, including `UpdateUserRequest`'s deliberate two-tier null semantics (role/isActive/isLocked/notes/departments/shortIdleTimeout: null means "leave unchanged", matching Node's `?? prev.X`; username/firstName/lastName/email: any non-null value — including an explicit empty string — means "this field was present in the request", matching Node's `!== undefined` checks, since a plain nullable C# property can't distinguish "omitted" from "explicitly null" the way JS can. This loses Node's one edge case of explicitly clearing firstName/lastName back to blank via a literal `null` in the JSON body specifically — the same "field omitted vs. explicitly null collapse" simplification `UpdateManualCargoItemRequest` (8a.2) already flagged, not a new gap this slice introduces).
- 52 new tests (`UserAdminHelperTests`) — every pure validator (`IsValidRole`/`IsValidDepartment`/`AreValidDepartments`/`IsValidUsername`/`IsValidEmail`/`IsValidPermissionCode`/`IsValidAuditEvent`/`IsStrongEnoughPassword`) and both role-hierarchy helpers (`ActorCanEditTargetRole`/`ActorCanAssignRole`), fully exercised with no DB needed. The DB-orchestration methods themselves (`UpdateUserAsync`, `BulkCreateUsersAsync`, etc.) have no mock built, consistent with this migration's established precedent for DB-heavy orchestration Helpers. Real HTTP smoke test confirms all 8 sampled new routes return 401 unauthenticated with no 404/500 (`GET pending/users/audit/permissions`, `PUT users/{id}`, `POST users/bulk-create`, `POST`/`DELETE users/{id}/permissions*`); `dotnet build`/`dotnet test` both clean (658 tests total, 52 new).
- **Not yet built**: the full permission-group CRUD *UI* the plan calls for (group list/create/edit/delete with a proper permission-checkbox grid, bulk-assign-groups-to-users, folded into this same admin surface) — Phase 1's `PermissionGroupAdminService`/`Pages/Admin/PermissionGroups.cshtml` already covers the minimal version; extending it to the full Phase 9 UX is tracked as a follow-up slice, not done in this one (this slice's own scope was the 1255-line `useradmin.js` port). Frontend for `useradmin.js`'s own surface (the admin user-management page itself) is likewise deliberately deferred, same precedent as every department phase since Engineering.
### Sub-phase 9b: DB Explorer + BAPI Inspector

Port of `routes/dbexplorer.js` (357 lines) and `routes/bapiInspector.js` (100 lines), both mounted under `api/admin/*` per server.js but gated `Role:superadmin` specifically — stricter than `UserAdminController`'s blanket `Role:admin` class-level gate, matching each file's own `requireSuperadmin` call exactly (confirmed by reading both files directly rather than assuming the outer `api/admin` mount's `requireRole('admin')` is the real gate).

- **Checked whether SapServer already has something reusable for `dbexplorer.js`'s generic SQL browser, per the original migration plan's explicit instruction — it doesn't.** SapServer never touches this app's own portal SQL Server at all (it only talks to SAP via NCo), so there was nothing to reuse; this needed building fresh, exactly as Node has it.
- **New `Helpers/Admin/DbExplorerHelper.cs`** — `Bracket` (pure, T-SQL identifier escaping — doubles any `]`), `VerifyDatabaseAsync`/`VerifyTableAsync` (the two-step "look up in `sys.databases`/`sys.tables` first, only interpolate the *verified* canonical name" pattern Node's own header comment describes as the whole SQL-injection defense here, since database/schema/table names can't be parameterized the normal way), then `ListDatabasesAsync`/`ListTablesAsync`/`ListColumnsAsync`/`GetConstraintsAsync` (keys, foreign-keys-out, foreign-keys-in, check constraints, indexes — same SQL Server 2005-compatible `STUFF(...FOR XML PATH(''))` column-list concatenation this codebase already uses elsewhere, no `STRING_AGG`) and `PreviewRowsAsync` (`TOP (N)` clamped 1-500 server-side regardless of what's requested, audited on both success (`DBEXPLORER_PREVIEW`) and failure (`DBEXPLORER_PREVIEW_ERROR`) — the one route here that returns actual row data, not just metadata). One connection to the default Nexus database is enough for every database on the instance, via three-part names (`[NexusOperations].sys.tables`) — confirmed against `INexusDb`/`INexusOperationsDb`/`INexusArchiveDb` all pointing at the same server, different databases, so no per-database connection pool was needed here either.
- **Preview rows are returned as a loose `IReadOnlyList<Dictionary<string, object?>>`**, not a typed DTO — the whole point of this route is browsing an arbitrary table's arbitrary columns, so there's no fixed shape to model. Dapper's non-generic `QueryAsync` returns rows backed by an `IDictionary<string, object>` under the hood; each row is copied into a plain `Dictionary<string, object?>` for JSON serialization.
- **New `Helpers/Admin/BapiInspectorHelper.cs`** — confirmed reusable exactly as Node's own header comment claims: a thin proxy in front of SapServer's already-existing `GET /api/function/params` (`FunctionController`), no new SapServer work needed. `ISapServerClient` already unwraps SapServer's `{success, data, error}` envelope and throws `SapProxyException` on failure, so this Helper only adds the `functionName` blank-guard and the `SAP_OK`/`SAP_ERROR` audit wrapping Node's own handler does — the response body itself is passed through verbatim as a loose `Dictionary<string, object?>`, never modelled field-for-field, matching Node's own thin-proxy behavior.
- **New `Controllers/DbExplorerController.cs`** (`api/admin/dbexplorer`) and **`Controllers/BapiInspectorController.cs`** (`api/admin/bapi-inspector`), both class-level `[Authorize(Policy = "Role:superadmin")]`.
- 7 new tests (`DbExplorerHelperTests` — `Bracket`'s escaping including the doubled-`]` case; `BapiInspectorHelperTests` — the blank-`functionName` guard against `Mock<ISapServerClient>`/`Mock<IAuditLogger>` set to `MockBehavior.Strict`, proving neither is ever touched). Every DB-querying method in `DbExplorerHelper` needs a live SQL Server with real cross-database visibility to test meaningfully — consistent with this migration's established precedent of not unit-testing raw schema-query internals, only pure logic and pre-I/O guards. Real HTTP smoke test confirms all 6 new routes return 401 unauthenticated with no 404/500; `dotnet build`/`dotnet test` both clean (665 tests total, 7 new).
- Next: `deploy.js` — read in full, see below for why it's deliberately NOT ported as a Phase 9 slice.

### `deploy.js` — read, deliberately deferred to Phase 10 rather than ported here

Read in full (268 lines). Confirmed this is genuinely Node-hosting-specific, not a generic admin feature that happens to be written against Node: the entire mechanism is "schedule a future `git pull` + restart of the Normanton Nexus **Windows Service**" (`dbo.ScheduledDeployments`, polled every minute by a `node-cron` checker in `server.js`, handed off to a detached `deploy-runner.cjs` process) — a design that exists specifically to work around `node-windows`/WinSW's unreliable restart semantics, exactly the failure mode the migration plan's own Architecture section already flags as **not applicable** to this rebuild (IIS/ANCM hosting has no "restart a Windows Service" concept, and a running IIS-hosted app self-updating via `git pull` while serving requests is not how a `dotnet publish`-based deploy works at all — see `scripts/deploy.ps1`'s stop-apppool → publish → start-apppool → warm-up flow instead).

The plan's own words on this: *"The 2 deploy-related cron jobs exist specifically to work around node-windows/WinSW's unreliable restart semantics — that specific failure mode... goes away entirely once hosting is IIS/ANCM... But IIS introduces its own restart-adjacent risk in its place — app-pool recycling... don't just delete them outright: re-evaluate in Phase 10 whether a much simpler replacement is still warranted."* This is a straight instruction to defer the *design question* (does this app need a scheduled-maintenance-window concept at all, and if so what should it look like against IIS app-pool recycling instead of a Windows Service restart) to Phase 10, not a port to mechanically carry out now — porting `ScheduledDeployments`/the countdown banner/the cancel flow as-is would encode a Windows-Service-restart assumption this app's hosting model no longer has. Confirmed by reading the file directly rather than assuming; the timezone-handling logic (`SITE_NOW_SQL`'s EU-DC-vs-UK-site DATEADD correction) would still be relevant to *whatever* Phase 10 designs, so it's noted here for that future work rather than discarded.

**Phase 9's backend is complete** across the three files that are genuinely in scope for it (`useradmin.js` — 9a, `dbexplorer.js`/`bapiInspector.js` — 9b); `deploy.js` is correctly out of this phase's scope per the reasoning above, tracked instead under Phase 10.

**Not yet built, deliberately deferred as separately-scoped frontend effort** (same precedent as every department phase since Engineering): Admin's own Razor Pages surface for `useradmin.js`/`dbexplorer.js`/`bapiInspector.js`, and the full permission-group CRUD UI (group list/create/edit/delete with a proper permission-checkbox grid, bulk-assign-groups-to-users) the original plan calls for here. Worth flagging a real sequencing tension discovered while closing this phase out, not just restating the plan: the plan assumed every department's tiles would already carry real fine-grained permission codes by the time Phase 9 built the UI to manage them ("by this phase every department already has real per-tile codes and default groups for it to manage") — but Production, Warehouse, and Logistics (the three largest departments) all deferred that per-tile-code split to Phase 10's own cross-cutting catch-up (see Phase 7/8's own flagged gaps). Building the *full* polished group-management UI now would mean managing group membership for permission codes that don't exist in fine-grained form yet for three of eight departments — so the full UI is deferred alongside the general frontend backlog, not built prematurely against an incomplete backend.

## Build & Test

```bash
cd dotnet
dotnet build
dotnet test
```

Target framework: `net10.0`, `Microsoft.NET.Sdk.Web` (unlike SapServer's net48 + plain `Microsoft.NET.Sdk`, this SDK produces a correct IIS-ready `dotnet publish` layout automatically — `web.config` with the ASP.NET Core Module v2 `<aspNetCore>` handler, appsettings copied to the publish root — confirmed via a real local `dotnet publish -c Release -o <dir>`, no relocation MSBuild target needed the way SapServer needed one).

Local smoke-testing: `dotnet run --urls http://127.0.0.1:<port>` then `curl`/browser against it works out of the box (no HTTPS dev-cert setup needed) — see the cookie `SecurePolicy` note under Phase 1.

## Hosting

**IIS via ASP.NET Core Module v2, in-process hosting model** (the ASP.NET Core Web App template's default `hostingModel="inprocess"` in the generated `web.config` — the app runs directly inside the IIS worker process, not as a separate Kestrel process behind a reverse proxy). No `UseWindowsService()` — a deliberate departure from how the Node app is hosted today (a genuine Windows Service via `node-windows`/WinSW), per explicit user preference, and also means this app ends up hosted the same way as the sibling `SapServer` repo (both IIS sites), even though the two stay separate deployed apps.

IIS hosting carries real gotchas already confirmed painfully for real on SapServer this same session (see `/home/user/SapServer/CLAUDE.md`'s "Critical Platform Constraints") that this app's own `install.ps1`/app-pool config will need to account for once they're written:
- App pool default `OnDemand` start mode delays all startup code (DI, any background scheduler) until the first real request — needs `startMode=AlwaysRunning` + `preloadEnabled=true` + IIS Application Initialization (the `/health` endpoint above is what it would warm).
- `ApplicationPoolIdentity` needs explicit `icacls` grants (read+execute on site root, Modify on any writable path) and `processModel.loadUserProfile=$false`.
- App-pool recycling can kill an in-flight Quartz.NET job — a risk the old Windows-Service hosting didn't have. Needs either `idleTimeout=00:00:00` + recycling disabled, or every scheduled job made safely resumable across a restart. Not yet decided — no Quartz.NET jobs exist yet (Phase 10).

## Project layout

```
NormantonNexus.slnx
NormantonNexus/
  Data/                     EF Core migration tooling only (Data/Migrations/) — see Data/NexusMigrationContext.cs
  Services/Sql/             Dapper connection factories (INexusDb/INexusOperationsDb/INexusArchiveDb)
  Services/Auth/            Cookie auth, SQL-backed session store, idle timeout, role/department/permission authorization, SAP-credential cipher
  Services/                 SapServerClient.cs (root of Services/ — cross-department, not tied to one Helpers/ folder)
  Services/Admin/           Minimal permission-group management (Phase 9 gets the full CRUD UI)
  Controllers/              Thin [ApiController] JSON layer, one per department — Engineering, Quality, Sales, ProductionSchedule, Finance, StockCount, ProductionNexus so far
  Helpers/<Department>/     100% of each department's logic — Engineering/, Quality/, Sales/, ProductionSchedule/, Finance/, StockCount/, Production/ so far
  Models/Dto/               Wire DTOs, one file per department — Engineering, Quality, Sales, ProductionSchedule, Finance, StockCount, Production so far
  Models/                   ApiResponse<T>/NexusExceptions.cs (root — shared by every department)
  Middleware/               ApiExceptionMiddleware.cs
  Pages/                    Razor Pages — Login, Index (Hub), ChangePassword, Admin/, Engineering/, Quality/, Sales/, Finance/, Production/ (one page per tile) so far
  wwwroot/js/<department>/  One dedicated JS file per tile page (engineering/, quality/, sales/, finance/, production/ so far) + wwwroot/js/production-schedule/ (department-neutral — shared between Sales and Production) + wwwroot/js/shared/ (session-guard.js)
NormantonNexus.Tests/      xUnit, InternalsVisibleTo from NormantonNexus — mirrors SapServer.Tests's Helpers-first testing approach
```

`NormantonNexus.Tests` reaches `internal` `Helpers/*` types directly via `[InternalsVisibleTo("NormantonNexus.Tests")]` in `NormantonNexus.csproj`, same convention as `SapServer.csproj`. `NormantonNexus.Tests/Controllers/ControllerTestHelpers.cs` mirrors SapServer.Tests's `ControllerTestHelpers.SetUser` for direct-instantiation + Moq controller tests.

## CI

`.github/workflows/dotnet-test.yml` (repo root) — separate from the Node app's own `.github/workflows/test.yml`, scoped to `dotnet/**` changes on `dotnet-rewrite` and its PRs. Runs on `ubuntu-latest` (no Windows-only native dependency the way SapServer's SAP NCo requires): `dotnet restore && dotnet build && dotnet test`.
